# KD MTF P0 `b` 阻塞说明 v1

## 目的

- 把 `DY-R1 / KD_MTF_P0` 当前为什么长期补不出真实 `b` 样本写成独立阻塞页。
- 避免后续重复在同一批样本上盲扫。

## 当前口径

- 本页只记录现行口径下的阻塞证据，不修改任何逻辑。
- 当前 `b` 的判定仍是：
  - `day_signal` 有主信号：`golden_cross` 或 `death_cross`
  - `h4_confirm = none`
- 当前 `h4_confirm` 的实现仍是：
  - `h4_k > h4_d -> confirm_up`
  - `h4_k < h4_d -> confirm_down`
  - 其余才记为 `none`

## 当前工程结论

- 在现行实现下，`h4_confirm = none` 实际只会落在 `h4_k == h4_d` 的精确相等情形。
- 当前已扫真实浮点样本里，没有自然出现 `h4_k == h4_d`。
- 因此当前 `b = 0` 不是单纯“还没挑到合适 bar_time”，而是现行定义在真实样本里缺自然落点。

## 已扫样本结论

- 当前主样本总扫描统计：
  - `s=3587 / a=1856 / b=0 / conflict=8318 / unknown=73438`
- 当前总体结论：
  - `b=0`
  - `h4_confirm = none = 0`

## 外扩搜索结果

- 已在 `本机目录树` 范围扩扫过更长 `M1/M15` 连续窗口
- 当前没有找到新的可直接接入长窗口

## 后续接入条件

- 若后续继续补 `b`，优先接入新的真实 `M1/M15` 连续窗口
- 新样本建议至少满足：
  - 连续时间更长
  - 能稳定上卷成大量 `H1` bars
  - 保留原始浮点精度，不做额外四舍五入

## 当前不做的事

- 不修改 `b` 的定义
- 不修改 `h4_confirm` 的定义
- 不因为补不出 `b` 就回头碰策略门控
