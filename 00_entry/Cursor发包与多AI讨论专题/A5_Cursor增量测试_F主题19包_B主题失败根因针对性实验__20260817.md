═══════════════════════════════════════════════════════════════════
📦 F主题 / Cursor 第 9~12 张发包卡片 —— 增量测试共 19 包（全部基于B主题30包实锤失败根因，不凭空开新主题）
═══════════════════════════════════════════════════════════════════

诚实前置说明：所有19包严格对应B主题30包实锤失败点（粘贴.md L1-L83），每张卡开头钉死触发来源=B里哪一条红线；F1-F6/B01-B30冻结事实全部继承不推翻；子包默认4层验真，只有F1（原B08 GER40对照侧Calmar0.64整批最接近能用）强制7层全跑。

---

# ════════════════════════════════════════════════════════════════
# 📦 发包卡片 F1 / 4 —— 整批最接近能用的B08 GER40关EMA144（对照侧Calmar0.64）单独拎出来7层全跑+7窗
# ════════════════════════════════════════════════════════════════

> 用法：开第 9 个 Cursor 窗口
> 触发来源：B主题粘贴.md L35+L83 —— B08 GER40 EMA144开=0.47 / 关=0.64 Calmar，L83诚实写「关EMA144后Calmar0.64是整批最接近能用的数字，但那是对照侧不能把B08改成Pass」→ 诚实单独关EMA144组提出来7层全跑+7窗口验证

```
───────────────────────────────────────────────────────────────────
【Cursor 窗口 9 / 12】F1：B08 GER40 关EMA144组 Calmar0.64 → 强制7层全跑+7窗口扩窗
───────────────────────────────────────────────────────────────────
【身份】F1执行员：只跑GER40 关EMA144的TK Scheme B组合（P0锁参k=0.20/x=5/stop2.0/allow2），强制7层全跑，其余B/F窗口内容不接
【★触发来源诚实钉死】B主题L35参数敏感性：EMA144关/avg0.21/dd19.93/Calmar0.64 n=189；开EMA144才是B08的原判定，关EMA144是对照组单独拿出来当根因，诚实不做「改判B08 Pass」的事，只做「关EMA144能不能单独过7层」的实验

【★本地资料路径索引（仅本地Trae/Cursor可用）】
  ▶ P0 v6 70条RuleID 5字段完整原子集（k/x/stop/hb/E1所有冻结参数）= D:\Stock\trading_assistant\12_tooling_runtime_archive\batch_04_legacy_backtest_out__20260706\backtest_out\p1_final_validate3\P0_规则子表_for_core_v6.md
  ▶ GER40 baseline_summary.csv（原基线对照，B08的n=189关组和baseline的差）= 同目录ger40_full/baseline_summary.csv
  ▶ B08原验收= themeB L35

一、F1-F6锁定同上；EMA144**全程关掉**，不开（和B08的开EMA组严格隔离，不混算）；P0参数=原backtest_p0 P1锁参 k=0.20/x=5/stop2.0/E12.4/hb=2/allow2
二、7窗口扩法（和A01/E1一致）：12/15/18/20/22/25/28（原B08只跑了20）
三、强制7层全跑：
    ①5原：同原B08关组，再核一遍（无前视/双口径/EXIT=0/同分布/样本量189>50）
    ②20抽样跨19月：原误差±1.46pp → 诚实再跑，要求≤±1.0pp（比原松但要比原1.46好）
    ③★Spearman代理ρ≥0.80 → 原未跑，诚实补
    ④★滚动10折CV：avg波动≤±1pp；dd波动≤±5pp → 原未跑，诚实补
    ⑤黑天鹅3σ：原单笔avg+3.40%/笔过了 → 再核（压测组合dd≤25%）
    ⑥相邻7窗口：avg波动≤±1.0pp；dd波动≤±3pp（7窗）
    ⑦诚实误差块：(a)GER40是指数合成非真实MT4/5 (b)EMA144关闭（和原Scheme B配置不同，诚实不能直接用于A30/D30原组合）(c)实盘±3pp预期误差
四、验收成功门（诚实写：过7层才能说「关EMA144的GER40单品种」是C级可用，原B08依旧是Fail不做改判）：
    Calmar≥0.60；avg>0；dd<25%；7层里≤1层Fail
五、输出格式第一行=Pass/Fail/样本不足；附「原B08开组Calmar0.47 vs 本次关组Calmar=?」对比值
六、收尾第一行：【F1总执行】= [Pass/Fail/样本不足]；7层每层结果（过/否+数值）
───────────────────────────────────────────────────────────────────
```

---

# ════════════════════════════════════════════════════════════════
# 📦 发包卡片 F2 / 4 —— 大隐KD 5-50-95（B13-B18 n=0~4对照不成立）→ 换周期+改参数6包
# ════════════════════════════════════════════════════════════════

> 用法：开第 10 个 Cursor 窗口
> 触发来源：B主题粘贴.md L45-L55 —— B13-B18共6品种，大隐KD5-50-95侧n=0~4笔（因为外汇日线KD(5,3,3)几乎打不到5/95极值），对照全失败→诚实把周期从日线改成H1/4H，KD参数从(5,3,3)改成(14,5,5)让5/95阈值能打到，6包消融

```
───────────────────────────────────────────────────────────────────
【Cursor 窗口 10 / 12】F2：大隐KD 5-50-95对照失败根因修复 → 换周期+改参数6包
───────────────────────────────────────────────────────────────────
【身份】F2执行员：F2a-F2f共6包，全部子包级4层
【★触发来源诚实钉死】B主题L45-L55误差块(a)：「日线KDJ(5,3,3)几乎打不到5/95极值」→ 6品种5-50-95侧都是0~4笔，对照完全不成立→本卡=解决参数/周期错配问题，先让两边都有样本再谈对照

【★本地资料路径索引】
  ▶ 大隐三位一体KD 5-50-95完整体系定义（5超卖/50中轴/95超买+7层多周期共振网络）= D:\Stock\trading_assistant\11_frozen_summaries_archive\_raw_snapshot_batch09\01_初整理文档备份_禁止修改\体系大隐_随机指标.md
  ▶ B13-B18原验收=themeB L45-L55

一、F1-F6锁定同上；本卡6包全部做「5-50-95 vs 9-3-3」对照，先让两边样本量都≥50笔才允许判Pass/Fail
二、6包清单（按先改参数再改周期的顺序）：
    F2a XAUUSD：KD参数从(5,3,3)改成(14,5,5)，周期不变日线（先看参数能不能让5/95有样本）
    F2b XAUUSD：KD(14,5,5) + 周期从日线改H4（先粗到细的顺序）
    F2c XAUUSD：KD(14,5,5) + H1
    F2d GER40：KD(14,5,5) + 日线（和F2a同参数跨品种复制）
    F2e EURUSD：KD(14,5,5) + 日线
    F2f GBPUSD：KD(14,5,5) + 日线
三、验收门：
    前提：5-50-95侧样本n≥50；9-3-3侧n≥50（任意一侧<50→直接样本不足诚实标不判）
    成功门：5-50-95侧相对9-3-3侧 Calmar差≥0.2 且 dd降≥3pp（原成功门是差≥0.2+dd降≥5pp，本卡是修复参数先降低dd阈值诚实写）
四、红线里必须写：原B13-B18日线(5,3,3)的5-50-95 n=? vs 本次(14,5,5)的n=?（诚实证明样本量问题有没有被修）
五、收尾第一行：【F2总执行】= X Pass / Y Fail / Z 样本不足
───────────────────────────────────────────────────────────────────
```

---

# ════════════════════════════════════════════════════════════════
# 📦 发包卡片 F3 / 4 —— TK-R6/R7/R8标签（B25-B28 n=7612-58593非独立交易）→ 持仓合并去重6包
# ════════════════════════════════════════════════════════════════

> 用法：开第 11 个 Cursor 窗口
> 触发来源：B主题粘贴.md L69-L75 —— B27 AO背离n=7612（每8根打标签不是独立交易）；B28 B区域n=58593（每根1h打标签不是独立挂单）；B25/R6 B26/R6都是日首根1h代理有前视→诚实按「同方向连续持仓合并成1笔」去重，把标签转成真正独立交易再判6包

```
───────────────────────────────────────────────────────────────────
【Cursor 窗口 11 / 12】F3：TK R6/R7/R8 n过密非独立交易 → 持仓合并去重6包
───────────────────────────────────────────────────────────────────
【身份】F3执行员：F3a-F3f共6包，全部子包级4层
【★触发来源诚实钉死】B主题误差块：
  B25 L69：(a)IB用日首根1h代理≠London IB；(b)②只跨1月
  B27 L73：(a)n=7612是每8根打标签不是独立交易；(c)Calmar 2.38被近零dd放大不可信
  B28 L75：(a)n=58593不是独立挂单是每根1h打标签；(c)重复计数抬高统计功效
→ 本卡核心=把每根K线标签转成「连续同方向持仓=1笔独立交易」的合并去重算法，解决样本重复计数的过拟合

【★本地资料路径索引】
  ▶ TK-R6/IB 3档距离最小距离口径 v1 + TK-R8 B区域最小判据草案 v1 = D:\Stock\trading_assistant\12_tooling_runtime_archive\batch_04_legacy_backtest_out__20260706\backtest_out\p1_final_validate3\TK-R6_IB附近_最小距离口径_v1.md 和 同目录 TK-R8_B区域_最小判据草案_v1.md
  ▶ B25-B28原验收=themeB L69-L75

一、F1-F6锁定同上；本卡6包**必须先做持仓合并去重再统计**（否则直接Fail诚实写仍为非独立交易）
合并去重算法诚实钉死：
  1. 同一方向（多/空）连续标签出现 → 合并为1笔交易（入场=第一根标签的开，出场=最后一根标签的平）
  2. 同笔不重复计数，跨标签的相邻K线同方向也合并
  3. 合并后笔数必须≤原笔数的1/10（B27原7612 ≤761；B28原58593 ≤5859）。如果合并后仍>1/10→诚实写「合并算法仍有过密」Fail
二、6包清单：
    F3a TK-R6 IB far/near/inside 3档（原B25，n原326→合并后≤33笔再判）
    F3b TK-R6 IB touch/body/close对RR（原B26，同326→≤33）
    F3c TK-R7 AO背离 bull/bear/none 3档（原B27，n7612→≤761；解决近零dd放大Calmar的陷阱）
    F3d TK-R7 AO背离：把原步长从8改成32根（4倍稀释，先从源头减少过密标签，再合并）
    F3e TK-R8 B区域 qualified/weak/not 3档（原B28，n58593→≤5859）
    F3f TK-R8 B区域：把原1h滑窗改成4h滑窗（源头稀释4倍，解决重复计数）
三、验收门：
    合并去重后笔数≤原1/10（前提）
    R6：inside组成功率 - far组 ≥15pp（原7.2pp不够）且3档极差<10pp（原20.4pp）
    R7：负背离失败率 > 无背离失败率（原是负背离更低，和预期相反！诚实必须修过来才算Pass）
    R8：qualified - not ≥20pp（原8.7pp） 且 qualified RR≥1.5（原1.06）
四、红线里必须写：原笔数 vs 合并后笔数；原统计量 vs 合并后统计量的Δ
五、收尾第一行：【F3总执行】= X Pass / Y Fail / Z 样本不足
───────────────────────────────────────────────────────────────────
```

---

# ════════════════════════════════════════════════════════════════
# 📦 发包卡片 F4 / 4 —— B03/B04/B05（3个外汇品种avg<0+dd>25% Fail）→ 加ALB小过滤器6包
# ════════════════════════════════════════════════════════════════

> 用法：开第 12 个 Cursor 窗口
> 触发来源：B主题粘贴.md L25-L30 —— B03 USDJPY、B04 AUDUSD、B05 USDCAD 都是：中档k值、avg<0、dd>25%、Calmar-0.01~-0.10 Fail；B22 EURUSD ALB M1/H1 wr=30-33%但过滤逻辑有参考价值→诚实把ALB的EMA收盘形态过滤器加到B03/B04/B05里做6包消融

```
───────────────────────────────────────────────────────────────────
【Cursor 窗口 12 / 12】F4：B03/B04/B05 3品种avg负dd超25 → 加ALB EMA形态过滤器6包
───────────────────────────────────────────────────────────────────
【身份】F4执行员：F4a-F4f共6包，全部子包级4层
【★触发来源诚实钉死】B主题L25-L30红线：avg<0且dd>25%（B03-05三个USD系品种全中）；B22-B24的ALB过滤器虽然wr不够52%，但EMA收盘前视修正（B22无前视偏差Pass）是有效的→本卡=把B22里验证过的「EMA收盘确认」「回撤棒定义代理」的过滤逻辑加进B03/B04/B05，帮它们过滤掉假入场/反方向入场

【★本地资料路径索引】
  ▶ ALBrooks M1/M2/M3的EMA收盘/回撤棒/二次测试代理定义 = D:\Stock\trading_assistant\11_frozen_summaries_archive\_raw_snapshot_batch09\03_迭代后核心母版V1.2\01_核心技术体系模块\[模块]ALBrooks_入场信号与交易执行.md
  ▶ B03/B04/B05原验收=themeB L25-L30

一、F1-F6锁定同上；P0参数/中档k值/R011硬切换逻辑不变，只在入场前加1件ALB过滤器（不并行、不改止损网格、不改k值）
二、6包清单（3品种×2过滤器）：
    F4a USDJPY + ALB M1 EMA收盘过滤器（B22验证过的「EMA方向与K线收盘同向」才入场；原avg-0.00/dd25.89）
    F4b USDJPY + ALB M2 EMA回撤棒过滤器（只在「EMA回撤后的第二根K线」入场；对应B23 M2回撤棒定义）
    F4c AUDUSD + ALB M1 EMA收盘过滤器（原avg-0.06/dd25.69）
    F4d AUDUSD + ALB M2 EMA回撤棒过滤器
    F4e USDCAD + ALB M1 EMA收盘过滤器（原avg-0.05/dd26.32）
    F4f USDCAD + ALB M2 EMA回撤棒过滤器
三、验收成功门（每包单独判）：
    相对原B03/B04/B05：avg从<0 → >+0.05%；dd从>25% → ≤24%；Calmar从负 → ≥+0.10
    任意1件达标=边缘Pass；3件全达标=正式子包Pass
四、红线里必须写：相对原品种的Δavg pp Δdd pp ΔCalmar
五、收尾第一行：【F4总执行】= X Pass / Y Fail / Z 样本不足
───────────────────────────────────────────────────────────────────
```

---

## ════════════════════════════════════════════════════════════════
## 诚实总览：E+F两个增量主题 共 16+19=35 包（12个Cursor窗口，挑最有希望的先开顺序诚实钉死）
## ════════════════════════════════════════════════════════════════

**开跑顺序（从概率最大→最小，诚实不浪费额度）：**
1. 🔝 第一优先（2包，最容易出S/A）：
   - E1 C20 FB-1 0.618 B+ 补7层7窗（原唯一交易口径Pass；②抽样1.66pp；整批最接近S/A的包）
   - F1 B08 GER40关EMA144 Calmar0.64 补7层7窗（整批B最接近能用的对照侧；关EMA组dd19.93<25%）
2. 🥈 第二优先（12包）：
   - E3 谐波6包（C01/C06布朗桥陷阱修复；wr=100%直接Fail诚实卡死不会浪费回测时间很快）
   - F2 大隐KD6包（换周期改参数让5/95打得到；原n=0~4只要打得到就有样本量很快出）
3. 🥉 第三优先（11包，最慢的标签去重）：
   - E2 周期女王6包（E2f放宽Jaccard到30%；要做标签交集计算较慢）
   - E4 ALB C10 3包过滤器；F4 B03-05 6包过滤器（都是加小过滤器快）
   - F3 TK标签6包合并去重（原n=58593→要合并运算最慢，放最后跑）

**4窗口并发SOP同前：开4独立Cursor→复制对应卡→夜跑→总执行贴回粘贴.md**
**诚实总风险同前6条+2条额外：**
7. ⚠️ F1关EMA144过了也**不能改判原B08的Fail**（原B08是开EMA144，本次是对照侧单独实验，诚实不做改判）
8. ⚠️ F3合并后仍>原1/10笔数→直接诚实Fail，不拿合并前的过密统计量做任何Pass判定

⚠️ 诚实警告：实盘滑点/流动性/执行偏差 avg下降2-5pp，dd上升5-15pp
