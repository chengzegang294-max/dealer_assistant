# Trae System Transition Execution Card

## 生成入口

- `GENERATOR`: `working_transition_copy_20260709`
- `SOURCE`: `21_trae_system_archive/SKILLS_INDEX.md + SKILLS_GROUP_VIEW.md`

## 当前作用

- 为 `21_trae_system_archive` 中仍承担 legacy router 角色的索引文件建立 repo-global 工作副本。
- 保持旧路径不动，同时在 `00_entry/trae_system_transition/` 下记录过渡期的 family index 与 promotion map。
- 当前属于治理过渡层，不替代 `.trae` 的 active router，也不替代 `21_trae_system_archive` 的 legacy archive router。
