# A5 信息直播间阶段A收尾_其他房增量抽检执行页

更新时间：2026-08-06

## 一、这页用途

- 孙悟空金牌当前页 + 增量已通过后，
  阶段 A 收尾只做一件事：
  - 对其余模板房做**增量抽检**（不是全量重做、不是扩新源）

## 二、当前主线

- 阶段 A 收尾：其他模板房增量抽检
- 当前不是：
  - 功能需求大会（阶段 B）
  - 买新源 / 指标组合

## 三、抽检范围

| 房间 | 现有增量证据（batch_05） | 现有 stop_reason | 抽检动作 |
|------|-------------------------|------------------|----------|
| 孙悟空金牌 | `182013.json`（v1.1） | `scroll_end` | **免抽**（本轮已收口） |
| 格兰投研 | `183901.json` 等（已吸收） | 链：`manual_limit`→`scroll_end`→**`not_scrollable`（到尽，1050条）** | **抽检#1完成**；房间名旧证误 `k神`，已修 v1.4/v1.2 |
| 周期女王 | `153634.json` / `155438.json`（v1.2，已归入女王目录） | **`scroll_end`（251）** | **抽检#2完成**；主负责人确认本房。JSON 内曾误写 `浮光`/`k神`，以确认与正式目录为准 |
| 天机 | `163547.json`（v1.2）+ 旧 `174742` | **`scroll_end`（486）** | **抽检#3完成**；侧栏=天机短线试更新；脚本误锚`先知`；广告覆盖仍在 |
| 龙头交易猿 | `20260805_065556.json`（v1） | `scroll_end`（117 条，房间名正确） | **下一手：轻量复核** |

## 四、统一抽检口令

每房固定顺序：

1. 登录态进入该房历史记录（建议倒序）
2. 先跑当前页：
   - `live_info_current_page_export_v1.js`（v1.3）
3. 再设增量变量并跑：
   - `live_info_incremental_export_v1.js`（v1.1）

### 4.1 格兰投研（续跑到顶）

若本地仍有该房 checkpoint，可直接续跑；否则先小跑再建续跑：

```js
// 若确定要清旧断点重来：
window.__infoLiveIncrementalExportV1Options = {
  reset_checkpoint: true,
  scroll_direction: "up",
  start_position: "keep",
  max_rounds: 40
};
// 跑增量脚本后，若 stop_reason=manual_limit，再续：
window.__infoLiveIncrementalExportV1Options = {
  reset_checkpoint: false,
  scroll_direction: "up",
  max_rounds: 200
};
```

### 4.2 周期女王（已完成）

- 旧 `scroll_stuck`（`054021`）已由 `153634`/`155438` 的 `scroll_end`/251 纠过
- 主负责人确认：即使 JSON 写 `浮光`/`k神`，目标房仍是周期女王
- 正式证据：`batch_05/.../priority_rooms/周期女王/`

### 4.3 天机 / 龙头交易猿（轻量复核）

```js
window.__infoLiveIncrementalExportV1Options = {
  reset_checkpoint: true,
  scroll_direction: "up",
  start_position: "keep",
  max_rounds: 40
};
```

- 若很快 `scroll_end` 且房间名正确 → 记通过
- 天机若 `room_anchor` 仍为 `天机短线试更新`：可接受为同房别名，状态表备注即可，不阻断

## 五、抽检通过门槛

单房记「增量抽检通过」需同时满足：

1. 视觉真实滚动
2. `stop_reason = scroll_end`（或明确无更多记录）
3. `room_anchor` 可接受（正确名、已登记别名，或**主负责人当场确认房间身份**）
4. JSON 落入：
   - `02_runtime/info_live_room_sampling/batch_05__.../00_raw/priority_rooms/<房间名>/`

## 六、阶段 A 收尾完成定义

以下全部成立才算阶段 A 增量抽检收尾完成：

1. 孙悟空：已通过（已满足）
2. 格兰：由 `manual_limit` 推进到 `scroll_end`（已满足）
3. 周期女王：由 `scroll_stuck` 推进到 `scroll_end`（已满足，主负责人确认）
4. 天机、龙头：至少各有一份可接受的 `scroll_end` 抽检记录（天机已满足 `163547`；龙头待轻量复核）

完成后才允许进入阶段 B（功能需求清单）。

## 七、建议执行顺序（唯一）

1. ~~**格兰投研** 续跑到顶~~  
2. ~~**周期女王** 重跑纠 stuck~~  
3. ~~**天机** 轻量复核~~（`163547`=`scroll_end`/486）  
4. **龙头交易猿** 轻量复核  

每做完一房：把新 JSON 路径丢给 Cursor，立刻回填状态表。

## 八、一句话口径

- 天机抽检#3已通过；阶段 A 增量抽检只剩**龙头交易猿**。