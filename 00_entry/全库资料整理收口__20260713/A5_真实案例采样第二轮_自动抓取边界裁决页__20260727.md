# A5 真实案例采样第二轮 自动抓取边界裁决页

更新时间：2026-07-27

## 一、这页用途

- 本页只做：
  - 正式裁决第二轮新日期样本是否能仅靠当前仓内线索自动抓取完成
  - 防止后续继续围绕同一组外部页面/API 空转

## 二、当前自动抓取目标

- 当前自动抓取目标是：
  - 不依赖用户先截图
  - 直接沿 `batch149` 已吸收的页面/API 家族
    取到一个新交易日样本
  - 以便启动：
    `第二轮日期锚点确认 -> 三张最小代表卡`

## 三、当前已验证的自动抓取线

### 3.1 longhuvip API 线

- 已验证入口来自：
  - `00_raw_snapshot/pages/市场情绪.html`

- 当前已试的历史/实时接口包括：
  - `RealRankingInfo`
  - `HisHomeDingPan / DiskReview`
  - `HisZhangFuDetail`
  - `ZhangTingExpression`
  - `MarketCapacity`
  - `MarketCapacityKLine`

- 当前覆盖的日期探针包括：
  - `2026-07-21`
  - `2026-07-22`
  - `2026-07-23`
  - `2026-07-24`
  - `2026-07-27`

- 当前结果统一为：
  - 接口可达
  - 返回 `200`
  - 但正文统一只回：
    - `{"list":[],"List":[],"errcode":"0"}`

### 3.2 page.tdx 页面线

- 已验证入口来自：
  - `00_raw_snapshot/pages/热点提醒.html`
  - `00_raw_snapshot/pages/通达信F11.html`

- 当前已试的页面入口包括：
  - `pc_tcld_rdbk.html`
  - `tk_ygzl_ts.html`
  - `tk_ygzl_lhb.html`

- 当前结果为：
  - 页面地址可定位
  - 但当前环境无法直接抓到可用正文内容

## 四、当前自动抓取结论

- 当前正式结论为：
  1. 自动抓取链并非“完全不可达”
  2. 但在当前环境下，
     它不足以产出第二轮所需的有效新日期样本
  3. 因此第二轮当前不能继续假装“我自己就能直接抓完”

## 五、主负责人裁决

- 当前正式裁决为：
  1. 第二轮新日期样本的自动抓取，
     已触到新的证据/权限边界
  2. 当前这一步应从：
     `自动抓取`
     正式切到：
     `最小人工补证`
  3. 当前不再继续围绕同一组接口与页面做重复试探

## 六、当前下一手

- 当前下一手冻结为：
  - `最小人工补证清单`

- 只要补齐最小清单，
  就可以立即继续：
  - 新日期锚点确认页
  - 第二轮三张最小代表卡

## 七、一句话口径

- 第二轮新日期样本这一步，
  当前已经正式从“自动抓取尝试”
  切到
  `最小人工补证`
  ；
  不是接口不存在，
  而是当前环境拿不到有效载荷。
