# A5 FV gate v0 通过条件与补证计划页

更新时间：2026-07-23

## 一、这页用途

- 本页只做：
  - 在 `A5_FV_gate_v0新阶段最小入口页__20260723.md`
    已冻结后，
    把当前 `FV gate`
    的通过条件边界与补证顺序正式写死
  - 明确：
    - 现在先补什么
    - 现在不补什么
    - 什么情况下才允许进入下一类动作
- 本页不做：
  - 新 runtime
  - 第四窗
  - 新 evidence type
  - 多市场扩展
  - 状态名升格

## 二、主负责人裁决

- 当前主线继续保持为：
  - `FV gate`
- 当前已完成的最小入口冻结是：
  - `contract-first`
- 当前因此不再缺：
  - 第一刀入口页
  - 最小输入字段
  - 最小成绩单字段
  - 最小失败回退规则
- 当前真正缺的是：
  - 把“后面如何补到哪停”写成正式计划
- 因此当前第二刀正式选：
  - `plan-first`

## 三、为什么当前选 plan-first

- 原因 1：
  - `contract-first`
    已经补完，
    再停在入口页会变成半收口
- 原因 2：
  - 当前仓内已经有：
    - `v0 current_best`
    - `v1 sample_boundary`
    - `v2 third_window`
    - `active_underperformance`
    这些既有事实
  - 但新阶段还没有把它们重新压成：
    - “哪些继续算当前证据包”
    - “哪些只是历史结果，不继续默认外扩”
- 原因 3：
  - 若现在直接跳 `run-first`，
    很容易重新滑回：
    - 看到缺口就再跑一轮
    - 结果先出来，合同和停止规则后补
- 原因 4：
  - 当前最需要的不是新增结果，
    而是防止：
    - `FV gate`
      再次失去边界

## 四、当前不选什么

### 1. 当前不选 `run-first`

- 当前不选原因是：
  - 第三窗、`active_underperformance`
    与 `current_best`
    都已经是既有运行事实
  - 当前新阶段还没必要默认再开新运行物

### 2. 当前不选 `new-evidence-type-first`

- 当前不选原因是：
  - 新证据类型会立刻触发新的判断边界
  - 当前仓内并没有完成“旧证据包重排 + 停止规则重写”

### 3. 当前不选 `第四窗 / 多市场 / 本地 AI`

- 当前不选原因是：
  - 这些都不属于当前最窄主线
  - 一开就会把本轮 `FV gate`
    从单点补证变成新系统扩张

## 五、当前有效证据包怎么认

- 当前新阶段继续承认的既有证据包只包括：
  - `A5_FV_gate_v0_当前最佳最小口径冻结页__20260719.md`
  - `A5_FV_gate_v1_sample_boundary阶段页__20260719.md`
  - `A5_FV_gate_v2_third_window阶段页__20260719.md`
  - `A5_FV_gate_active_underperformance_v0解释层页__20260719.md`
  - `A5_FV_gate_active_underperformance挂起说明页__20260723.md`
- 当前继续保留的总标签是：
  - `still_need_evidence`
- 当前不允许把上述任何一页偷写成：
  - `financial-valid`
  - `output_passed`
  - `ready_to_deploy`

## 六、当前通过条件边界

- 当前“通过条件”只允许分成两层：

### 1. 可写成“已完成”的层

- 当前允许写成已完成的是：
  - 最小入口已冻结
  - 最小输入字段已冻结
  - 最小成绩单字段已冻结
  - 最小失败回退规则已冻结
  - 既有证据包已形成：
    - `v0`
    - `v1`
    - `v2`
    - `active_underperformance`

### 2. 仍必须保留 NEED_EVIDENCE 的层

- 当前仍必须保留的是：
  - `financial-valid`
  - `output_passed`
  - `strict_out_of_time_generalization`
  - `full_impact_model_ready`
  - `ready_to_deploy`

## 七、当前补证顺序冻结

- 当前补证顺序固定为：

### 1. 先补“计划层”

- 当前先做：
  - 把补证对象、补证顺序、停止规则、禁止项冻结清楚
- 当前目的：
  - 防止下一手重新变成开放式试跑

### 2. 再判断是否需要“单一新运行物”

- 只有当计划层已经冻结后，
  才允许判断：
  - 是否需要一个新的单一运行物
- 当前该新运行物若未来被允许，
  也只能是：
  - 单点补证对象
- 当前不预设：
  - 一定要马上跑

### 3. 最后才允许讨论状态是否有资格改写

- 当前只有在：
  - 新运行事实已出现
  - 且能明确压住禁止宣称检查
  后，
  才允许讨论是否改写更高层状态
- 当前这一步明确不进入

## 八、当前唯一允许的后续候选

- 当前计划冻结后，
  后续候选只允许保留两类：

### A. 单点运行物

- 例如：
  - 针对既有张力点的单一复跑或单一对照物
- 但必须满足：
  - 不扩字段家族
  - 不开第四窗
  - 不开新 evidence type
  - 不改市场范围

### B. 单点解释层

- 例如：
  - 对既有 `active_underperformance`
    的更窄解释补页
- 但必须满足：
  - 不伪装成新运行事实
  - 不替代真正补证

## 九、当前明确禁止项

- 禁止 1：
  - 把 `plan-first`
    继续扩成新的长篇讨论包
- 禁止 2：
  - 把第三窗历史结果当成“当前默认还要继续跑第四窗”的理由
- 禁止 3：
  - 把 `active_underperformance`
    解释层直接偷换成通过判断
- 禁止 4：
  - 顺手重开：
    - `Batch2`
    - `batch149`
    - `git`
    - 多市场
    - 本地 AI 技术栈

## 十、当前下一手

- 当前这页当前只承担：
  - `plan-first`
    的通过条件边界、补证顺序与停止规则冻结
- 当前后续已不再停留在“下一手待定”，而是已经连续推进到：
  - `A5_FV_gate_v0下一手对象裁决页__20260723.md`
  - `A5_FV_gate_v0相对基准张力窄化解释页__20260723.md`
  - `A5_FV_gate_v0单点运行物暂不重开判断页__20260723.md`
  - `A5_FV_gate_v0当前阶段停点冻结页__20260723.md`
- 当前阶段最终结论已冻为：
  - 保持 `still_need_evidence`
  - 暂不重开单点运行物
- 本页自身到此即停

## 十一、一句话口径

- 当前最准确的一句话是：
  - `FV gate`
    已完成 `contract-first` 入口冻结，
    当前第二刀正式切到
    `plan-first`；
    只冻结通过条件边界、补证顺序与停止规则，
    不开第四窗、不重开新 evidence type、不升格状态名。

## 回链

- `A5_FV_gate_v0新阶段最小入口页__20260723.md`
- `A5_FV_gate_active_underperformance挂起说明页__20260723.md`
- `A5_FV_gate_v0下一手对象裁决页__20260723.md`
- `A5_FV_gate_v0_当前最佳最小口径冻结页__20260719.md`
- `A5_FV_gate_v1_sample_boundary阶段页__20260719.md`
- `A5_FV_gate_v2_third_window阶段页__20260719.md`
