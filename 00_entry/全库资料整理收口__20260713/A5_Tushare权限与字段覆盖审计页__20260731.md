# A5 Tushare权限与字段覆盖审计页

更新时间：2026-07-31

## 一、这页用途

- 本页只做：
  1. 把 `Tushare`
     重新顶回当前信息源主线第一手
  2. 审计 `Tushare`
     当前到底具备什么权限与字段覆盖
  3. 判断它是：
     - `正式结构化数据源候选`
     - 还是仅能作为
       `当前可用 proxy`

## 二、这页属于哪条线

- 文件类型：`REPO_GLOBAL`
- 推荐落点：`00_entry\全库资料整理收口__20260713\`
- 原因：
  - 这是当前信息源主线的正式审计页，
    不是临时笔记
- 是否允许放根目录：`no`
- 是否需要后续迁移：`no`

## 三、先纠偏：信息源主线重新回到最前面

- 当前必须明确：
  - 这页是当前默认第一手
  - 围绕 `Pi` 的审计与角色合同，
    继续保留为辅助支线
  - 不再覆盖信息源默认顺序

- 当前双层顺序重新冻结为：
  1. `信息源主线第一手`
     - `Tushare 权限与字段覆盖审计`
  2. `信息源主线第二手`
     - `quicktiny / 连板天梯 历史导出能力核查`
  3. `辅助支线`
     - `Pi 接入方式与长期可用性审计`
     - `Pi 角色最小合同`

## 四、为什么当前先审 Tushare

- 第一轮信息源多 AI 已正式收口为：
  1. `Tushare = 正式结构化数据源候选`
  2. `东方财富 = 当前样本锚点源`
  3. `连板天梯 / quicktiny = 高度结构增强源`
  4. 当前最值得优先补证的唯一缺口是：
     - `Tushare 当前具体权限层级与关键字段覆盖`

- 所以现在最值钱的一步不是继续讨论抽象优先级，
  而是把：
  - `Tushare 到底能拿什么`
  - `还缺什么`
  - `需不需要最小付费`
  压成正式审计结论

## 五、当前仓内已知证据

### 5.1 当前来源盘点证据

- `batch_151_info_source_inventory__20260730`
  已冻结：
  - `Tushare = 正式源候选`
  - 当前最小下一步：
    - `字段列表或最小导出样本`
    - `做来源审计与字段目录`

### 5.2 当前历史裁决证据

- `batch_140_tushare_tdx_data_source_absorb__20260712`
  已冻结一条很重要的旧裁决：
  - `Tushare` 不适合大而全预付
  - 但若目标是尽快打通当前关键链路，
    值得先补最小层级

- 旧裁决里的分层组合是：
  - `Tushare`
    解决当前必需缺口
  - `通达信`
    后续承担 `OHLCV / 分钟线` 底表候选
  - `AKShare / 雪球`
    做补源候选

### 5.3 当前字段级实证证据

- 在历史 `MFLOW` 审计线上，
  仓内已经出现过 `Tushare moneyflow + daily`
  的实证结论：
  1. 已有真实抓取脚手架
  2. 已有批量样本 CSV
  3. 已有宽表 join 结果
  4. 已有 `northbound / regime / industry`
     辅助源成功 join 的记录

- 但同时也已经明确保留了硬缺口：
  1. 并未证明
     `Tushare`
     已完整覆盖全部冻结字段
  2. `mflow_sellord_ratio`
     仍是硬缺口
  3. `mflow_open_intent`
     仍是硬缺口
  4. 当前还不能直接把它包装成
     `Wind / iFinD`
     等价替身

## 六、当前要审的不是“能不能用”，而是“够不够格”

- 当前审计问题分成 4 组：

### 6.1 权限层级

- 当前必须回答：
  1. 是否已有 `TUSHARE_TOKEN`
  2. 当前实际积分/权限层级到哪里
  3. 免费层是否足够做最小验证
  4. 若不足，
     最小补点应补到哪一档

### 6.2 核心字段覆盖

- 当前至少要确认以下字段族：
  1. `daily OHLCV`
  2. `trade_cal`
  3. `moneyflow`
  4. `northbound`
  5. `industry / concept 映射`
  6. `top_list / 龙虎榜`

- 当前真正想确认的是：
  - 哪些字段族已经够当前主线用
  - 哪些还只能算增强
  - 哪些仍需外部补源

### 6.3 稳定性与可复用性

- 当前必须回答：
  1. 是否能稳定导出或抓取
  2. 是否能持续复用到标准化输入包
  3. 是否只在少量样本上成立，
     还是能站住更长期使用

### 6.4 canonical provider 资格

- 当前最关键判断不是
  `能抓到一点数据`
  ，而是：
  - `Tushare`
    能不能成为当前信息源主线里的
    `canonical provider`
  - 还是只能先认定为
    `当前最强可用 proxy`

## 七、当前最小字段族判断

### 7.1 已较强站住的字段族

- 当前仓内证据显示，
  以下字段族相对更有希望先站住：
  1. `moneyflow`
  2. `daily`
  3. `northbound`
  4. `industry`

- 当前判断：
  - `可作为优先审计入口`

### 7.2 可以作为补充但不宜误判成主线的字段族

- 当前更适合当补充证据的有：
  1. `top_list / 龙虎榜`

- 当前判断：
  - 它适合做事件级补充
  - 不适合直接冒充季度或长期结构化主轴

### 7.3 当前仍保留硬缺口的字段族

- 当前至少保留以下硬缺口：
  1. `mflow_sellord_ratio`
  2. `mflow_open_intent`
  3. 更高质量、
     更完整、
     更高频的机构行为字段

- 当前判断：
  - 这部分还不能只靠
    `Tushare`
    一口气宣布“全解决”

## 八、其他 AI / 历史裁决里提过的补充信息源

- 对这个问题，
  当前可以明确回答：
  - `有补充候选`
  - 但没有任何一个在当前阶段压过
    `Tushare`
    的主位

### 8.1 高质量理想参考层

1. `Wind / iFinD`
   - 反复被提为：
     - 高质量
     - 字段更完整
     - 更接近理想 canonical provider
   - 当前判断：
     - `理想参考`
     - `高成本`
     - `当前不作第一刀`

2. `同花顺 F10`
   - 在机构持仓这类季度字段里，
     被提为贴近主源的人工/半人工入口
   - 当前判断：
     - `适合一次性样本探查`
     - `不适合当前直接升为主线`

### 8.2 低成本 fallback / probe 层

1. `AkShare`
   - 被反复提为：
     - 免费替代路线
     - 低成本 probe
     - fallback 候选
   - 当前判断：
     - `值得保留`
     - `但字段稳定性与语义仍需对照`

2. `通达信`
   - 被提为：
     - `OHLCV / 分钟线`
       底表候选
   - 当前判断：
     - `适合行情底座`
     - `不直接替代 Tushare 当前 moneyflow 主问题`

3. `QMT`
   - 在旧讨论里被作为分钟级与稳定协议对比候选提过
   - 当前判断：
     - `future_only`
     - 当前不进入第一优先级

4. `雪球`
   - 被提为补源候选
   - 当前判断：
     - `补充线`
     - `不作当前主线`

### 8.3 当前主负责人对“是否还要补别的信息源”的裁决

- 当前正式裁决为：
  1. `有候选`
     ，但都不改当前主排序
  2. 当前真正该补的
     不是“再开一堆新源”
     ，而是先把
     `Tushare`
     的权限和字段站实
  3. 只有当
     `Tushare`
     在某些关键字段族上确认不够格，
     才把：
     - `AkShare`
     - `通达信`
     - `Wind / iFinD`
     - `同花顺 F10`
     按字段族拆出来补位

## 九、当前主负责人正式审计结论

- 当前正式审计结论为：

1. `Tushare`
   继续保持：
   - `正式结构化数据源候选`
   - `当前信息源主线第一手`

2. 当前不能直接把
   `Tushare`
   写成：
   - `已完成 canonical provider 定稿`
   因为关键字段覆盖仍有 `NEED_EVIDENCE`

3. 当前可先把
   `Tushare`
   认定为：
   - `当前最强可审计候选`
   - `当前最值得优先补证的结构化数据源`

4. 当前对付费层级的口径继续沿用旧裁决：
   - `不建议大而全预付`
   - `若免费层不够，就只补最小必要层级`

5. 当前其它补充信息源，
   统一降格为：
   - `候选补位层`
   - `参考层`
   - `future_only`

## 十、当前最小下一步

- 当前最小下一步冻结为：
  1. 先补
     `Tushare 当前实际权限层级`
  2. 再补
     `Tushare 当前已可用字段列表 / 最小导出样本`
  3. 再把字段族分成：
     - `已够当前主线`
     - `可增强`
     - `仍需补源`

## 十一、一句话口径

- 当前信息源主线已经重新顶回最前面：先审 `Tushare` 到底具备什么权限、能稳定覆盖哪些关键字段，再决定它能不能正式站成当前结构化主源候选；其他 AI 确实提过 `Wind / iFinD / 同花顺 F10 / AkShare / 通达信 / QMT / 雪球` 这些补充源，但现在都还没有压过 `Tushare` 的优先级。
