# A5 首批真实案例采样三卡汇总页

更新时间：2026-07-27

## 一、这页用途

- 本页只做：
  - 把首批真实案例采样的前三张正式记录卡收成一个可回看的汇总页
  - 明确：
    - 当前三张卡分别覆盖什么联动类型
    - 首批三卡合起来解决了什么
    - 还保留哪些 `still_need_evidence`

## 二、当前首批三张卡

- 当前已正式落盘：
  1. `A5_REAL_CASE_001`
     - `HYDB行业对比 / 沪深涨跌停 -> 上榜资金`
  2. `A5_REAL_CASE_002`
     - `ZSDB指数对比 / 沪深涨跌停 -> 打板资金`
  3. `A5_REAL_CASE_003`
     - `HYDB行业对比 / ZSDB指数对比 -> 打板资金`

## 三、当前三张卡共同冻结的东西

- 当前三张卡共同冻结：
  1. `sample_date = 2026-07-20`
  2. `source_anchor` 已有统一写法
  3. 当前全部仍保持：
     - `not_financial_truth: yes`
     - `not_trade_ready: yes`
  4. 当前精确 `occurredAt`
     仍统一保留为：
     - `still_need_evidence`

## 四、当前三张卡分别覆盖什么

| sample_id | 背景对象 | 触发对象 | 当前联动类型 | 当前优先动作 |
| --- | --- | --- | --- | --- |
| `A5_REAL_CASE_001` | `HYDB行业对比` + `沪深涨跌停` | `上榜资金` | `背景先坏_触发后退` | `先降级职责` |
| `A5_REAL_CASE_002` | `ZSDB指数对比` + `沪深涨跌停` | `打板资金` | `指数转弱_宽度走坏_触发先降权` | `先降权` |
| `A5_REAL_CASE_003` | `HYDB行业对比` + `ZSDB指数对比` | `打板资金` | `双背景背离_触发退回观察` | `先降权 / 必要时回观察位` |

## 五、当前这三张卡合起来解决了什么

- 当前首批三卡合起来已经解决：
  1. 五条代表指标里的关键背景/触发对象，
     已经不再只是职责细化页，
     而是进入了案例级写法
  2. 当前已经有三种不同的联动主路径：
     - `背景先坏_触发后退`
     - `背景转弱_触发先降权`
     - `双背景背离_触发退回观察`
  3. 当前已经能看出统一规律：
     - 背景型对象负责解释“原解释还值不值得继续背书”
     - 触发型对象负责解释“当前触发还值不值得继续被信”

## 六、当前仍保留的边界

- 当前仍保留：
  1. 三张卡的精确 `occurredAt`
     尚未补齐
  2. 三张卡之间的更细事件时序，
     还未细到分钟或秒级
  3. 当前三张卡仍是
     `案例级解释样本`
     ，
     不是金融有效性结论

## 七、主负责人裁决

- 当前正式裁决为：
  1. 首批真实案例采样已经不是“只有首张卡”
     ，
     而是正式形成
     `三卡包`
  2. 当前最值钱的一步，
     已从“继续补单卡字段”
     升级为：
     `用三卡包做首批阶段性收口`
  3. 当前下一手应接：
     - 首批真实案例采样阶段性收口页
     - 或精确 `occurredAt` 的单点补齐

## 八、一句话口径

- 当前首批真实案例采样已经正式形成三张卡的最小样本包：
  三条联动路径都已落盘，
  当前缺的已不再是“有没有案例卡”，
  而是“要不要继续补精确时刻”。
