# A5 第二批真实案例采样三卡汇总页

更新时间：2026-07-27

## 一、这页用途

- 本页只做：
  - 把第二批真实案例采样的三张正式记录卡收成一个可回看的汇总页
  - 明确第二批三卡相对首批三卡补了什么

## 二、当前第二批三张卡

- 当前已正式落盘：
  1. `A5_REAL_CASE_004`
     - `HYDB行业对比 / ZSDB指数对比 -> 上榜资金`
  2. `A5_REAL_CASE_005`
     - `ZSDB指数对比 / 沪深涨跌停 -> 上榜资金`
  3. `A5_REAL_CASE_006`
     - `HYDB行业对比 / 沪深涨跌停 -> 打板资金`

## 三、第二批相对首批补了什么

- 当前第二批三卡主要补齐：
  1. `上榜资金`
     在不同双背景组合下的两种正式案例写法
  2. `打板资金`
     在
     `HYDB行业对比 / 沪深涨跌停`
     这一组背景下的正式案例写法
  3. 让两类触发对象都不再只挂在单一背景组合上

## 四、第二批三张卡分别覆盖什么

| sample_id | 背景对象 | 触发对象 | 当前联动类型 | 当前优先动作 |
| --- | --- | --- | --- | --- |
| `A5_REAL_CASE_004` | `HYDB行业对比` + `ZSDB指数对比` | `上榜资金` | `双背景走弱_榜单先退一层职责` | `先降级职责` |
| `A5_REAL_CASE_005` | `ZSDB指数对比` + `沪深涨跌停` | `上榜资金` | `指数转弱_宽度退潮_榜单降级再降权` | `先降级职责 / 再降权` |
| `A5_REAL_CASE_006` | `HYDB行业对比` + `沪深涨跌停` | `打板资金` | `行业背离_宽度退潮_触发退回观察` | `先降权 / 必要时回观察位` |

## 五、当前第二批三卡合起来解决了什么

- 当前第二批三卡合起来已经解决：
  1. `上榜资金`
     不再只有一张样本卡
  2. `打板资金`
     与两类背景组合之间的案例级写法更完整
  3. 六张卡合起来，
     已经能覆盖两类触发对象在主要双背景组合下的首轮案例表达

## 六、当前仍保留的边界

- 当前仍保留：
  1. 三张卡的精确 `occurredAt`
  2. 六张卡的更细分钟级事件时序
  3. 当前仍然不是金融有效性结论

## 七、主负责人裁决

- 当前正式裁决为：
  1. 第二批真实案例采样已经正式形成
     `三卡包`
  2. 当前主线最值钱的一步，
     已从“补第二张第三张卡”
     继续升级为：
     `把六张卡收成第一轮案例矩阵`

## 八、一句话口径

- 当前第二批真实案例采样已经补齐了首批三卡没有覆盖到的关键交叉组合，
  现在主线已经具备从 `三卡包` 升到 `六卡矩阵` 的条件。
