# 方案A 第二轮多AI回收记录与主负责人裁决

更新时间：2026-07-15

## 临时材料吸收记录

- 临时路径：
  - `D:\Stock\trading_assistant\暂时存放\粘贴区.md`
- 材料类型：
  - 第二轮多AI回包临时粘贴汇总
- 是否值得吸收：
  - `yes`
- 正式去向：
  - `00_entry/全库资料整理收口__20260713/方案A_第二轮多AI回收记录与主负责人裁决__20260715.md`
- 是否允许继续留在暂时存放：
  - `no`
- 删除条件：
  - 本页与投票包、README 回链完成后，可删除原临时文件

## TASK

- 吸收第二轮多AI回收结果，并把不同模型的口径归一化为：
  - 是否同意“已达到可转入定向补阈值”
  - 当前最推荐的工作模式
  - 最应优先补的单点缺口
  - 主负责人最终裁决

## 当前结论先行

- 第二轮多AI回收的真正共识不是：
  - “资料已经全齐”
- 而是：
  - “已经达到可转入定向补阈值”
  - 当前应停止大范围泛补
  - 当前应进入“工作模式投票 + 单点缺口补采”

## 一、各模型归一化回收

### 1) GPT

- 阈值判断：
  - 明确同意已达到 `可转入定向补阈值`
- 工作模式归一化：
  - 主模式：`按 high/medium req 做定向补`
  - 配套动作：`先落补采优先级表 + 停止规则`
- 单点缺口排序：
  - `A_REQ_002`
  - `A_REQ_003`
  - `A_REQ_001`
- 关键价值：
  - 把“资料问题”明确收敛为“字段化 + 验收化 + 数据底座闭合”问题

### 2) Kimi

- 阈值判断：
  - 明确同意已达到 `可转入定向补阈值`
- 工作模式归一化：
  - 主模式：`冻结新增泛补，专注现有资料收口与桥接`
  - 等价于：`定向补中的保守执行版`
- 单点缺口排序：
  - `A_REQ_001`
  - `A_REQ_005`
  - `A_REQ_007`
- 关键价值：
  - 把“研究合同 -> runtime 验收”的最后一跳明确抬成高优先缺口

### 3) GLM

- 阈值判断：
  - 明确同意已达到 `可转入定向补阈值`
- 工作模式归一化：
  - 主模式：`投票 + 单点缺口补采`
  - 与 `按 req 定向补` 高度一致
- 单点缺口排序：
  - `A_REQ_001`
  - `A_REQ_002`
  - `A_REQ_003`
- 关键价值：
  - 明确把“泛补 -> 泥潭、定向补 -> 主线稳定”这个逻辑钉死

### 4) DeepSeek

- 阈值判断：
  - 明确同意已达到 `可转入定向补阈值`
- 工作模式归一化：
  - 主模式：`定向单点缺口补采`
- 单点缺口排序：
  - `A_REQ_002`
  - `A_REQ_003`
  - `A_REQ_004`
- 关键价值：
  - 给出了最明确的停止规则模板：
    - 补完后一周内必须产出“字段 -> 验收例”映射草稿，否则立即中止

### 5) Qwen

- 阈值判断：
  - 同意已达到 `可转入定向补阈值`
- 工作模式归一化：
  - 主模式：`双轨80/20`
- 单点缺口排序：
  - `A_REQ_008`
  - `A_REQ_010`
  - `A_REQ_002`
- 关键价值：
  - 支持“少量副轨借鉴”的存在，但其缺口排序更偏学习资源扩张，和当前“强落地”主线一致性偏弱

## 二、归一化投票结果

### 1. 是否已达到可转入定向补阈值

- 同意：
  - `GPT / Kimi / GLM / DeepSeek / Qwen`
- 结果：
  - `5/5 同意`

### 2. 工作模式归一化投票

按“真实执行含义”归一化，而不按各模型原文中的自定义编号统计。

- `模式R1：按 req 定向补 + 单点缺口补采`
  - 支持：
    - `GPT / GLM / DeepSeek`
- `模式R2：冻结新增泛补，先桥接/收口`
  - 支持：
    - `Kimi`
- `模式R3：双轨80/20（主轨定向补，副轨少量借鉴）`
  - 支持：
    - `Qwen`

### 3. 单点缺口出现频次

- `A_REQ_001`
  - `3票`：GPT / Kimi / GLM
- `A_REQ_002`
  - `4票`：GPT / GLM / DeepSeek / Qwen
- `A_REQ_003`
  - `3票`：GPT / GLM / DeepSeek
- `A_REQ_004`
  - `1票`：DeepSeek
- `A_REQ_005`
  - `1票`：Kimi
- `A_REQ_007`
  - `1票`：Kimi
- `A_REQ_008`
  - `1票`：Qwen
- `A_REQ_010`
  - `1票`：Qwen

## 三、主负责人裁决

### 裁决 1：是否继续大范围找资料

- 不继续。
- 当前正式口径改为：
  - `停止大范围泛补`
  - `进入定向单点缺口补采`

### 裁决 2：当前采用哪种工作模式

- 采用：
  - `模式R1：按 req 定向补 + 单点缺口补采`
- 吸收：
  - `Kimi` 的保守约束
  - `Qwen` 的少量副轨借鉴提醒
- 但不把它们升格为主模式。

### 裁决 3：当前最优先的三个缺口

- 第一优先：
  - `A_REQ_002`
  - 原因：出现频次最高，且直接关系“基本面/估值能否落到字段与验收”
- 第二优先：
  - `A_REQ_003`
  - 原因：与组合构建/风险预算直接相关，是 A股主线里最需要结构化落地的一段
- 第三优先：
  - `A_REQ_001`
  - 原因：这是高优先底座缺口，若不闭合，后续分钟/盘口方向会反复返工

### 裁决 4：暂缓哪些缺口

- 暂缓到下一轮：
  - `A_REQ_004`
  - `A_REQ_005`
  - `A_REQ_007`
  - `A_REQ_008`
  - `A_REQ_010`
- 原因：
  - 当前不是没价值
  - 而是优先级低于 `002/003/001`
  - 现在若同时打开，会打散主线

## 四、执行口径

- 当前先做：
  - `补采优先级表`
  - `停止规则`
- 然后只按以下顺序开缺口族：
  - `A_REQ_002`
  - `A_REQ_003`
  - `A_REQ_001`
- 每次只开一个缺口族。
- 每补完一个缺口族，必须回填：
  - `方案A_需求矩阵_v1.tsv`
  - `方案A_资料资产清单_v1.tsv`
  - 对应裁决页或验收口径页
- 当前这两份正式执行文件已落盘：
  - `方案A_补采优先级表__20260715.md`
  - `方案A_停止规则__20260715.md`

## 五、一句话结论

- 第二轮多AI已经把事情说清楚了：
  - 现在不是继续“把资料攒齐”
  - 现在是：
    - `按 req 定向补`
    - `先补 A_REQ_002 / A_REQ_003 / A_REQ_001`
    - `并先写补采优先级表与停止规则`

## 回链

- `方案A_AI补充建议回收表_v1.tsv`
- `方案A_多AI回收汇总与投票包__20260715.md`
- `方案A_可转入定向补阈值说明__20260715.md`
- `方案A_第二轮多AI发包稿__投票与单点缺口__20260715.md`
- `方案A_补采优先级表__20260715.md`
- `方案A_停止规则__20260715.md`
