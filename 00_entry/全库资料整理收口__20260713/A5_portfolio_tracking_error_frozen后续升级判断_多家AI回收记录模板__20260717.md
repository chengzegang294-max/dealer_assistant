# A5 portfolio_tracking_error frozen 后续升级判断 多家AI回收记录模板

更新时间：2026-07-18

## 一、回收总表

| 模型 | 有效性 | 结论 | 一句话原因 | 唯一最小剩余缺口 / 唯一附加条件 | 禁止误写提醒 | yes 时唯一焦点 | 备注 |
|---|---|---|---|---|---|---|---|
| `GPT` | `invalid` | `无效` | `未答题，要求重发问题` | `无效` | `仍不能写成 output_passed` | `N/A` | `未形成任何状态票` |
| `DeepSeek` | `invalid` | `无效` | `泛化为主线排序，不按新题作答` | `无效` | `仍不能写成 output_passed` | `N/A` | `使用过时上游边界，且未按 1-5 合同输出` |
| `Kimi` | `invalid` | `无效` | `回答了 adjusted_position_weight` | `无效` | `仍不能写成 output_passed` | `N/A` | `对象串题` |
| `GLM` | `invalid` | `无效` | `串回 target_weight 旧题` | `无效` | `仍不能写成 output_passed` | `N/A` | `不是当前对象` |
| `Qwen` | `invalid` | `无效` | `未给 yes/no/conditional` | `无效` | `仍不能写成 output_passed` | `N/A` | `虽在谈本对象，但仍按旧的保守/平衡/激进框架作答` |

## 二、单模型吸收

### 1. `GPT`

- 有效性：
  - `invalid`
- 结论：
  - `无效`
- 一句话原因：
  - `未答题，要求重发具体问题`
- 唯一最小剩余缺口 / 唯一附加条件：
  - `无效`
- 禁止误写提醒：
  - `仍不能写成 output_passed`
- yes 时唯一焦点：
  - `N/A`
- 备注：
  - 没有给出 `yes / no / conditional`
  - 也没有按 `1-5` 合同输出

### 2. `DeepSeek`

- 有效性：
  - `invalid`
- 结论：
  - `无效`
- 一句话原因：
  - `泛化为主线排序，不按新题作答`
- 唯一最小剩余缺口 / 唯一附加条件：
  - `无效`
- 禁止误写提醒：
  - `仍不能写成 output_passed`
- yes 时唯一焦点：
  - `N/A`
- 备注：
  - 使用了过时边界
  - 仍在谈推进顺序与停止规则
  - 不是当前 `frozen` 后续升级判断的直接状态票

### 3. `Kimi`

- 有效性：
  - `invalid`
- 结论：
  - `无效`
- 一句话原因：
  - `回答了 adjusted_position_weight`
- 唯一最小剩余缺口 / 唯一附加条件：
  - `无效`
- 禁止误写提醒：
  - `仍不能写成 output_passed`
- yes 时唯一焦点：
  - `N/A`
- 备注：
  - 直接串到别的对象
  - 不纳入当前轮统计

### 4. `GLM`

- 有效性：
  - `invalid`
- 结论：
  - `无效`
- 一句话原因：
  - `串回 target_weight 旧题`
- 唯一最小剩余缺口 / 唯一附加条件：
  - `无效`
- 禁止误写提醒：
  - `仍不能写成 output_passed`
- yes 时唯一焦点：
  - `N/A`
- 备注：
  - 直接回答了别的已裁定旧题
  - 不纳入当前轮统计

### 5. `Qwen`

- 有效性：
  - `invalid`
- 结论：
  - `无效`
- 一句话原因：
  - `未给 yes/no/conditional`
- 唯一最小剩余缺口 / 唯一附加条件：
  - `无效`
- 禁止误写提醒：
  - `仍不能写成 output_passed`
- yes 时唯一焦点：
  - `N/A`
- 备注：
  - 虽在谈 `portfolio_tracking_error`
  - 但仍沿用保守 / 平衡 / 激进框架
  - 未按本轮 `1-5` 固定合同输出

## 三、主负责人裁决

- 当前有效票面：
  - `无`
- 当前正式裁决：
  - `继续维持 pass_conditions_frozen__not_output_passed`
- 当前为什么选这个：
  - 本轮没有形成一张符合合同的有效状态票
  - 当前能正式吸收的是：
    - `第二轮回包仍然失焦`
    - `票面质量进一步证明发包合同还要继续收紧`
- 当前为什么不选另外几个：
  - 不能把旧格式泛答硬吸收成正式状态票
  - 也不能把对象串题或未答题票当成 `no` 或 `conditional`
- 当前先做什么：
  - 新增一份更硬的超窄纯文本发包稿
  - 新增下一轮回收模板
  - 把主链状态回填为：
    - `状态不变`
    - `等待超窄纯文本回包`
- 当前暂缓什么：
  - 继续为 `portfolio_tracking_error` 主负责人单拍更高状态名
  - 切到 `adjusted_position_weight`
