# A5 A股 P0 技术指标产品层第二轮共享优先回链适配合同页

更新时间：2026-07-24

## 一、这页用途

- 本页只做：
  - 冻结
    `首页 / 标的页 / 问答页`
    共享优先回链适配器的职责边界
- 本页不做：
  - 新指标扩充
  - 金融真值判断
  - 页面结构改版

## 二、当前主线

- 当前主线是：
  - `A股 P0 技术指标产品层第二轮补全`
- 当前不是：
  - `FV gate`
  - 技术指标组合金融可行性
  - 指标工作台

## 三、当前最值钱的一步

- 当前最值钱的一步是：
  - 不再让
    `home / stock / qa`
    三处各自维护一套优先回链判断，
    而是收成
    `一套共享适配器`

## 四、主负责人裁决

- 当前正式裁决：
  - 三页关于
    `角色判断`
    `代表指标判断`
    `背景解释优先级`
    `短句拼接`
    的逻辑，
    应收进同一层 helper
- 当前不选：
  - 继续三处分散写 if/else
  - 把共享逻辑藏到页面组件里
  - 先做外观美化再补共享层

## 五、共享适配器要负责什么

- 当前共享适配器只负责：
  - 判断一个来源名属于：
    - `背景解释`
    - `技术指标语义源`
  - 判断一个来源名是否属于
    `5`
    条代表指标
  - 生成统一的标签结构：
    - `label`
    - `detail`
  - 生成统一的短句结构：
    - `当前优先回链`
    - `当前解释先看`
    - `当前回答优先回链`
  - 生成统一的背景优先级：
    - `ZSDB指数对比`
    - `HYDB行业对比`

## 六、共享适配器当前不负责什么

- 当前不负责：
  - 事件列表装配
  - 解释正文内容
  - 问答答案正文
  - 页面布局与样式
  - 金融限制口径

## 七、当前检查清单

- 若
  `home / stock / qa`
  仍各自保留不同的
  `角色判断`
  或
  `背景优先级`
  逻辑，
  视为未收口
- 若共享层开始承担：
  - 样式
  - 页面文案大段内容
  - 金融判断
  视为越界

## 八、下一手执行物

- 当前这一页之后，
  下一手唯一应落的是：
  - 前端共享 helper 文件
  - 对应测试文件
- 只做：
  - 吸收重复逻辑
  - 回归现有页面 contract
  - 不顺手开新 feature 工作台

## 九、一句话口径

- 第二轮当前继续补的，
  不是新指标，
  而是把
  `三页分散的优先回链判断`
  收成
  `一套共享适配器`，
  以此压低后续口径漂移。
