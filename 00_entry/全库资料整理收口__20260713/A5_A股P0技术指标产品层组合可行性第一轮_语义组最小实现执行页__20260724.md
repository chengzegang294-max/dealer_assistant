# A5 A股 P0 技术指标产品层组合可行性第一轮 语义组最小实现执行页

更新时间：2026-07-24

## 一、这页用途

- 本页只做：
  - 冻结当前阶段
    `语义组最小实现`
    的执行边界
- 本页不做：
  - 前台按组重排
  - 新卡片
  - 问答页按组生成问题

## 二、当前主线

- 当前主线是：
  - `A股 P0 技术指标产品层组合可行性第一轮`
- 当前不是：
  - 金融真值层
  - 组合回测
  - 页面大改版

## 三、当前最值钱的一步

- 当前最值钱的一步是：
  - 让首页 / 标的页 / 问答页
    现有的
    `优先回链短句`
    统一带出对应语义组
- 但仍保持：
  - `sourceCard`
    在前
  - 语义组只作为第二层说明

## 四、当前正式裁决

- 当前最小实现只改：
  - `buildIndicatorContextSummary`
    这类共享短句生成逻辑
- 当前继续收口时允许：
  - 把
    `行业 / 指数参照 -> 当前解释骨架`
    `资金活跃 -> 当前事件说明补强`
    `市场宽度 -> 当前轻背景说明`
    收成共享 helper
- 当前不改：
  - `tag chip`
    展示文本
  - 问答题目文本
  - 页面结构

## 五、输出合同

- 当前输出格式统一为：
  - `前缀：角色 / sourceCard · 语义组：groupLabel`
- 若当前对象没有语义组，
  则保持旧格式

## 六、三页边界

- 首页：
  - 只让解释卡短句带出语义组
- 标的页：
  - 只让解释区短句带出语义组
- 问答页：
  - 只让回答优先回链短句带出语义组

## 七、停止规则

- 当前做到以下条件后立刻停：
  1. 三页短句统一吃共享语义组说明
  2. `sourceCard`
     仍是第一锚点
  3. 标的页 / 问答页角色短句不再各自维护重复映射
  4. 测试与 build 通过

## 八、一句话口径

- 当前最准确的一句话是：
  - 组合可行性第一轮当前先把
    `语义组`
    压进三页现有短句里，
    只做第二层说明，
    不做页面重排。
