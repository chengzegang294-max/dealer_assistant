# A5 对象卡正式化检查清单

更新时间：2026-07-16

## 用途

- 这份清单只服务一个目的：
  - 判断 `A5_最小对象卡草案` 距离“正式对象卡”还差哪几步
- 当前不做实现。
- 当前只做检查与冻结边界。

## 当前检查结论

- 当前已经具备：
  - 最小输入字段
  - 最小输出字段
  - 统一验收断言
  - 统一降级矩阵
  - 统一对象接口草案
  - 最小样本实例卡
- 当前仍未具备：
  - runtime 实现
  - 完整 provider 闭合
  - 历史验证闭合

## 一、字段检查

- `A_REQ_002` 字段是否都能归入统一接口：
  - `yes`
- `A_REQ_003` 字段是否都能归入统一接口：
  - `yes`
- 是否仍有未分组字段：
  - `no`

## 二、断言检查

- 解释层关键字段回溯断言是否已冻结：
  - `yes`
- 组合层关键公式断言是否已冻结：
  - `yes`
- 是否已有统一断言表：
  - `yes`

## 三、降级检查

- 财务侧降级是否已冻结：
  - `yes`
- 组合侧降级是否已冻结：
  - `yes`
- 数据侧边界是否已有独立口径页：
  - `yes`
  - 见 `A_REQ_001` 两页

## 四、原始缺口回看

- 原缺口 1：
  - `字段真实来源状态表`
  - 当前状态：
    - `已补`
- 原缺口 2：
  - `最小样本实例卡`
  - 当前状态：
    - `已补`
- 原缺口 3：
  - `runtime 对接边界页`
  - 当前状态：
    - `已补`

## 四点五、当前已补齐的最小正式化三件套

- 已落盘：
  - `A5_字段真实来源状态表__20260715.tsv`
  - `A5_runtime对接边界页__20260715.md`
  - `A5_最小样本实例卡__20260716.md`

### 当前推进结果

- 现在已经能明确区分：
  - `direct`
  - `proxy`
  - `rules_engine_derivable`
  - `future_only`
- 也已经能明确区分：
  - 哪些字段可 formalize
  - 哪些字段不得误写成 runtime 已支持
- 现在也已经有：
  - 一张不冒充 runtime 的最小阅读样例

## 五、当前禁止误写

- 禁止写成：
  - `A5 对象卡已完成`
  - `A5 已可直接运行`
  - `A5 所有字段都已闭合`
- 当前只能写成：
  - `A5 已到对象卡草案 + 样本实例卡层`
  - `A5 已补齐最小正式化三件套，但仍未到 runtime 层`

## 六、主负责人裁决

- 现在不该回去扩资料。
- 现在也不该直接跳 runtime。
- 当前这条线最顺的缺口已经补齐：
  - `最小样本实例卡`
- 当前可以正式写成：
  - `A5 已到可读样本层`
- 若后续还要继续下沉：
  - 应转去做未来对象卡模板或实现前冻结
  - 不应回退到资料泛补

## 回链

- `A5_最小对象卡草案__20260715.md`
- `A5_合同层桥接总表__20260715.md`
- `A5_统一字段分组表__20260715.tsv`
- `A5_统一验收断言表__20260715.tsv`
- `A5_统一降级矩阵__20260715.tsv`
- `A5_字段真实来源状态表__20260715.tsv`
- `A5_runtime对接边界页__20260715.md`
- `A5_最小样本实例卡__20260716.md`
- `A5_正式对象卡模板__20260716.md`
- `A5_实现前冻结门槛页__20260716.md`
