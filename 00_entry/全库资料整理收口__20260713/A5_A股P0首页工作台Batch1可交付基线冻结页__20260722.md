# A5 A股 P0 首页工作台 Batch1 可交付基线冻结页

更新时间：2026-07-22

## 一、冻结对象

- 冻结对象：`A股 P0 首页工作台 Batch1` 前台最小闭环实现（前端代码）
- 冻结目的：
  - 把“可交付基线”写成可复用停点
  - 后续开下一刀时不回头空转结构美化与无停点扩测

## 二、冻结口径（一句话）

- `首页薄壳 + workspace 不变量 + section props + adapter 分层 + 模型/WorkspaceHook/PageHook 三层护栏 + 异步数据源桩`

## 三、冻结证据

- 自动化：
  - `npm run test` 通过（模型/WorkspaceHook/PageHook）
  - `npm run lint` 通过
  - `npm run build` 通过
- 走查：
  - 桌面端第三轮最小走查通过：
    - `00_entry/全库资料整理收口__20260713/A5_A股P0_Batch1桌面端第三轮走查记录__20260722.md`

## 四、冻结范围与红线

- 冻结范围：
  - 当前页面结构、section contract、adapter 分层、workspace 不变量
  - 当前事件/解释/记录数据仍为样本，但入口已具备异步数据源桩
- 红线：
  - 不在冻结点上继续做 `EventStreamPanel` contract 对齐
  - 不扩成 E2E 自动化
  - 不在未引入真实数据协议前做“看似完整”的数据层重构

## 五、下一阶段候选（只开一刀）

- 下一阶段候选：接入真实数据协议或后端接口前的最小桩扩展（仍保持 UI 不变）。
- 停止规则：完成最小数据源替换点并保持 `test/lint/build` 全绿后立刻停止。
