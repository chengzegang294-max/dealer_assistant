# A5 portfolio_tracking_error frozen 后续升级判断 超窄纯文本回收记录模板

更新时间：2026-07-18

## 一、回收总表

| 模型 | 有效性 | 第 1 行结论 | 第 2 行一句话原因 | 第 3 行唯一缺口 / 条件 | 第 4 行禁止误写提醒 | 第 5 行唯一焦点 | 备注 |
|---|---|---|---|---|---|---|---|
| `GPT` | `valid` | `no` | `第二轮正式裁决已判零有效票面` | `形成可互相校验的更强状态票面` | `当前仍维持 not_output_passed` | `N/A` | `格式合格，是当前最稳保守票之一` |
| `DeepSeek` | `weak_valid` | `conditional` | `上游风险模型仍未正式闭合` | `covariance_model_id 需给出更强正式闭合写法` | `风险模型未闭合，输出基础不成立` | `N/A` | `方向可参考，但把上游条件写得偏旧` |
| `Kimi` | `valid` | `no` | `风险模型仍为有条件状态` | `covariance_model_id 更强闭合或降级口径最终验收` | `covariance_model_id 未正式闭合` | `N/A` | `格式合格，是当前最稳保守票之一` |
| `GLM` | `weak_valid` | `conditional` | `当前连一张有效状态票都没有` | `需先通过超窄纯文本发包获取有效票面` | `仍处降级风险且协方差未闭合` | `N/A` | `格式合格，但其附加条件已被本轮部分满足，强度偏弱` |
| `Qwen` | `invalid` | `conditional` | `需验证下游稳定性` | `N/A` | `仍处降级风险模式` | `N/A` | `未按 1)-5) 合同输出，且第 3 行缺少唯一条件` |

## 二、逐模型记录

### 1. `GPT`

- 有效性：
  - `valid`
- 第 1 行结论：
  - `no`
- 第 2 行一句话原因：
  - `第二轮正式裁决已判零有效票面`
- 第 3 行唯一缺口 / 条件：
  - `形成可互相校验的更强状态票面`
- 第 4 行禁止误写提醒：
  - `当前状态仍维持 not_output_passed`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `格式合格`
  - `理由偏保守，但没有串题`

### 2. `DeepSeek`

- 有效性：
  - `weak_valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `上游风险模型仍未正式闭合`
- 第 3 行唯一缺口 / 条件：
  - `covariance_model_id 需给出更强正式闭合写法`
- 第 4 行禁止误写提醒：
  - `风险模型未闭合，输出基础不成立`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `格式合格`
  - `但把上游需补条件写得偏旧，不能直接作为主模式`

### 3. `Kimi`

- 有效性：
  - `valid`
- 第 1 行结论：
  - `no`
- 第 2 行一句话原因：
  - `风险模型仍为有条件状态`
- 第 3 行唯一缺口 / 条件：
  - `covariance_model_id 更强闭合或降级口径最终验收`
- 第 4 行禁止误写提醒：
  - `covariance_model_id 未正式闭合`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `格式合格`
  - `与当前最稳保守口径一致`

### 4. `GLM`

- 有效性：
  - `weak_valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `当前连一张有效状态票都没有`
- 第 3 行唯一缺口 / 条件：
  - `需先通过超窄纯文本发包获取有效票面`
- 第 4 行禁止误写提醒：
  - `仍处降级风险且协方差未闭合`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `格式合格`
  - `但其唯一条件在本轮已被部分满足，解释力偏弱`

### 5. `Qwen`

- 有效性：
  - `invalid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `需验证下游稳定性`
- 第 3 行唯一缺口 / 条件：
  - `N/A`
- 第 4 行禁止误写提醒：
  - `仍处降级风险模式`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `没有按 1)-5) 固定行前缀输出`
  - `且未给出唯一条件`

## 三、主负责人预留

- 当前有效票面：
  - `GPT = no`
  - `Kimi = no`
  - `DeepSeek = weak conditional`
  - `GLM = weak conditional`
- 当前正式裁决：
  - `继续维持 pass_conditions_frozen__not_output_passed`
  - `当前题正式裁为 no`
- 当前为什么选这个：
  - `没有出现 yes 票`
  - `最稳直接票是 GPT / Kimi 的 no`
  - `两个 conditional 票都把唯一条件写得偏弱或偏旧`
- 当前为什么不选另外几个：
  - `不选 conditional 为主模式，因为其条件没有稳定收敛为同一条`
  - `不采纳 Qwen，因为未按固定合同输出`
- 当前先做什么：
  - `把 portfolio_tracking_error frozen 后续升级判断正式裁成 no`
  - `把唯一剩余缺口收缩回 covariance_model_id 更强闭合`
- 当前暂缓什么：
  - `继续为 portfolio_tracking_error 本身反复重问`
  - `切回 adjusted_position_weight`
