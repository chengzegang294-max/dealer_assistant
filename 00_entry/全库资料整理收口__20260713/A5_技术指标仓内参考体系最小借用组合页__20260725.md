# A5 技术指标仓内参考体系最小借用组合页

更新时间：2026-07-25

## 一、这页用途

- 本页只做：
  - 把当前已裁定的最小借用组合正式写成执行页
  - 明确：
    - `NFTRADEZ`
      借哪一层
    - `Smile_SMC`
      借哪一层
    - `TK`
      为什么暂不进入第一手
- 本页不做：
  - 直接抽原文内容
  - 直接展开五条代表指标映射
  - 直接判断金融有效性

## 二、当前正式裁决

- 当前正式裁决已经写死为：
  - `borrow_minimal_combo_first`

- 当前最小借用组合是：
  1. `NFTRADEZ`
  2. `Smile_SMC`

- 当前不进入第一手的是：
  - `TK`

## 三、为什么这个组合最小且够用

- 当前主线真正要补的，
  不是：
  - 策略本体
  - 回测入口
  - 执行规则

- 当前真正要补的是两类抓手：
  1. `怎么说边界`
  2. `怎么讲失效`

- 因此当前最小可用组合自然拆成：
  - `NFTRADEZ`
    负责补：
    `怎么说边界`
  - `Smile_SMC`
    负责补：
    `怎么讲失效`

## 四、NFTRADEZ 当前只借哪一层

- 当前只借：
  - `风险约束语言`

- 更具体地说，
  当前优先看：
  1. `risk_guard`
  2. `glossary`
  3. `premarket`

- 当前它要补的不是：
  - 信号本体
  - 执行逻辑
  - 对象合同

- 当前它只负责补：
  1. 风险边界怎么说
  2. 什么情况下该降权
  3. 什么情况下该回观察位
  4. 哪些表达适合做统一风险语言模板

## 五、Smile_SMC 当前只借哪一层

- 当前只借：
  1. `explain / review 结构`
  2. `失效样式命名`

- 当前它要补的不是：
  - SMC 主策略
  - 交易执行规则
  - ICT 概念整体迁移

- 当前它只负责补：
  1. 结构破坏怎么讲
  2. 误判样式怎么讲
  3. 失效过程如何写成可复用叙事
  4. review 时如何把
     `发生了什么 -> 为什么降级 -> 现在归到哪一类`
     讲清楚

## 六、TK 为什么先不进第一手

- 当前不是说：
  - `TK`
    没价值

- 当前只是说：
  - 它更适合第二补位

- 当前它最适合补的是：
  1. `例外`
  2. `降级`
  3. `细判据口径`

- 这说明它更像：
  - 第一手组合已经搭起来后，
    再补细判据

- 若一开始就把
  `TK`
  拉进第一手，
  会让当前借用组合从：
  - `最小组合`
  变成：
  - `三体系混借`

## 七、当前借用合同怎么写

| 体系 | 当前只借哪一层 | 当前补什么 | 当前明确不借什么 |
| --- | --- | --- | --- |
| `NFTRADEZ` | `风险约束语言` | 风险边界、降权口径、观察位口径 | 策略本体、执行逻辑、对象合同 |
| `Smile_SMC` | `explain / review 结构` + `失效样式命名` | 结构破坏叙事、误判样式、失效解释模板 | 主策略、交易规则、概念整体迁移 |
| `TK` | `暂不进入第一手` | 候补的例外-降级细判据 | 不提前混入第一手组合 |

## 八、当前如何回接主线

- 当前这组最小借用组合，
  后续只回接到：
  1. `失效方式与波动风险约束第一轮内部分类页`
  2. `退出层与失效方式第一轮讨论页`
  3. 后续的：
     `五条代表指标 x 仓位层 / 退出层 / 失效方式`
     职责切换讨论

- 当前不回接到：
  - 页面层
  - 回测层
  - 自动交易层

## 九、当前 still_need_evidence

- 当前继续保留
  `still_need_evidence`
  的问题有：
  1. `NFTRADEZ`
     的风险约束语言，
     是否完全适配 A股技术指标主线
  2. `Smile_SMC`
     的 explain / review 结构，
     是否会引入概念迁移噪音
  3. 两者组合后的模板，
     是否足以稳住下一步门槛复核

## 十、主负责人裁决

- 当前正式裁决为：
  1. 第一手只借：
     - `NFTRADEZ`
       的风险约束语言
     - `Smile_SMC`
       的
       explain / review 结构
       与失效样式命名
  2. `TK`
     当前留在候补位
  3. 下一步不再讨论
     `借谁`
     ，
     而是讨论：
     `这组最小组合如何映射回五条代表指标`

## 十一、一句话口径

- 当前仓内参考体系的最小借用组合已经冻结：
  `NFTRADEZ`
  负责补
  `风险约束语言`
  ，
  `Smile_SMC`
  负责补
  `explain-review / 失效样式命名`
  ，
  `TK`
  暂留候补位，
  后面再看是否需要补
  `例外-降级细判据`。

## 回链

- `A5_技术指标外部成熟体系辅助输入_仓内参考体系优先_多家AI回收记录与主负责人裁决__20260725.md`
- `A5_技术指标结构性缺位第一轮_仓位层退出层失效方式与外部参考边界页__20260725.md`
- `A5_技术指标失效方式与波动风险约束第一轮内部分类页__20260725.md`
- `参考交易系统补齐状态表__20260713.md`
