# A5 技术指标外部成熟体系辅助输入 仓内参考体系优先 多家AI回收记录与主负责人裁决

更新时间：2026-07-25

## 一、这页用途

- 本页只做：
  - 吸收本轮
    `仓内参考体系辅助输入`
    的多AI回包
  - 正式拍板：
    - 先借哪一个体系
    - 还是先借最小组合
    - 各自只借到哪一层
- 本页不做：
  - 直接代替后续映射执行页
  - 把任何参考体系写成金融真值

## 二、临时中转吸收记录

- 原临时路径：
  - `d:\Stock\dealer_assistant\暂时存放\粘贴区.md`
- 材料类型：
  - 本轮
    `仓内参考体系辅助输入`
    多家AI回包汇总
- 是否值得吸收：
  - `yes`
- 正式去向：
  - `A5_技术指标外部成熟体系辅助输入_仓内参考体系优先_多家AI回收记录与主负责人裁决__20260725.md`
  - `A5_技术指标仓内参考体系最小借用组合页__20260725.md`
- 是否允许继续留在暂时存放：
  - `no`
- 删除条件：
  - 正式页、README 与活跃入口回填完成后，
    按
    `delete_after_review`
    处理

## 三、回收摘要

### 3.1 各模型结论

- `qwen`
  - 结论：
    `borrow_minimal_combo_first`
  - 倾向：
    先借
    `NFTRADEZ`
    的风险约束语言
    +
    `Smile_SMC / TK`
    的例外-降级判据
- `kimi`
  - 结论：
    `borrow_nftradez_first`
  - 倾向：
    先只借
    `NFTRADEZ`
    的
    `例外与降级判据层`
- `deepseek`
  - 结论：
    `borrow_minimal_combo_first`
  - 倾向：
    先借
    `NFTRADEZ`
    的风险约束语言
    +
    `Smile_SMC`
    的
    `explain / review`
    结构
- `glm`
  - 结论：
    `borrow_minimal_combo_first`
  - 倾向：
    先借
    `NFTRADEZ`
    的
    `例外 / 降级判据`
    +
    `Smile_SMC`
    的
    `失效样式命名 / explain-review`
    结构
- `gpt`
  - 结论：
    `borrow_minimal_combo_first`
  - 倾向：
    先借
    `NFTRADEZ`
    的风险约束语言
    +
    `Smile_SMC`
    的
    `explain-review`
    结构
  - `TK`
    只保留为下一顺位候补

### 3.2 结论统计

- 当前支持
  `borrow_nftradez_first`
  的模型：
  - `kimi`
- 当前支持
  `borrow_smile_or_tk_first`
  的模型：
  - `无`
- 当前支持
  `borrow_minimal_combo_first`
  的模型：
  - `qwen`
  - `deepseek`
  - `glm`
  - `gpt`

### 3.3 当前一致点

- 当前高信号一致点已经足够明确：
  1. 当前不建议先借
     `Smile_SMC`
     或
     `TK`
     单体
  2. 当前第一优先对象都指向：
     `NFTRADEZ`
  3. 当前若不是只借
     `NFTRADEZ`
     单体，
     那也应是：
     `NFTRADEZ + Smile_SMC`
     的最小组合
  4. `TK`
     当前只适合留在：
     - 第二顺位候补
     - 例外 / 降级细判据补位

### 3.4 当前最大分歧点

- 当前最大分歧点只剩：
  1. 是不是先只借
     `NFTRADEZ`
     一层就够
  2. 还是应一开始就直接上
     `NFTRADEZ + Smile_SMC`
     的最小组合

- 但这个分歧已经足够收敛为：
  - `NFTRADEZ`
    必在第一手
  - `Smile_SMC`
    是最自然的第二手互补对象

## 四、主负责人判断

### 4.1 当前最稳结论是什么

- 当前判断是：
  - `borrow_minimal_combo_first`

- 原因是：
  1. 当前
     `4 / 5`
     模型都支持最小组合
  2. 当前没有任何模型支持：
     `borrow_smile_or_tk_first`
  3. 唯一的单体派，
     也同样把
     `NFTRADEZ`
     放在第一优先
  4. 这说明当前不是对象分歧，
     而是：
     - 第一手要不要只借
       `NFTRADEZ`
       就停
     - 还是直接把
       `Smile_SMC`
       一起纳入最小组合

### 4.2 为什么不是先只借 NFTRADEZ

- 因为：
  1. `NFTRADEZ`
     最强的是：
     - 风险约束语言
     - risk_guard / glossary / premarket
  2. 但本轮高频回包都指出：
     当前还缺：
     - `失效样式怎么讲`
     - `explain / review`
       结构
     - `结构破坏与误判`
       的叙事骨架
  3. 这正是
     `Smile_SMC`
     当前最适合补的层
  4. 因此只借
     `NFTRADEZ`
     会让主线变成：
     `有边界语言，但失效叙事仍偏虚`

### 4.3 为什么不是把 TK 也一起拉进第一手

- 因为：
  1. 当前没有模型支持：
     `TK`
     单体优先
  2. 多个回包都把
     `TK`
     放在：
     - 第二顺位候补
     - 例外 / 降级细判据补位
  3. 若一开始就上三者，
     会破坏
     `最小组合`
     原则
  4. 当前更稳的做法是：
     - 第一手先借
       `NFTRADEZ + Smile_SMC`
     - 若仍不足，
       再补
       `TK`

## 五、主负责人裁决

- 当前正式裁决为：
  1. 当前先不走
     `NFTRADEZ`
     单体优先
  2. 当前正式采用：
     `borrow_minimal_combo_first`
  3. 当前最小借用组合写死为：
     - `NFTRADEZ`
       借：
       `风险约束语言`
     - `Smile_SMC`
       借：
       `explain / review 结构`
       与
       `失效样式命名`
  4. `TK`
     当前不进入第一手，
     只保留为：
     - `例外 / 降级判据`
       的候补补位
  5. 这些材料都只作为：
     `辅助案例输入`
     ，
     不并入主线本体

## 六、为什么这样裁

### 6.1 为什么选最小组合

- 因为：
  1. 当前缺口不是单层缺口
  2. 当前至少缺两类抓手：
     - `怎么说边界`
     - `怎么讲失效`
  3. `NFTRADEZ`
     更适合补第一类
  4. `Smile_SMC`
     更适合补第二类

### 6.2 为什么最小组合是 NFTRADEZ + Smile_SMC

- 因为：
  1. 这是当前回包里最稳定的组合
  2. 与旧边界页里的优先顺序：
     `NFTRADEZ -> Smile_SMC -> TK`
     一致
  3. 能在不扩大借用面的前提下，
     一次补齐：
     - 风险约束语言
     - explain-review 结构
     - 失效样式命名

### 6.3 为什么 TK 先放候补位

- 因为：
  1. `TK`
     当前最适合补的是：
     `例外 / 降级 / 细判据`
  2. 这层更像第二补位，
     不是第一手的主抓手
  3. 若第一手就拉进来，
     会让支线过早膨胀

## 七、当前先做什么

- 当前先做：
  1. 落
     `A5_技术指标仓内参考体系最小借用组合页__20260725.md`
  2. 把
     `NFTRADEZ`
     与
     `Smile_SMC`
     各自借用层写死
  3. 以该页为前情，
     准备下一轮：
     `最小借用组合如何映射回五条代表指标`
     的正式讨论

## 八、暂缓什么

- 暂缓：
  - `TK`
    第一手进入
  - 仓外公开体系 / YouTube
  - 任何参考体系并入主线本体
  - 任何金融有效性升级

## 九、一句话口径

- 本轮仓内参考体系辅助输入的正式裁决已经收敛为：
  当前先用
  `NFTRADEZ + Smile_SMC`
  的最小组合，
  分别补
  `风险约束语言`
  与
  `explain-review / 失效样式命名`
  ；
  `TK`
  暂时留在例外-降级细判据的候补位。
