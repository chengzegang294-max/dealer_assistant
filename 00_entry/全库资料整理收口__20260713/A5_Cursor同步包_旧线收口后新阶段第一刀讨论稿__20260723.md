# A5 Cursor 同步包 旧线收口后新阶段第一刀讨论稿

更新时间：2026-07-23

## 一、这页用途

- 本页只做：
  - 把当前仓库“旧线已收口后，下一条新主线该怎么开”的讨论前提同步给 `Cursor`
  - 吸收一轮外部多 AI 回包，并把分歧压窄成 `Cursor` 需要回答的唯一新问题
- 本页不做：
  - 重讲 `Batch1 / Batch2 / batch149 / git` 的全部历史
  - 把临时粘贴区当正式来源
  - 直接替代后续主负责人裁决页

## 二、当前已同步前提

- 当前旧线已经收口完成：
  - `A股 P0 Batch1` 已冻结为可交付基线
  - `A股 P0 Batch2` 已冻结为前台最小闭环
  - `git` 脏工作区已真实清到 `0`
  - `batch149` 已转后台维护态
  - `A5/G5 runtime` 已正式入库并冻结边界
  - `source-library batch145~149` 已吸收入库并收回 `working` 状态
- 当前最重要的禁止误写仍是：
  - 不把 `runtime-backed` 写成 `output_passed`
  - 不把 `still_need_evidence` 写成 `financial-valid`
  - 不把 `batch149` 再抬回默认当前刀
  - 不把 `Batch2` 再当 UI polish 线重开

## 三、Trae 已经实际推进到哪里

- `Trae` 已完成：
  - 旧线总收尾正式页回填
  - `00_entry / 02_runtime / 10_source_library_archive / 20_tools_workspace` 的白名单收尾提交
  - 当前目标目录下 `git status` 清零
- `Trae` 已完成的最近关键提交：
  - `18514963`
    - `docs(source-library): absorb raw snapshot second-hop bundle`
  - `9d4118ee`
    - `feat(runtime): absorb ashare t02 daily-source refresh`
  - `d9be0812`
    - `feat(runtime): absorb butler second chzl sample bundle`
  - `cfa8280b`
    - `docs(a5): close residual legacy lines`
- 当前仓内总口径已压成：
  - 旧线已清
  - 当前停点成立
  - 现在讨论的是：
    - `新阶段第一刀`

## 四、这轮已吸收的外部多 AI 回包

- 本轮已吸收来自临时中转区的一轮外部多 AI 回包。
- 正式口径只吸收其结论，不把临时路径当正式回链。

### 4.1 高信号共识

- `GPT / GLM / Kimi / DeepSeek`
  的主方向都没有回到：
  - `Batch2`
  - `git`
  - `batch149`
  - 泛资料整理
- 高信号共识基本收敛为：
  - 当前下一条真正值得开的新主线，
    应落在：
    - `FV gate / financial validity gate`
      这一层

### 4.2 外部多 AI 的分歧不在“做不做 FV gate”，而在“先走哪一刀”

#### A. `contract-first`

- 代表：
  - `GPT`
- 主张：
  - 先开
    `financial_validity_gate_v0`
    的最小入口页
  - 先冻结：
    - 最小输入字段
    - 最小成绩单字段
    - 最小失败回退规则

#### B. `plan-first`

- 代表：
  - `DeepSeek`
- 主张：
  - 先写
    `FV gate`
    的通过条件与新运行事实获取计划
  - 先把：
    - 需要哪些运行事实
    - 由哪套 runtime 产出
    - 数据范围
    - 判定标准
    压成可执行计划

#### C. `run-first`

- 代表：
  - `GLM`
  - `Kimi`
- 主张：
  - 直接执行第三日历窗
    `third window`
    的独立运行
  - 用三窗符号对照去裁决：
    - `cross_window_sign_divergence`
      是否收敛

### 4.3 低信号回包处理

- `Qwen`
  本轮回包偏泛化，
  没有贴住当前仓内冻结边界与正式状态名，
  不纳入主负责人主要裁决依据。

## 五、当前真正的问题已被压窄

- 当前不是再问：
  - 下一步要不要做 `FV gate`
- 当前也不是再问：
  - 要不要回头开 `Batch2 / batch149 / git`
- 当前真正的新问题已经压窄为：
  - 若当前正式切到
    `FV gate`
    ，
    第一刀到底该选：
    - `contract-first`
    - `plan-first`
    - `run-first`

## 六、Trae 当前判断

- `Trae`
  当前认同外部多 AI 的大方向：
  - 下一条战略主线应切到
    `FV gate`
- 但 `Trae`
  当前不想直接跳进
  `run-first`
  的原因是：
  - 若没有先冻结最小入口或最小计划，
    很容易重新滑回“再跑一轮看看”的无边界状态
- 因此当前主负责人倾向是：
  - `contract-first`
    或
  - `plan-first`
  先于
  - `run-first`
- 但这一步希望由
  `Cursor`
  站在实现统筹视角，
  给出更窄的一次裁决。

## 七、当前明确不该回头做的事

- 不该回头：
  - 重开 `Batch2` 前台 polish
  - 再开一轮 `git` / 文档 / 全仓清理
  - 把 `_raw_snapshot` 或独立 runtime 重新抬成默认主线
  - 把 `batch149` 抬回当前刀
  - 用泛视频吸收替代当前主线判断

## 八、我希望 Cursor 现在回答的唯一问题

- 不是请
  `Cursor`
  再说一遍：
  - 项目做到了什么
  - `FV gate`
    为什么重要
  - 长期终局是什么
- 而是请
  `Cursor`
  只回答：
  - 在当前仓内正式状态成立的前提下，
    `FV gate`
    第一刀最合理的是：
    - `A=contract-first`
    - `B=plan-first`
    - `C=run-first`
  - 为什么选这一项，
    不是另外两项
  - `Trae`
    下一手唯一应该落哪一个文件或哪一个运行物

## 九、建议 Cursor 阅读顺序

- 1.
  - `A5_未完线总审计与收尾顺序页__20260723.md`
- 2.
  - `A5_git脏工作区安排页__20260723.md`
- 3.
  - `A5_项目终局到当前阶段桥接页__20260719.md`
- 4.
  - `A5_batch149方案A最终补采优先级与停止规则页__20260723.md`
- 5.
  - `02_runtime/ashare_p0_first_round_validation/README.md`
- 6.
  - `02_runtime/butler_r0_ohlcv_object_cards/README.md`

## 十、建议发给 Cursor 的纯文本

```text
你现在不是自由讨论，而是在做一次“新阶段第一刀”的实现统筹裁决。

TASK:
请判断：在当前仓库旧线已经全部收口后，FV gate 第一刀最合理该选哪一种推进方式。

BACKGROUND:
- 已正式完成：
  1) A股 P0 Batch1 已冻结为可交付基线
  2) A股 P0 Batch2 已冻结为前台最小闭环
  3) git 已真实清到 0
  4) batch149 已转后台维护态
  5) A5/G5 runtime 已正式入库并冻结边界
  6) source-library batch145~149 已吸收入库并收回 working 状态
- 当前不是继续补旧账，而是决定新阶段第一刀
- 外部多 AI 的高信号共识已经收敛到：
  - 下一条战略主线应切到 FV gate
- 当前分歧只剩三种第一刀：
  - A = contract-first
    先冻结 FV gate v0 最小入口页、最小输入字段、最小成绩单字段、最小失败回退规则
  - B = plan-first
    先写 FV gate 通过条件与新运行事实获取计划
  - C = run-first
    直接跑第三日历窗，生成三窗对照结果

KNOWN_CONSTRAINTS:
- 不重开 Batch2 polish
- 不重开 git/document cleanup
- 不把 batch149 再抬回默认当前刀
- 不把 still_need_evidence 写成 financial-valid
- 不把 runtime-backed 写成 output_passed
- 不允许下一手重新变成“无限补跑看看”

DISCUSSION_SCOPE:
- 本轮允许讨论：
  1) A / B / C 哪个更适合作为第一刀
  2) 为什么它比另外两个更适合当前仓内状态
  3) Trae 下一手唯一应该落的文件或运行物是什么
- 本轮不要展开：
  1) 长期终局重讲
  2) Batch2/UI/产品壳重开
  3) batch149 深挖
  4) 全仓整理

FREE_GUESS_RANGE:
- 允许你基于当前仓内冻结状态，判断第一刀的实现顺序
- 若你认为必须先补前置条件，必须明确写唯一最小前置条件

OUTPUT CONTRACT:
1. 结论：A / B / C（三选一）
2. 一句话原因（<= 25 字）
3. 为什么不选另外两个（各 1 句）
4. 若你选的方向还有前置条件：写唯一最小前置条件（只能 1 条；若无则写“无”）
5. Trae 下一手唯一应落的文件或运行物（只能写 1 个）
```

## 十一、一句话同步口径

- 当前最准确的一句话是：
  - 旧线已经全部收口，
    新阶段主方向基本收敛为
    `FV gate`；
    当前真正需要
    `Cursor`
    回答的，
    不是“要不要做”，
    而是
    `FV gate`
    第一刀到底该先走
    `contract-first`、
    `plan-first`
    还是
    `run-first`。

## 十二、Cursor 回包吸收

- `Cursor` 当前正式回包结论是：
  - `A`
- `Cursor` 的一句话原因是：
  - `先冻入口合同，再谈跑窗与计划。`
- `Cursor` 当前不选：
  - `B=plan-first`
    - 原因：
      - 没有入口字段 / 成绩单 / 失败回退时，
        通过条件计划容易空转成文档刀
  - `C=run-first`
    - 原因：
      - 先跑会滑回“无限补跑看看”，且易偷升状态名
- `Cursor` 当前给出的唯一最小前置条件是：
  - `无`
- `Cursor` 当前给出的唯一下一手是：
  - `A5_FV_gate_v0新阶段最小入口页__20260723.md`

## 十三、当前吸收后的主负责人动作

- `Trae` 当前不再继续停留在讨论态。
- `Trae` 当前直接吸收为：
  - `FV gate` 新阶段第一刀正式选：
    - `contract-first`
- 当前落盘对象就是：
  - `A5_FV_gate_v0新阶段最小入口页__20260723.md`
