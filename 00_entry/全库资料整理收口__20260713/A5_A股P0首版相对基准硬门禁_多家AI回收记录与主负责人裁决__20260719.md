# A5 A股 P0 首版相对基准硬门禁 多家AI回收记录与主负责人裁决

更新时间：2026-07-19

## 一、当前唯一问题

- 当前要裁决的不是：
  - 项目终局是否存在
  - 第一版要不要做全市场
  - 要不要继续第四窗
- 当前要裁决的唯一问题是：
  - 对 `A股 P0`
    第一版可用雏形而言，
    是否应把：
    - `相对基准不系统性跑输`
    写成进入产品原型阶段的
    `硬门禁`

## 二、回收对象

- 本轮已回收：
  - `DeepSeek`
  - `Qwen`
  - `GPT`
  - `GLM`
  - `Kimi`

## 三、回收摘要表

| 模型 | 结论 | 推荐方案 | 是否对题 | 是否越界 | 备注 |
|---|---|---|---|---|---|
| DeepSeek | `conditional` | `C` | `yes` | `no` | 认为当前不能设硬门禁，但也不能无门槛放行，建议补条件式门禁合同 |
| Qwen | `conditional` | `C` | `partially` | `partially` | 方向与多数一致，但抽象度偏高，唯一下一手不够硬 |
| GPT | `conditional` | `C` | `yes` | `no` | 把“原型可进入”与“金融仍是 still_need_evidence”区分得最清楚 |
| GLM | `no` | `B` | `yes` | `no` | 主张直接允许原型进入，但强调强披露与强限权 |
| Kimi | `no` | `B` | `yes` | `partially` | 主张先做原型，但给了第四窗后的停止规则，略有越界倾向 |

## 四、主负责人对回包的判断

- 当前没有任何模型支持：
  - `A`
    即：
    - 直接设为硬门禁
- 当前真实分歧只剩：
  - `B`
    即：
    - 不设硬门禁，
      允许明示限制下先做原型
  - `C`
    即：
    - 条件式门禁
- 当前我判断：
  - `C`
    比
    `B`
    更适合当前仓库
- 原因是：
  - `B`
    容易把
    `still_need_evidence`
    偷换成
    “先做出来再说”
  - `C`
    则能把两件事同时冻结：
    - 允许
      `A股 P0`
      进入最小原型闭环
    - 但不允许把当前金融限制包装成
      已可信

## 五、为什么不选 A

- 当前不选：
  - `A`
- 原因是：
  - 当前 `FV gate evidence packet v0`
    已冻结为：
    - `cross_window_return_sign_majority_positive__active_sign_majority_negative__still_need_evidence`
  - 若此时直接把
    `相对基准不系统性跑输`
    设为进入原型的硬门禁，
    会把
    `A股 P0`
    长期卡在产品原型阶段之前
  - 这会让：
    - `提醒 -> 解释 -> 记录 -> 回看`
      这个最小闭环
      无法验证
- 当前项目终局是：
  - `可解释投资决策辅助驾驶舱`
  而不是：
  - 先证明稳定战胜基准，
    才允许有产品原型

## 六、为什么不直接选 B

- 当前也不直接选：
  - `B`
- 原因是：
  - `B`
    虽然能最快允许原型启动，
    但太容易把：
    - 产品原型许可
    - 金融可信度
    混写
  - 在当前
    `net active_total_return`
    仍是：
    - `1 正 2 负`
    的前提下，
    若没有额外门禁页，
    很容易在 UI、解释卡或后续规划里，
    逐步滑成：
    - “虽然没明说，但系统其实已经可信”

## 七、主负责人最终裁决

- 当前正式选择：
  - `C`
    即：
    - 条件式门禁
- 当前最准确口径是：
  - 不把
    `相对基准不系统性跑输`
    写成进入
    `A股 P0`
    原型的绝对硬门禁
  - 但也不允许在没有正式披露与停止规则的前提下，
    直接放行第一版原型
- 当前因此要先冻结一页：
  - `A股 P0`
    原型阶段金融披露与条件式门禁页

## 八、当前先做什么

- 当前先做：
  - 创建：
    - `A5_A股P0原型阶段金融披露与条件式门禁页__20260719.md`
- 当前该页必须写清：
  - 原型阶段可以做什么
  - 原型阶段不能暗示什么
  - 当前金融状态仍然是：
    - `still_need_evidence`
  - 哪些风险披露必须强制存在
  - 何时才允许移除这些披露

## 九、暂缓什么

- 当前暂缓：
  - 第四窗
  - 同窗微调
  - 新 runner
  - 更宽成本带
  - 持有规则扩网格
  - 新 evidence type
  - UI 细节实现
  - 本地 AI 技术选型
  - 多市场并行落地

## 十、继续保留的 NEED_EVIDENCE

- 当前继续保留：
  - `financial-valid`
  - `output_passed`
  - `strict_out_of_time_generalization`
  - `full_impact_model`
  - `robustness_suite`
  - `cross_window_metric_alignment`
  - `A股 P0`
    第一版金融门禁应绑定：
    - `CSI300`
    - 还是
      `选定基准`

## 十一、一句话裁决

- 当前最准确的一句话是：
  - `A股 P0`
    第一版不采用“相对基准不系统性跑输”的绝对硬门禁，
    但必须先以
    `条件式门禁`
    冻结一页正式披露与停止规则，
    才允许进入最小原型闭环。
