# A5 五条代表指标第一轮映射汇总页

更新时间：2026-07-25

## 一、这页用途

- 本页只做：
  - 把五条代表指标的第一轮映射结果收成一页统一汇总
  - 给当前主线形成一个可回看的阶段性停点

- 本页不做：
  - 代替各单页细节
  - 金融有效性证明
  - 回测方案

## 二、当前统一前提

- 当前第一手只借：
  - `NFTRADEZ`
    的
    `风险约束语言`
  - `Smile_SMC`
    的
    `explain / review 结构`
    与
    `失效样式命名`

- 当前不把：
  - `订单流`
    升成主线正式对象

- 当前五条代表指标第一轮映射已经补齐：
  1. `沪深涨跌停`
  2. `打板资金`
  3. `上榜资金`
  4. `HYDB行业对比`
  5. `ZSDB指数对比`

## 三、五条代表指标第一轮总表

| 指标 | 当前主职责 | 第一高频失效 | 第二高频失效 | 当前优先处理 |
| --- | --- | --- | --- | --- |
| `沪深涨跌停` | `方向层 / 过滤层` | `背景背离型` | `强度衰减型` | `先降级职责 / 回观察位` |
| `打板资金` | `触发层 / 过滤层` | `波动失控型` | `事件退潮型` | `先降权 / 先降级职责` |
| `上榜资金` | `触发层 / 过滤层` | `事件退潮型` | `强度衰减型` | `先降级职责 / 先降权` |
| `HYDB行业对比` | `方向层 / 过滤层` | `背景背离型` | `结构破坏型` | `先降级职责 / 直接退出` |
| `ZSDB指数对比` | `方向层 / 过滤层` | `背景背离型` | `波动失控型` | `先降权 / 回观察位` |

## 四、当前已经收敛出的结构

### 4.1 两组明显分层已经出现

- 当前已经能看出两组：
  1. `打板资金 / 上榜资金`
     更偏：
     - `触发层`
     - `事件 / 波动`
     - `先降权 / 退一层职责`
  2. `HYDB / ZSDB / 沪深涨跌停`
     更偏：
     - `方向层`
     - `背景 / 结构 / 宽度`
     - `退回过滤 / 回观察位 / 直接退出解释`

### 4.2 当前最值钱的统一语言已经成型

- 当前已经能统一用：
  - `逻辑未坏，但当前先降权`
  - `当前先退一层职责，不再继续当触发依据`
  - `背景不再支持原解释权重`
  - `当前噪音过大，先回观察位`
  - `原来的前提已经不在了，这不是弱一点，而是逻辑失效`

- 这意味着：
  - 当前讨论已经从
    `抽象觉得像`
    往
    `可复用解释语言`
    迈了一步

## 五、当前还没做什么

- 当前还没做：
  1. 把这五条映射写成金融有效性结论
  2. 把这些语言写成交易门禁
  3. 开参数化回测
  4. 引入
     `TK`
     第一手补位
  5. 把
     `订单流`
     升成正式对象

## 六、当前 still_need_evidence

- 当前继续保留
  `still_need_evidence`
  的问题有：
  1. 五条指标的映射边界，
     后续是否仍需更细分
  2. 当前这轮语言层映射，
     是否已经足够稳住下一轮门槛复核
  3. 是否仍存在
     `微观成交结构 / 细粒度失效解释`
     的持续空位

## 七、主负责人裁决

- 当前正式裁决为：
  1. 五条代表指标第一轮映射，
     已经形成阶段性闭环
  2. 当前先不扩对象池
  3. 当前先不把
     `订单流`
     升成正式对象
  4. 当前下一手，
     更适合开：
     `五条代表指标映射后的门槛复核`
     或
     `映射页与现有矩阵/失效页的统一回接`
     其中之一

## 八、一句话口径

- 当前五条代表指标第一轮映射已经收成一页统一汇总：
  触发型对象优先处理
  `降权 / 降级职责`
  ，
  背景型对象优先处理
  `退回过滤 / 回观察位 / 退出原解释`
  ；
  当前主线已经比上一轮更接近可进入下一步。

## 回链

- `A5_五条代表指标映射草稿页_沪深涨跌停__20260725.md`
- `A5_五条代表指标映射草稿页_打板资金__20260725.md`
- `A5_五条代表指标映射草稿页_上榜资金__20260725.md`
- `A5_五条代表指标映射草稿页_HYDB行业对比__20260725.md`
- `A5_五条代表指标映射草稿页_ZSDB指数对比__20260725.md`
