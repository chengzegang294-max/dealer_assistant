# A5 A股 P0 技术指标产品层组合可行性第一轮 多家AI回收记录与主负责人裁决

更新时间：2026-07-24

## 一、这页用途

- 本页只做：
  - 吸收本轮多家 AI 回包
  - 补一轮实现向审校意见
  - 给出主负责人最终裁决
- 本页不做：
  - 前端实现
  - 金融真值判断
  - 回测

## 二、当前任务

- 当前任务是：
  - 判断
    `A股 P0`
    技术指标产品层当前这
    `5`
    条代表指标，
    是否应该压成更稳定的
    `产品语义组`
  - 若压组，
    进一步判断这些组
    更适合挂在哪个页面

## 三、回包摘要

### 1. `GPT`

- 推荐：
  - `3 组`
- 核心口径：
  - `沪深涨跌停`
    单独成组
  - `打板资金 + 上榜资金`
    构成
    `资金活跃`
  - `HYDB + ZSDB`
    构成
    `行业 / 指数参照`
- 价值：
  - 认为这最符合
    `首页看提醒 / 标的页看解释 / 问答页看补全`

### 2. `DeepSeek`

- 推荐：
  - `3 组`
- 核心口径：
  - 更偏
    `市场宽度 / 资金温度 / 个股位置`
    的分法
- 价值：
  - 强调语义边界清晰，
    同时提醒：
    `资金温度`
    若写成强弱判断，
    需要
    `NEED_EVIDENCE`

### 3. `Kimi`

- 推荐：
  - `3 组`
- 核心口径：
  - 更偏
    `市场温度 / 资金热度 / 相对位置`
- 价值：
  - 强调首页必须继续极简，
    不允许重新滑成指标拼盘

### 4. `GLM`

- 推荐：
  - `2 组`
    的事件触发型组织
- 核心口径：
  - `市场基准背景组`
  - `资金异动解释组`
- 价值：
  - 提醒当前分组不能脱离
    `事件解释闭环`
- 保留问题：
  - 把
    `HYDB`
    并入
    `资金异动解释组`
    与当前背景解释合同不一致

### 5. `Qwen`

- 推荐：
  - `2 组`
- 问题：
  - 把
    `HYDB`
    与
    `沪深涨跌停`
    拼成首页
    `市场概览组`
    的依据较弱
  - 与当前页面分工和合同贴合度不足
- 当前处理：
  - 只作低信号参考，
    不作为主裁依据

## 四、实现向审校结论

- 实现向审校当前推荐：
  - 仍选
    `3 组`
  - 但命名应更保守，
    更贴当前代码与合同
- 当前更贴实现边界的命名为：
  1. `市场宽度`
     - `沪深涨跌停`
  2. `资金活跃`
     - `打板资金`
     - `上榜资金`
  3. `行业 / 指数参照`
     - `HYDB行业对比`
     - `ZSDB指数对比`

### 4.1 为什么不是 `2 组`

- 当前代码和合同，
  仍以
  `sourceCard`
  为底层锚点
- 若先压成
  `2 组`
  大词，
  容易出现：
  - 文档层已经是大组
  - 代码层仍是单指标
  的双口径并存

### 4.2 为什么不直接采纳 `GLM` 的事件触发型分组

- 当前共享 helper
  已明确把：
  - `HYDB / ZSDB`
    放在
    `背景解释`
    角色里
- 因此把
  `HYDB`
  并入
  `资金异动解释组`
  会和当前合同冲突

## 五、真正共识是什么

- 当前高信号共识不是：
  - 哪个组名最好听
- 当前真正共识是：
  1. 不应继续保持
     `5`
     条逐条露出
  2. 当前应该压组，
     以免主路径重新滑回指标拼盘
  3. 首页必须继续克制，
     不能长成多组并列的大看板
  4. 问答页仍应保留
     `sourceCard`
     级释义，
     不能直接跳成按大组问答
  5. 任何涉及：
     - 强弱
     - 热度
     - 趋势判断
     的表达，
     若越出产品说明，
     都应写
     `NEED_EVIDENCE`

## 六、分歧只剩在哪里

- 当前真正分歧只剩：
  1. 是压成
     `2 组`
     还是
     `3 组`
  2. `ZSDB`
     应不应该和
     `沪深涨跌停`
     一起放进首页主组
  3. 首页是否允许同时露出两个以上主组

## 七、主负责人裁决

- 当前我选：
  - `3 组`
    方向
  - 但采用更贴当前实现边界的保守命名
- 当前正式裁决为：
  1. `市场宽度`
     - `沪深涨跌停`
  2. `资金活跃`
     - `打板资金`
     - `上榜资金`
  3. `行业 / 指数参照`
     - `HYDB行业对比`
     - `ZSDB指数对比`

## 八、为什么这样选

- 选这个的原因：
  1. 最符合当前已经冻结的
     `首页 -> 标的页 -> 问答页`
     页面分工
  2. 最不容易让首页重新滑成：
     - 指标大拼盘
     - 行情终端
  3. 比
     `2 组`
     更不容易把语义压得过粗
  4. 比
     `情绪 / 温度 / 个股位置`
     这类更重命名，
     更贴当前代码里
     `sourceCard + 背景解释`
     的组织方式

## 九、页面边界裁决

- 当前页面边界正式冻结为：

### 9.1 首页

- 首页当前不新增独立多组入口
- 首页只允许：
  - `市场宽度`
    作为最轻背景说明候选
- `资金活跃`
  当前继续通过事件语义源露出，
  不开首页独立主组
- `行业 / 指数参照`
  当前不做首页主组

### 9.2 标的页

- 标的页当前主承接：
  - `资金活跃`
  - `行业 / 指数参照`
- 其中：
  - `行业 / 指数参照`
    继续保持解释骨架角色

### 9.3 问答页

- 问答页当前继续保留：
  - `sourceCard`
    级逐条释义
- 当前不改成：
  - 按大语义组直接回答

## 十、为什么不选另外几条

### 10.1 不选 `2 组`

- 因为：
  - 当前压得太粗
  - 容易把
    `沪深涨跌停`
    与
    `打板 / 上榜资金`
    之间仍然不同的用户心智压扁

### 10.2 不选 `GLM` 式事件触发型分组

- 因为：
  - 它提醒了
    `事件解释闭环`
    这个价值，
    但具体成员划分
    与当前背景解释合同冲突

### 10.3 不选 `Qwen` 的首页概览组

- 因为：
  - `HYDB`
    与
    `沪深涨跌停`
    直接并到首页一组，
    依据不足
  - 与当前首页主轴不贴

## 十一、当前先做什么

- 当前先做的唯一一刀是：
  - 落
    `A5_A股P0技术指标产品层组合可行性第一轮_语义组定义与页面边界说明__20260724.md`
- 只写死：
  - `3`
    个语义组成员表
  - 首页 / 标的页 / 问答页挂载边界
  - 禁止把
    `资金活跃 / 市场宽度`
    写成交易信号

## 十一点五、当前后续执行回填

- 当前在裁决页之后，
  `Trae`
  已继续落下：
  - `A5_A股P0技术指标产品层组合可行性第一轮_实现前合同页__20260724.md`
- 当前代码侧已继续落下：
  - `src/features/indicator/utils/semanticGroups.ts`
  - `src/features/indicator/utils/semanticGroups.test.ts`
- 当前已继续把：
  - `contextPriority.ts`
    改为直接吃共享语义组定义
- 当前验证已通过：
  - `semanticGroups.test.ts`
  - `contextPriority.test.ts`
  - `npm run build`

## 十二、暂缓什么

- 暂缓：
  - 前端直接改成按组展示
  - 问答页按组重写问题
  - 强弱 / 热度定性词扩张
  - 金融有效性判断
  - 回测

## 十三、一句话口径

- 当前最准确的一句话是：
  - `A股 P0`
    技术指标产品层组合可行性第一轮，
    正式选
    `3 组方向 + 保守命名`
    ：
    `市场宽度 / 资金活跃 / 行业-指数参照`；
    当前先冻结语义组定义与页面边界，
    不直接跳到金融可行性。
