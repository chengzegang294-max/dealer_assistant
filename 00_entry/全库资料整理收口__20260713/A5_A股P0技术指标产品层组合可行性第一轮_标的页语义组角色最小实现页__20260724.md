# A5 A股 P0 技术指标产品层组合可行性第一轮 标的页语义组角色最小实现页

更新时间：2026-07-24

## 一、这页用途

- 本页只做：
  - 冻结标的页侧
    `资金活跃 / 行业-指数参照`
    的最小角色实现
- 本页不做：
  - 标的页重排
  - 新增分组卡片
  - 解释正文重写

## 二、当前主线

- 当前主线是：
  - `A股 P0 技术指标产品层组合可行性第一轮`
- 当前不是：
  - 金融真值层
  - 组合回测
  - 标的页重做

## 三、当前最值钱的一步

- 当前最值钱的一步是：
  - 让标的页当前解释区
    不只展示
    `tag`
  - 还明确告诉用户：
    - 哪部分是解释骨架
    - 哪部分是事件说明补强

## 四、当前正式裁决

- 标的页当前只允许：
  - `行业 / 指数参照`
    写成：
    - 解释骨架
  - `资金活跃`
    写成：
    - 事件说明补强
- 当前不允许写成：
  - 买卖依据
  - 强弱门禁
  - 通过条件

## 五、实现合同

- 当前只改：
  - 标的页解释区
    `技术指标背景`
    里的角色说明短句
- 并同步补齐上下文可见性：
  - 标的页相关事件列表在直连标的事件外，
    额外带入该事件前置的 `3` 条市场上下文事件，
    使 `资金活跃`（打板/上榜资金）可在标的页侧被选中并触发角色说明
- 当前输出统一为：
  - 若当前有
    `行业 / 指数参照`
    tag：
    - `当前解释骨架：行业 / 指数参照`
  - 若当前有
    `资金活跃`
    tag：
    - `当前事件说明补强：资金活跃`
- 若同时存在：
  - 先写解释骨架
  - 再写事件说明补强

## 六、停止规则

- 当前做到以下条件后立刻停：
  1. 标的页角色说明短句已落进解释区
  2. 顺序固定为：
     - 解释骨架在前
     - 事件说明补强在后
  3. hook / adapter / 页面层至少各有一条断言
     能证明：
     - `资金活跃`
       已可被选中并触发角色说明
  4. 测试与 build 通过

## 七、当前后续执行回填

- 当前在本页之后，
  `Trae`
  已继续把标的页解释区补成：
  - `当前解释骨架：行业 / 指数参照`
- 并补上顺序护栏：
  - 若同时存在两个角色，固定输出为：
    1. `当前解释骨架：行业 / 指数参照`
    2. `当前事件说明补强：资金活跃`
- 并把相关事件上下文补齐为：
  - 直连标的事件 + 事件前置 `3` 条市场上下文事件
  - 使 `资金活跃`（打板/上榜资金）能在标的页侧进入可选相关事件并触发角色说明
- 当前代码侧已落到：
  - `src/features/stock/adapters/stockPageViewModel.ts`
  - `src/features/stock/adapters/stockPageViewModel.test.ts`
  - `src/features/stock/hooks/useStockPage.ts`
  - `src/pages/Stock.tsx`
  - `src/features/stock/hooks/useStockPage.test.tsx`
  - `src/pages/Stock.test.tsx`
- 当前验证已通过：
  - `stockPageViewModel.test.ts`
  - `useStockPage.test.tsx`
  - `Stock.test.tsx`
  - `semanticGroups.test.ts`
  - `contextPriority.test.ts`
  - `npm run build`

## 八、一句话口径

- 当前最准确的一句话是：
  - 标的页当前先把
    `行业 / 指数参照`
    和
    `资金活跃`
    收成解释区里的角色说明，
    不改页面结构。
