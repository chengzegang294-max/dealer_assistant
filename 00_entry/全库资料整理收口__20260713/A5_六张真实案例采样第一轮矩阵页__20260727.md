# A5 六张真实案例采样第一轮矩阵页

更新时间：2026-07-27

## 一、这页用途

- 本页只做：
  - 把六张正式案例卡压成第一轮矩阵
  - 让后续回看时可以直接看：
    - 哪组背景
    - 配哪类触发
    - 当前动作先落在哪一档

## 二、当前第一轮矩阵

| 双背景组合 | `上榜资金` | `打板资金` |
| --- | --- | --- |
| `HYDB行业对比 + 沪深涨跌停` | `A5_REAL_CASE_001` `背景先坏_触发后退` `先降级职责` | `A5_REAL_CASE_006` `行业背离_宽度退潮_触发退回观察` `先降权 / 必要时回观察位` |
| `ZSDB指数对比 + 沪深涨跌停` | `A5_REAL_CASE_005` `指数转弱_宽度退潮_榜单降级再降权` `先降级职责 / 再降权` | `A5_REAL_CASE_002` `指数转弱_宽度走坏_触发先降权` `先降权` |
| `HYDB行业对比 + ZSDB指数对比` | `A5_REAL_CASE_004` `双背景走弱_榜单先退一层职责` `先降级职责` | `A5_REAL_CASE_003` `双背景背离_触发退回观察` `先降权 / 必要时回观察位` |

## 三、当前从矩阵里能直接看出的规律

- 当前从矩阵里能直接看出：
  1. `上榜资金`
     更容易先走：
     - `先降级职责`
     - 再视情况 `降权`
  2. `打板资金`
     更容易先走：
     - `先降权`
     - 污染加重时 `回观察位`
  3. 三组双背景组合，
     当前都已经形成了完整对照

## 四、当前矩阵的用途

- 当前矩阵后续可直接用于：
  1. 回看不同双背景组合下，
     两类触发对象的默认动作差异
  2. 判断下一轮若扩案例，
     是要补新背景组合，
     还是补精确时刻层

## 五、一句话口径

- 当前六张真实案例卡已经不是散页，
  而是正式压成了一张
  `双背景组合 x 触发对象`
  的第一轮矩阵。
