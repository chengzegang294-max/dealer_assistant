# A5 信息直播间房间状态总表

更新时间：2026-08-06

## 一、这页用途

- 冻结第一批重点房间的：
  - 当前页导出状态
  - 增量导出状态
  - 分型
  - 下一手
- 作为阶段 A 收口入口，不讨论指标组合、不扩新源。

## 二、验收口径（统一）

### 当前页导出通过

1. `export_version` 为仓库最新（当前：`live_info_current_page_export_v1.4`）
2. `room_anchor` 正确（含说话人别名映射）
3. `visible_messages` 非空，且与当前屏幕可见卡片一致
4. 倒序观看下：`display_date / display_time / excerpt` 取**当前可见里时间最新**一条
5. 不含 UI 壳：`历史记录 / 搜索直播间消息 / 选择日期 / 倒序观看历史记录`

### 增量导出通过

1. 视觉滚动真实发生（不是只改外层 `scrollTop`）
2. 滚动后能稳定并入新消息且去重
3. 到顶 / 无新消息可正常收口
4. checkpoint 可续跑

## 三、房间状态总表

| 房间 | 分型 | 当前页导出 | 增量导出 | 证据/备注 | 下一手 |
|------|------|------------|----------|-----------|--------|
| 格兰投研 | 标准文字卡片（`cu-card`）；侧栏易误选 `k神` | **内容通过；房间名旧证误为k神（已修 v1.4）** | **抽检通过（到尽）** | 当前页 `182544`：正文均为 `#格兰投研#`，但 `room_anchor=k神`。增量链：`182738`(`manual_limit`/228) → `182931`(`scroll_end`/248) → `183901`(`not_scrollable`/1050，`end_scroll_top=0`，区间约 `07/07`～`08/05`)。证据已落入 batch_05/格兰投研。脚本已升：当前页 `v1.4` / 增量 `v1.2`（`#房间名#` 优先于侧栏） | **抽检#1完成**；进女王前可用 v1.4 秒确认当前页房间名（可选） |
| 天机 | 广告文案覆盖型（曾）→ 现作模板 | 已通过（模板） | **待轻量复核** | 旧增量：`174742.json` 已是 `scroll_end`/507 条，`room_anchor=天机短线试更新` | **抽检#3：v1.1 轻量复核** |
| 龙头交易猿 | 说话人前缀分离型（`陈子瞻`→房间名） | 已通过（模板） | **待轻量复核** | 旧增量：`065556.json` 已是 `scroll_end`/117 条，房间名正确 | **抽检#4：v1.1 轻量复核** |
| 周期女王 | 标准文字卡片 | 已通过（模板） | **待抽检（旧证 `scroll_stuck`）** | 旧增量：`054021.json` 为 `scroll_stuck`/221 条；状态表原“已确认到顶”与证据不一致，以证据为准重抽。**注意：20260806_153333/153634 实为「浮光」，不是女王** | **抽检#2：仍待；用 v1.4+v1.2 进女王房重跑** |
| 浮光 | 标准文字卡片（非本轮抽检序；顺带吸收） | **已通过（v1.4）** | **已通过（`scroll_end`）** | 当前页 `153333`：`room_anchor=浮光`，`topic_anchor=#浮光#`，倒序最新=`13:40:06`，可见 3 条。增量 `153634`：`v1.2`，`reset_checkpoint=true`/`max_rounds=80`，`rounds=40`，`3691→0`，251 条，区间约 `07/07`～`08/06`。正文有人提「女王的农场」，但房间锚点是浮光 | 不占抽检序；证据已落入 batch_05/浮光 |
| k神（锚点可疑） | 侧栏误选型（同格兰旧坑） | **内容可用；锚点可疑** | **`scroll_end`（251）已吸收** | `155043`/`155438`：脚本写 `room_anchor=k神`，但截图侧栏高亮为**知行合一**；正文与浮光批次几乎同文（`13:40:06` 等）。增量 `3733→40`、`rounds=39`。已落入 batch_05/k神（按 JSON 原名） | **不算周期女王**；进女王前先核对侧栏房间名 |
| 孙悟空金牌 | 说话人前缀分离型（`至尊宝`→房间名）；增量滚动容器敏感（已收口） | **已通过** | **已通过（到顶）** | 当前页：`180422`/`181145`；增量：`181233`→`182013`（`scroll_end`，626 条） | 免抽；保留模板 |

## 四、孙悟空金牌单点结论（2026-08-06）

### 已通过（当前页 + 增量）

- 当前页：`v1.3`，`room_anchor=孙悟空金牌`，倒序最新=`14:35:03` 传智教育
- 增量：先 `manual_limit@30`（227 条）再续跑 → `stop_reason=scroll_end`（626 条）
- 视觉滚动成立；checkpoint 续跑成立；区间约 `2026/07/07`～`2026/08/05`
- 关键脚本修复仍有效：
  1. UI 噪声不再误杀长正文
  2. 优先 `cu-card`
  3. 倒序取最新可见
  4. `至尊宝` → `孙悟空金牌`

### runtime 证据落点

- 已吸收到：
  - `02_runtime/info_live_room_sampling/batch_05__20260803_20260805_priority_room_history_rerun/00_raw/priority_rooms/孙悟空金牌/`
  - `info_live_export__20260805_180422.json`
  - `info_live_export__20260805_181145.json`
  - `info_live_incremental_export__20260805_181233.json`
  - `info_live_incremental_export__20260805_182013.json`

## 五、分型清单（冻结）

1. **标准文字卡片型**：右栏 `cu-card` + 时间标题 + 正文
2. **说话人前缀分离型**：正文以主讲名开头，房间名靠别名映射
3. **增量滚动容器敏感型**：当前页可过，增量依赖真实滚动节点
4. **广告文案覆盖型**：正文可能被固定广告句盖住（天机历史问题）
5. **图片为主型**：暂不进文字主链

## 六、固定执行口令

### 当前页导出

1. 登录态、单标签、进入目标房间历史记录
2. 选日期后等待 2～3 秒，期间不滚动、不切房
3. 跑 `live_info_current_page_export_v1.js`（仓库最新）
4. 对照本节「当前页导出通过」五条

### 增量导出（已冻结口令）

1. 先跑当前页导出（增量依赖 `__runInfoLiveCurrentPageExportV1`）
2. 倒序首测建议：
   ```js
   window.__infoLiveIncrementalExportV1Options = {
     reset_checkpoint: true,
     scroll_direction: "up",
     start_position: "keep",
     max_rounds: 30
   };
   ```
3. 未到顶且 `stop_reason=manual_limit` 时续跑：
   ```js
   window.__infoLiveIncrementalExportV1Options = {
     reset_checkpoint: false,
     scroll_direction: "up",
     max_rounds: 200
   };
   ```
4. 通过门槛：`stop_reason=scroll_end` + 视觉真实滚动 + checkpoint 可续

## 七、证据落点（统一）

- 活跃运行时批次：
  - [batch_05__20260803_20260805_priority_room_history_rerun](file:///d:/Stock/trading_assistant/02_runtime/info_live_room_sampling/batch_05__20260803_20260805_priority_room_history_rerun)
- 房间证据根目录：
  - [00_raw/priority_rooms](file:///d:/Stock/trading_assistant/02_runtime/info_live_room_sampling/batch_05__20260803_20260805_priority_room_history_rerun/00_raw/priority_rooms)
- 当前状态表只认这个批次下的重点房间证据作为第一落点。

## 八、主负责人 / Cursor 协作口径

- 当前主线：阶段 A 收尾 = 其他模板房增量抽检
- 执行页：
  - `A5_信息直播间阶段A收尾_其他房增量抽检执行页__20260806.md`
- 下一手唯一顺序：
  1. ~~格兰投研续跑到顶~~（已完成：`183901` 到尽）
  2. 周期女王重跑纠 `scroll_stuck`（用当前页 `v1.4` + 增量 `v1.2`；勿与「浮光」混淆）
  3. 天机轻量复核
  4. 龙头交易猿轻量复核

## 九、一句话口径

- 格兰已到尽；浮光顺带吸收且 `scroll_end` 通过；下一手仍是进**周期女王**房重跑纠 stuck（刚贴的 153333/153634 不是女王）。
