# A5 外部多因子视频线索核验与 TA01 字段映射合同

更新时间：2026-08-31
状态：`TA03_EXPLANATION_PATTERNS_RETAINED_NO_TESTABLE_FACTOR_READY`

## 1. 任务定位

本页只冻结一件事：

1. 外部多因子教学视频里，哪些内容可以保留为 `TA-01` 后续可复核的解释线索或候选字段线索。

本页明确不做：

1. 把视频升级成策略验证结果
2. 把作者观点升级成当前事实字段
3. 激活 `L4/L5` 自动预填
4. 输出股票推荐、仓位建议或交易指导
5. 直接重开多因子回测

## 2. 来源身份结论

本轮正式登记 1 条来源：

1. `VID-001`
2. URL：`https://www.bilibili.com/video/BV184oVBhE51/`
3. 标题：`多因子选股模型从零构建：因子筛选、有效性检验及权重配置详解`
4. UP 主：`我是王者quant`
5. 发布时间：`2026-05-01 10:49:00`
6. 当前可访问状态：`ACCESSIBLE_PAGE_METADATA_ONLY`
7. 内容取得方式：`PLATFORM_METADATA_ONLY`

固定口径：

1. 本轮回链到的是页面元数据和旧仓本地草案。
2. 本轮没有回收到可复核的本地音频实体或完整转写实体。
3. 因此本来源不得抬高为 `DIRECT_VIDEO_AND_TRANSCRIPT`。

旧仓完整证据包固定留在：

`D:\Stock\trading_assistant\90_SCRATCH_AND_TEST_ZONE\batch_oldrepo_cleanup__20260811\`

## 3. 可保留的解释模式

本轮可正式保留的，不是“哪条因子已经验证”，而是以下解释模式：

1. 因子研究应先分因子家族，再做预处理，再做评价，再讨论组合。
2. 预处理环节至少要显式检查去极值、标准化、行业中性化和市值中性化。
3. 评价环节可以保留 `IC / IR / 分层回测 / 相关性去冗余` 作为研究 checklist。
4. 视频点名的 `PE / PB / 营收增长 / 净利润增长 / ROE / 换手率` 仅保留为现有正式字段的解释补充，不新增正式字段定义。

固定边界：

1. 上述内容只代表研究流程和解释框架。
2. 这些描述不是当前项目的正式公式、正式参数或正式验收阈值。

## 4. 可测试候选清单

本轮没有形成合格的 `KEEP_AS_TESTABLE_CANDIDATE`。

因此当前正式清单固定为：

1. `testable_candidate_count = 0`
2. 不进入 `TA-04｜首个因子候选离线验证预注册`

## 5. 延期候选与字段映射

本轮只保留 3 个延期候选，供后续按 `field/source/gap` 预注册重开：

1. `quality_gross_margin`
   - `field`: `NEW_FIELD_CANDIDATE`
   - `source`: 需要新的财务 statement 来源
   - `gap`: 需要补 `data_available_at`、缺失值规则与字段合同
2. `price_return_momentum`
   - `field`: `NEW_FIELD_CANDIDATE`
   - `source`: 需要新的全市场价格历史来源
   - `gap`: 需要补窗口、方向、调仓频率和防未来函数合同
3. `volatility_factor`
   - `field`: 当前不冻结为正式字段
   - `source`: 依赖价格历史来源
   - `gap`: 需要补波动定义、历史窗口和评价协议

固定口径：

1. 没有稳定 `source_id` 前，不得把这些候选抬成当前可实现因子。
2. 没有稳定公式和参数前，不得进入离线验证。

## 6. 当前禁止推断

本轮正式禁止：

1. 把视频观点写成 `TA-01` 事实字段
2. 把 `IC / IR` 教学描述写成正式阈值
3. 把 `OLS / 随机森林 / XGBoost` 写成当前批准路线
4. 把 `等权 / IC 加权 / 风险平价 / 优化权重` 写成当前权重合同
5. 把动量、换手、波动率写成当前短线工作台自动预填因子
6. 形成当前建议、仓位建议或进场点
7. 推断用户画像

## 7. 固定结论与下一阶段

本页固定结论：

`TA03_EXPLANATION_PATTERNS_RETAINED_NO_TESTABLE_FACTOR_READY`

原因：

1. 来源身份已冻结，但证据等级只到页面元数据与本地草案层。
2. 原子 Claim、候选因子和 `field/source/gap` 映射已经闭合。
3. 黑箱组合与作者观点已经隔离。
4. 但当前没有任何候选同时满足公式明确、参数明确、数据入口明确和可预注册验证。

因此下一阶段最小启动点固定为：

`TA-04｜TA-01 现有事实字段离线可读点评样稿`
