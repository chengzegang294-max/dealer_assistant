# A5 G5 主链闭合状态页

更新时间：2026-07-18

## 用途

- 把 `G5` 当前五段主链的状态集中收口到一页。
- 这页不新增实现内容。
- 这页只回答：
  - `G5` 现在到底推进到哪
  - 哪几段已经从纯阻塞推进到可判断层
  - 哪几段仍未闭合

## 当前结论

- `G5` 当前已经不是：
  - `全链纯阻塞`
- `G5` 当前已经推进到：
  - `主链全段可读`
  - `状态分层明确`
  - `最终输出仍未闭合`
- 当前最准确口径应写成：
  - `G5 chain defined end-to-end__not implementation ready`

## 一、五段主链状态总览

| 链段 | 字段 | 当前状态 | 当前能写到哪 | 还不能写成什么 |
|---|---|---|---|---|
| 1 | `alpha_score` | `contract_frozen_proxy` | 正式代理合同已冻结 | 唯一正式主源已闭合 |
| 2 | `covariance_model_id` | `ready_judgement_conditional__downstream_still_locked` | 已完成 ready 级正式判断但下游仍锁定 | 风险模型已 ready |
| 3 | `target_weight` | `verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed` | 已完成降级风险约束下的升级判断推进，且已吸收上游最小集成验证 | 已有正式优化器输出 |
| 4 | `portfolio_tracking_error` | `pass_conditions_frozen__not_output_passed` | 已完成单点升级判断并冻结通过条件 | 已可正式输出 |
| 5 | `adjusted_position_weight` | `pass_conditions_frozen__not_output_passed` | 已完成单点升级判断并冻结通过条件 | 最终权重已可直接运行 |

## 二、当前主链阅读顺序

- 当前最顺阅读顺序是：
  - `A5_alpha_score_正式代理合同页__20260716.md`
  - `A5_covariance_model_id_实现前口径判断页__20260716.md`
  - `A5_target_weight_最小生成链判断页__20260716.md`
  - `A5_portfolio_tracking_error_风险输出链判断页__20260716.md`
  - `A5_adjusted_position_weight_回溯闭合判断页__20260716.md`

## 三、当前已完成的推进含义

- 当前已经完成的不是：
  - 组合层实现
- 当前已经完成的是：
  - 把 `G5` 从“全靠口头描述的阻塞链”推进到“每段都有独立判断页”
- 这意味着当前已经能清楚区分：
  - 哪些是输入合同
  - 哪些是风险模型判断
  - 哪些是生成链判断
  - 哪些是风险输出链判断
  - 哪些是最终回溯链判断

## 四、当前仍未通过的点

- 当前仍未通过：
  - `G5` 总门槛
- 原因不是：
  - 不知道链条长什么样
- 原因是：
  - 这些链条虽然都已定义，但都还没有进入正式输出闭合

## 五、当前允许写法

- 允许写成：
  - `G5 主链已全段定义`
  - `G5 当前已具备端到端可读链`
  - `G5 当前仍停在判断层，不是实现层`

## 六、禁止误写

- 禁止写成：
  - `G5 已闭合`
  - `G5 已 implementation ready`
  - `A5 组合层已可直接运行`
- 禁止把：
  - 五段判断页
  写成：
  - 已完成实现或已形成正式输出

## 七、主负责人裁决

- 当前阶段不再需要：
  - 回头扩资料
- 当前最顺收口是：
  - 把 `G5` 主链状态固定
  - 继续维持 `G6` 冻结
- 当前若继续往下推，下一类动作应是：
  - 对三个输出段发起单点多AI讨论
  - 裁定“输出通过条件”应如何冻结
- 但在没有新证据前：
  - 不直接升格任何字段为 ready

## 七点五、当前输出闭合判断结果

- 已落：
  - `A5_G5_输出闭合判断页__20260716.md`
- 当前含义：
  - `G5` 当前不是链条问题
  - 当前是输出闭合问题
- 当前第一优先对象：
  - `target_weight`
- 原因：
  - 它同时卡住：
    - `adjusted_position_weight`
    - `portfolio_tracking_error`
- 当前新进展：
  - `target_weight` 已继续顺推到：
    - `pass_conditions_frozen__not_output_passed`
  - 且已新增：
    - `升级为 output_passed 仍需哪些证据`
  - `portfolio_tracking_error` 已继续顺推到：
    - `pass_conditions_frozen__not_output_passed`
  - `adjusted_position_weight` 已继续顺推到：
    - `pass_conditions_frozen__not_output_passed`
  - 且三段已统一收进：
    - `A5_G5_输出升格证据总表__20260716.tsv`
  - 且三段升格顺序已正式裁决为：
    - `target_weight -> portfolio_tracking_error -> adjusted_position_weight`
  - 且第一手执行物已启动：
    - `A5_target_weight_升格证据补齐页__20260716.md`
  - 且当前已补齐：
    - `target_weight` 升格裁决三件套
  - 且第一轮回包已正式判定为：
    - `方向一致，但票面不足`
  - 且第二轮更窄回包已正式吸收并裁决为：
    - `no`
  - 且当前最小剩余缺口已冻结为：
    - `explicit validation run`
  - 且 `validation run` runtime 工作线已落：
    - `02_runtime/a5_g5_target_weight_validation/`
  - 且 success / failure 两类 template-level smoke-run 已完成：
    - `tw_validation_success_latest.json`
    - `tw_validation_failure_latest.json`
  - 且 real-input success / failure case validation smoke-run 已完成：
    - `tw_real_input_success_latest.json`
    - `tw_real_input_failure_latest.json`
  - 且 actual generation success / failure execution 已完成：
    - `tw_actual_generation_success_latest.json`
    - `tw_actual_generation_failure_latest.json`
  - 且重开升格裁决多AI回包已正式吸收并裁决为：
    - `yes`
  - 且 `actual_generation后` 升级判断首轮回包已正式吸收并判定：
    - `旧题残留严重，票面不足`
  - 且第二轮更窄追问稿已备好：
    - `A5_target_weight_升格裁决__actual_generation后_第二轮更窄追问稿__20260716.md`
  - 且第二轮更窄回包已正式吸收并裁决为：
    - `conditional`
  - 且唯一附加条件已冻结为：
    - `A5_degraded_risk_handling_充分性与稳健边界页__20260716.md`
  - 且唯一附加条件已进一步压成：
    - `A5_degraded_risk_handling_边界验证清单页__20260716.md`
  - 且主负责人书面验收页已完成：
    - `A5_degraded_risk_handling_主负责人书面验收页__20260716.md`
  - 且升级判断重开准备页已完成：
    - `A5_target_weight_升级判断重开准备页__20260716.md`
  - 且升级判断重开三件套已备好：
    - `A5_target_weight_升级判断重开_多AI前情提要与裁决框架__20260716.md`
    - `A5_target_weight_升级判断重开_多家AI正式发包稿__20260716.md`
    - `A5_target_weight_升级判断重开_多家AI回收记录模板__20260716.md`
  - 且升级判断重开回包已正式吸收并裁决为：
    - `verified_with_degraded_risk__not_output_passed`
  - 且 verified 后续升级判断回包已正式吸收并裁决为：
    - `verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed`
  - 且当前下一手已切到：
    - `portfolio_tracking_error` 上游口径同步与下一轮判断准备收缩
  - 且 `portfolio_tracking_error` 已继续新增：
    - `A5_portfolio_tracking_error_升级判断准备页__20260716.md`
    - `A5_portfolio_tracking_error_benchmark风险输出最小正式口径页__20260716.md`
    - `A5_portfolio_tracking_error_covariance最小输入层页__20260716.md`
    - `A5_portfolio_tracking_error_降级风险口径可审计样例页__20260716.md`
    - `A5_portfolio_tracking_error_升级判断_多AI前情提要与裁决框架__20260716.md`
    - `A5_portfolio_tracking_error_升级判断_多家AI正式发包稿__20260716.md`
    - `A5_portfolio_tracking_error_升级判断_多家AI回收记录模板__20260716.md`
    - `A5_portfolio_tracking_error_升级判断_多家AI回收记录与主负责人裁决__20260716.md`
- `A5_portfolio_tracking_error_frozen后续升级判断_首轮混合回包与主负责人裁决__20260717.md`
- `A5_portfolio_tracking_error_frozen后续升级判断准备页__20260717.md`
- `A5_portfolio_tracking_error_frozen后续升级判断_多AI前情提要与裁决框架__20260717.md`
- `A5_portfolio_tracking_error_frozen后续升级判断_多家AI正式发包稿__20260717.md`
- `A5_portfolio_tracking_error_frozen后续升级判断_多家AI回收记录模板__20260717.md`
  - 且 `portfolio_tracking_error` 当前已正式推进到：
    - `pass_conditions_frozen__not_output_passed`
  - 且其 frozen 后续升级判断首轮混合回包已正式吸收并裁决为：
    - `票面不足，状态不变`
  - 且其 frozen 后续升级判断第二轮回包已正式吸收并裁决为：
    - `零有效票，状态不变`
  - 且其 frozen 后续升级判断超窄纯文本回包已正式吸收并裁决为：
    - `no`
  - 且 `covariance_model_id downstream_still_locked` 后续判断超窄纯文本回包已正式吸收并裁决为：
    - `conditional`
  - 且其唯一剩余锁定原因回包已正式吸收并裁决为：
    - `下游单段仍未形成可解除 not_output_passed 的正式边界`
  - 且 `target_weight` 解除 `not_output_passed` 正式边界回包已正式吸收并裁决为：
    - `yes`
  - 且其最稳正式边界已冻结为：
    - `显式验证运行记录 + 失败路径一致性`
  - 且 `portfolio_tracking_error` 解除 `not_output_passed` 正式边界回包已正式吸收并裁决为：
    - `yes`
  - 且其最稳正式边界已冻结为：
    - `success 样例显式风险输出 + failure 样例 abort_reason 回链一致性`
  - 且该阶段当时的下一手曾切到：
    - `adjusted_position_weight 解除 not_output_passed 正式边界裁决`
  - 且 `adjusted_position_weight` 已继续新增：
    - `A5_adjusted_position_weight_升级判断准备页__20260716.md`
    - `A5_adjusted_position_weight_final_size_scalar降级样例页__20260716.md`
    - `A5_adjusted_position_weight_最终融合failure样例页__20260716.md`
    - `A5_adjusted_position_weight_升级判断_多AI前情提要与裁决框架__20260716.md`
    - `A5_adjusted_position_weight_升级判断_多家AI正式发包稿__20260716.md`
    - `A5_adjusted_position_weight_升级判断_多家AI回收记录模板__20260716.md`
    - `A5_adjusted_position_weight_升级判断_多家AI回收记录与主负责人裁决__20260716.md`
  - 且 `adjusted_position_weight` 当前已正式推进到：
    - `pass_conditions_frozen__not_output_passed`
  - 且当前下一手已切到：
    - `covariance_model_id 总瓶颈判断准备`
  - 且 `covariance_model_id` 已继续新增：
    - `A5_covariance_model_id_总瓶颈判断准备页__20260716.md`
    - `A5_covariance_model_id_总瓶颈判断_多AI前情提要与裁决框架__20260716.md`
    - `A5_covariance_model_id_总瓶颈判断_多家AI正式发包稿__20260716.md`
    - `A5_covariance_model_id_总瓶颈判断_多家AI回收记录模板__20260716.md`
    - `A5_covariance_model_id_总瓶颈判断_多家AI回收记录与主负责人裁决__20260716.md`
    - `A5_covariance_model_id_候选模型家族冻结页__20260716.md`
    - `A5_covariance_model_id_本体实跑最小准备页__20260716.md`
    - `A5_covariance_model_id_本体实跑输入装配页__20260716.md`
    - `A5_covariance_model_id_first_fresh_run入口准备页__20260716.md`
    - `A5_covariance_model_id_first_fresh_run前检查页__20260716.md`
    - `A5_covariance_model_id_first_fresh_run执行页__20260716.md`
    - `A5_covariance_model_id_最小稳定性检查准备页__20260716.md`
    - `A5_covariance_model_id_最小稳定性检查执行页__20260716.md`
    - `A5_covariance_model_id_唯一模型收敛准备页__20260716.md`
    - `A5_covariance_model_id_唯一模型收敛主负责人裁决页__20260716.md`
    - `A5_covariance_model_id_唯一模型定稿准备页__20260716.md`
    - `A5_covariance_model_id_ready判断_多家AI回收记录与主负责人裁决__20260716.md`
    - `A5_covariance_model_id_最小集成验证准备页__20260716.md`
    - `A5_covariance_model_id_最小集成验证执行页__20260717.md`
  - 且 `covariance_model_id` 当前已正式推进到：
    - `ready_judgement_conditional__downstream_still_locked`
  - 且当前已完成：
    - `first fresh-run preflight 已通过`
    - `benchmark_relative_sample_covariance` 的 current / adjacent 两窗口 fresh-run
    - `20 x 20` 协方差矩阵 latest
    - `最小稳定性检查 = passed`
    - `唯一活动实现候选 = benchmark_relative_sample_covariance`
    - `唯一模型最小合同已冻结`
  - 且当前已完成：
    - `ready 判断多AI回包吸收`
    - `下游仍锁定` 禁止性条款写入正式页
    - `最小集成验证执行 = passed`
  - 且当前下一手已切到：
    - `target_weight not_output_passed 剩余缺口收缩`
  - 且 `target_weight` 在当前轮已继续新增：
    - `A5_target_weight_verified_with_degraded_risk后续升级判断准备页__20260717.md`
    - `A5_target_weight_verified_with_degraded_risk后续升级判断_多AI前情提要与裁决框架__20260717.md`
    - `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI正式发包稿__20260717.md`
    - `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI回收记录模板__20260717.md`
    - `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI回收记录与主负责人裁决__20260717.md`
  - 且当前下一手已进一步压成：
    - `target_weight verified_with_degraded_risk 后续升级判断回包等待`

## 八、2026-07-18 执行验证主线复位

- 本轮主线已从：
  - `投票 / 裁决 / 填表优先`
  切回：
  - `runtime 执行验证优先`
- 本轮已实际复跑并确认可复现的入口先有两条：
  - `02_runtime/a5_g5_covariance_bodyrun/`
  - `02_runtime/a5_g5_target_weight_validation/`
- 且本轮已继续新增两条下游 runtime 入口：
  - `02_runtime/a5_g5_portfolio_tracking_error_validation/`
  - `02_runtime/a5_g5_adjusted_position_weight_validation/`
- 本轮新增执行确认：
  - `covariance first fresh-run` 再次执行成功
  - `covariance minimum stability check` 当前 latest 结果保持 `passed`
  - `target_weight actual generation` success / failure 两条路径再次执行成功
- 本轮新增下游执行确认：
  - `portfolio_tracking_error actual generation` success / failure 两条路径已执行成功
  - `adjusted_position_weight actual generation` success / failure 两条路径已执行成功
- 本轮不做的事：
  - 不因复跑成功而升级任何正式状态名
  - 不继续优先起新的多AI发包或回收模板
- 当前新增判断是：
  - `G5` 三个输出段当前都已有最小 runtime 工作线
  - 当前主问题已从“有没有入口”收缩到“是否继续补更强执行证据”
- 因此当前最顺下一手应切到：
  - 沿现有 runtime 线补更强执行证据
  - 或开始判断哪一段最值得先从最小执行推进到更强执行

## 九、2026-07-18 串联执行新进展

- 本轮新增的不是状态名升格，而是执行链增强：
  - `portfolio_tracking_error` success 已实际消费 `covariance_matrix_latest.csv`
  - `adjusted_position_weight` success 已实际消费
    - `tw_actual_generation_success_latest.json`
    - `pte_actual_generation_success_latest.json`
- 这意味着当前 `G5` 三个输出段已从：
  - `各自可跑`
  继续推进到：
  - `已开始形成上游产物被下游真实消费的串联执行`
- 当前仍不能写成：
  - `G5 output_passed`
  - `G5 implementation ready`

## 十、2026-07-18 最小链级执行

- 本轮已新增：
  - `02_runtime/a5_g5_min_chain_validation/`
- 本轮已实际完成：
  - `A5_G5_MIN_CHAIN_SUCCESS`
  - `A5_G5_MIN_CHAIN_PTE_FAILURE`
  - `A5_G5_MIN_CHAIN_APW_FAILURE`
- 当前 success 链已确认：
  - `target_weight -> portfolio_tracking_error -> adjusted_position_weight`
    可顺序执行到最后一段
- 当前 `pte_failure` 链已确认：
  - 最先停在：
    - `portfolio_tracking_error`
  - 原因为：
    - `missing_covariance_matrix_csv`
- 当前 `apw_failure` 链已确认：
  - 最先停在：
    - `adjusted_position_weight`
  - 原因为：
    - `final_size_scalar_below_abort_threshold`
- 当前新增判断是：
  - `G5` 已从“分段可跑”继续推进到“最小链级可跑且双 failure 停点已合同化”
- 这仍不能写成：
  - `output_passed`
  - `implementation ready`
  - 且该轮回包已正式吸收并裁决为：
    - `verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed`
  - 且当前下一手已切到：
    - `portfolio_tracking_error` 上游口径同步与下一轮判断准备收缩

## 十点五、2026-07-18 same-batch 串联继续下探到第三段

- 本轮又新增：
  - `02_runtime/a5_g5_adjusted_position_weight_validation/run_covariance_target_weight_pte_apw_same_batch_v1.py`
- 本轮已实际完成：
  - `covariance -> target_weight -> portfolio_tracking_error -> adjusted_position_weight`
    的 same-batch success
  - 同轮 `apw formula failure`
- 当前新增含义：
  - 第三段当前消费的不是旧的静态 `pte` success 样例
  - 而是本轮 fresh 的 `pte_same_batch_success_latest.json`
  - 因而 `G5` 当前已不只是“最小链能跑到第三段”，
    还具备“同轮 fresh 上下游产物被连续消费”的更强执行证据
- 这仍不能写成：
  - `output_passed`
  - `implementation ready`
  - `all_downstream_ready`

## 十点六、2026-07-18 三段 frozen 边界 runtime audit

- 本轮又新增：
  - `02_runtime/a5_g5_min_chain_validation/run_g5_same_batch_boundary_audit_v1.py`
- 本轮 audit 已实际确认：
  - `target_weight_boundary.runtime_backed = true`
  - `portfolio_tracking_error_boundary.runtime_backed = true`
  - `adjusted_position_weight_boundary.runtime_backed = true`
  - `all_segment_boundaries_runtime_backed = true`
- 当前新增含义：
  - `covariance_model_id downstream_still_locked`
    所指向的下游单段 formal boundary，
    当前已全部具备 same-batch hard 证据支撑
- 这仍不能写成：
  - `covariance_model_id ready`
  - `G5 output_passed`
  - `implementation ready`

## 十点七、2026-07-18 阶段切换到 FV gate

- 本轮又新增：
  - `A5_Cursor_Trae_FV_gate协同分区与交接页__20260718.md`
  - `A5_execution_validation到financial_validity_gate阶段切换页__20260718.md`
- 当前阶段切换已正式写成：
  - `execution-validation` 作为优先主线，当前允许阶段性结束
  - 当前下一手切到：
    - `financial validity gate`
- 这意味着当前应覆盖旧残迹写法：
  - `adjusted_position_weight 解除 not_output_passed 正式边界裁决仍待回包`
  - `继续优先补同构 same-batch 证明`
- 当前同时已盘点：
  - `02_runtime` 下尚未存在
    - `financial_validity`
    - `backtest`
    为主名的 `A5 -> G5` 独立最短入口
- 因此当前最准确下一手是：
  - 先开 `FV gate` 最小入口与最小通过标准
- 这仍不能写成：
  - `financial-valid`
  - `output_passed`

## 十点八、2026-07-19 FV gate v0 最小入口已冻结

- 本轮又新增：
  - `A5_financial_validity_gate最小入口与通过标准页__20260719.md`
- 当前新增含义：
  - `FV gate v0` 第一手已正式冻结为：
    - `contract-first minimal backtest entry`
  - `holdout` 已被写死为：
    - 回测内规则
  - 成本口径当前已被写死为：
    - `degraded_fixed_cost`
- 当前同时已盘点到：
  - `same-batch APW` 产物已存在
  - 历史 OHLCV、benchmark series、returns / active returns 输入已存在
- 本轮又已创建：
  - `02_runtime/a5_g5_financial_validity_gate_v0/README.md`
- 因此当前下一手已从：
  - “先定义 `FV gate` 最小入口与最小通过标准”
  推进到：
  - 以现有输入继续创建：
    - 最小 runtime params 模板
    - 最小成绩单模板
    - 第一手 minimal backtest runner
- 这仍不能写成：
  - `financial-valid`
  - `output_passed`

## 十点九、2026-07-19 FV gate v0 首轮执行结果

- 本轮又新增：
  - `02_runtime/a5_g5_financial_validity_gate_v0/`
  - `fv_gate_v0_scorecard_latest.json`
- 当前新增含义：
  - `G5` 已不再只是“阶段切换已成立”
  - 而是已经有：
    - `FV gate v0` 第一手真实成绩单
- 当前首轮成绩单显示：
  - gross / net 总收益为负
  - holdout net 收益为负
  - 相对 benchmark 的 active return 为负
- 因此当前最准确下一手不是：
  - 回退否定层 1-3 工程链
  - 回到旧投票主线
- 当前最准确下一手是：
  - 留在 `FV gate`
  - 调整最小信号组合 / 评价卡
  - 继续产出下一轮最小金融证据
- 当前又新增：
  - `A5_FV_gate_v0_最小信号组合卡__20260719.md`
  - `A5_FV_gate_v0_最小评价卡__20260719.md`
  - `fv_gate_v0_runtime_params_round2_template_v1.json`
- 本轮又新增真实对照：
  - `fv_gate_v0_round2_scorecard_latest.json`
  - `fv_gate_v0_round2_breakout_scorecard_latest.json`
- 当前进一步收窄为：
  - `trend_pullback_confirmation_v1` 优于首轮 proxy 与 breakout 对照
  - 但当前仍属于：
    - `improved_but_still_negative`
- 因此当前下一手继续是：
  - 留在 `FV gate`
  - 细化 A 候选风险暴露收缩 / 排序逻辑

## 十点十、2026-07-19 第三轮 risk-lite 继续改善

- 本轮又新增：
  - `fv_gate_v0_round3_risklite_scorecard_latest.json`
- 本轮先发生的失败是：
  - 更严过滤层触发 `non_positive_alpha_sum`
- 本轮随后完成的修正是：
  - 保留 A 候选原始排序
  - 仅下调 `final_size_scalar` 到 `0.6`
- 当前新增含义：
  - 第三轮 risk-lite 比第二轮 A 候选继续改善
  - 当前更值钱的改进杠杆是：
    - 风险暴露收缩
  - 当前不值得优先继续压：
    - 更严过滤层

## 十点十一、2026-07-19 第四轮 rank-decay + risk-lite 收口

- 本轮又新增：
  - `fv_gate_v0_round4_rankdecay_risklite_scorecard_latest.json`
- 当前新增含义：
  - 在 A 主候选不变的前提下，
    `弱名次降权 + 更轻仓位`
    比第三轮 risk-lite 继续改善
- 当前最准确写法是：
  - `current_best_minimal_fv_gate_v0_contract_frozen`
- 这仍不是：
  - `financial-valid`
  - `output_passed`
- 当前下一手若继续，只应落在：
  - 风险暴露收缩
  - 排序逻辑细化

## 十点十二、2026-07-19 v1 sample boundary 相邻窗复验

- 本轮又新增：
  - `A5_FV_gate_v1_sample_boundary阶段页__20260719.md`
  - `fv_gate_v1_sample_boundary_scorecard_latest.json`
- 当前新增含义：
  - `current_best` 冻结合同已在相邻窗完成复验
  - 且对照结果显示：
    - `net total_return` 由负转正
    - `holdout net total_return` 由负转正
    - `net active_total_return` 由大负转正
    - `max_drawdown` 继续收敛
- 当前最准确写法是：
  - `sample_boundary_reproduced__still_need_evidence`
- 这仍不是：
  - `financial-valid`
  - `output_passed`
- 当前下一手因此改写为：
  - 不再继续同窗微调
  - 不再继续相邻窗重复复跑
  - 由 `Cursor` 统筹是否进入：
    - `new evidence type` 子阶段

## 十点十三、2026-07-19 cost_sensitivity_v0 小成本带

- 本轮又新增：
  - `A5_Cursor仓库熟悉度验收与new_evidence_type统筹页__20260719.md`
  - `A5_FV_gate_new_evidence_type_cost_sensitivity_v0阶段页__20260719.md`
  - `fv_gate_cost_sensitivity_v0_band_summary_latest.json`
- 当前新增含义：
  - `Cursor` 已通过仓库熟悉度验收
  - 且已正式拍板首个 `new evidence type = cost_sensitivity_v0`
  - 在 `5 / 15 / 25 bps`
    小成本带内，
    当前正向结果没有塌掉
- 当前最准确标签是：
  - `cost_band_stable__still_need_evidence`
- 这仍不是：
  - `financial-valid`
  - `output_passed`
- 当前下一手因此改写为：
  - 不再继续同类成本带扩线
  - 由 `Cursor` 统筹是否进入：
    - 第二个 `new evidence type` 子阶段

## 十点十四、2026-07-19 holding_rule_v0 最小持有规则对照

- 本轮又新增：
  - `A5_FV_gate_new_evidence_type_holding_rule_v0阶段页__20260719.md`
  - `fv_gate_holding_rule_v0_summary_latest.json`
  - `fv_gate_holding_rule_v0_fixed_period_rebalance_v0_20d_scorecard_latest.json`
- 当前新增含义：
  - 第二个 `new evidence type`
    已正式从讨论进入实跑
  - 在 `v1 sample boundary + 15bps`
    冻结合同下，
    `20 trade days`
    定期再平衡版本仍保持：
    - `net total_return > 0`
    - `holdout net total_return > 0`
    - `net active_total_return > 0`
  - 相比静态基线，
    收益略收缩但未翻负，
    回撤略收敛
- 当前最准确标签是：
  - `holding_rule_stable__still_need_evidence`
- 这仍不是：
  - `financial-valid`
  - `output_passed`
- 当前下一手因此改写为：
  - 不再继续默认扩 holding rule 网格
  - 再次交回 `Cursor`
    统筹是否进入第三类 `new evidence type`

## 十点十五、2026-07-19 window_consistency_v0 跨窗符号对照

- 本轮又新增：
  - `A5_FV_gate_window_consistency_v0阶段页__20260719.md`
  - `fv_gate_window_consistency_v0_summary_latest.json`
- 当前新增含义：
  - `sample_boundary_reproduced`
    这条正向新证据仍然成立
  - 但同一冻结合同在：
    - `v0 current_best window`
    - `v1 adjacent window`
    上出现了：
    - 收益主字段符号分歧
  - 这意味着当前最诚实的跨窗结论不是：
    - `cross_window_stable`
  - 而是：
    - `cross_window_sign_divergence__still_need_evidence`
- 这仍不是：
  - `financial-valid`
  - `output_passed`
- 当前下一手因此改写为：
  - 不再默认进入第三类 `new evidence type`
  - 已完成：
    - `FV_gate_v2_third_window`
      实跑
  - 当前三窗正式标签为：
    - `cross_window_return_sign_majority_positive__active_sign_majority_negative__still_need_evidence`
- 当前已按 `Cursor`
  裁决正式落地：
  - `A5_FV_gate_active_underperformance_v0解释层页__20260719.md`
  - `fv_gate_active_underperformance_v0_summary_latest.json`
- 当前不再继续默认抓第四窗
- 当前主线可先停在：
  - `still_need_evidence`
- 后续若再推进，
  再由 `Cursor`
  统筹是否切回新的
  `new evidence type`

## 八、一句话口径

- 当前 `G5` 的正确写法是：
  - `chain_defined_end_to_end__outputs_not_closed`
- 其中当前最靠前的新增收口是：
  - `covariance_model_id` 已完成最小集成验证执行，
    但仍保持：
    - `ready_judgement_conditional__downstream_still_locked`
  - `target_weight` 当前主线已切回：
    - `verified_with_degraded_risk` 之后的后续升级判断
  - 且当前已正式前推到：
    - `verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed`
  - `target_weight` 已进入“通过条件已冻结 + 升级证据待补齐”状态
  - `三段输出的升格证据已集中成表`
  - `三段输出的升格顺序已正式拍板`
  - `target_weight` 的第一手升格证据补齐已启动
  - `target_weight` 已完成第二轮窄裁决收口
  - 当前新增执行证据已足以支撑重开升格裁决
  - 重开升格裁决资格已正式获得
  - `actual_generation后` 升级判断第二轮已正式裁成 `conditional`
  - 下一手已不再是继续问模型，而是补边界验证清单
  - 唯一附加条件已补到可判断层
  - 当前已完成升级判断重开并正式推进到：
    - `verified_with_degraded_risk__not_output_passed`
  - 且 verified 后续升级判断已继续正式推进到：
    - `verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed`
  - `portfolio_tracking_error` 已继续补齐：
    - `covariance_model_id 最小输入层`
    - `降级风险口径可审计样例`
  - `portfolio_tracking_error` 已继续备好：
    - `单点升级判断多AI三件套`
  - `portfolio_tracking_error` 已吸收单点升级判断回包并正式推进到：
    - `pass_conditions_frozen__not_output_passed`
  - 但其 frozen 后续升级判断首轮混合回包正式裁决为：
    - `票面不足，继续维持 frozen`
  - 且其 frozen 后续升级判断第二轮回包正式裁决为：
    - `零有效票，继续维持 frozen`
  - 且其 frozen 后续升级判断超窄纯文本回包正式裁决为：
    - `no`
  - 当前下一手已切到：
    - `covariance_model_id downstream_still_locked` 后续判断
  - 且该轮超窄纯文本回包已正式裁为：
    - `conditional`
  - 且其唯一附加条件已冻结为：
    - `唯一剩余锁定原因需收敛为单条正式边界`
  - 且该唯一剩余锁定原因当前已正式冻结为：
    - `下游单段仍未形成可解除 not_output_passed 的正式边界`
  - 且 `target_weight` 的解除 `not_output_passed` 正式边界当前已正式冻结为：
    - `显式验证运行记录 + 失败路径一致性`
  - 且 `portfolio_tracking_error` 的解除 `not_output_passed` 正式边界当前已正式冻结为：
    - `success 样例显式风险输出 + failure 样例 abort_reason 回链一致性`
  - 且 `adjusted_position_weight` 的解除 `not_output_passed` 正式边界当前已由 same-batch runtime 事实补裁冻结为：
    - `success 样例显式 adjusted_position_weight = target_weight * final_size_scalar + failure 样例 abort_reason / degrade_flags 回链一致性`
  - `adjusted_position_weight` 已继续补齐：
    - `final_size_scalar 降级样例`
    - `最终融合 failure 样例`
  - 且 `same_batch boundary audit` 最新已再次复跑通过，并继续保持：
    - `all_segment_boundaries_runtime_backed = true`
    - `covariance_lock_reason_now_has_runtime_backing = true`
  - `adjusted_position_weight` 已正式推进到：
    - `pass_conditions_frozen__not_output_passed`
  - 当前三段输出的“解除边界是否缺最短可复跑证据”这一问已收口为：
    - `no_longer_missing_shortest_rerunnable_runtime_evidence`
  - 当前仍保持：
    - 不写 `output_passed`
    - 不写 `covariance_model_id_ready`

## 回链

- `A5_实现阻塞项拆解表__20260716.tsv`
- `A5_实现前冻结门槛页__20260716.md`
- `A5_alpha_score_正式代理合同页__20260716.md`
- `A5_covariance_model_id_实现前口径判断页__20260716.md`
- `A5_target_weight_最小生成链判断页__20260716.md`
- `A5_target_weight_输出闭合判断页__20260716.md`
- `A5_target_weight_最小通过条件页__20260716.md`
- `A5_target_weight_通过后仍需证据清单页__20260716.md`
- `A5_portfolio_tracking_error_风险输出链判断页__20260716.md`
- `A5_portfolio_tracking_error_输出闭合判断页__20260716.md`
- `A5_portfolio_tracking_error_最小通过条件页__20260716.md`
- `A5_adjusted_position_weight_回溯闭合判断页__20260716.md`
- `A5_adjusted_position_weight_输出闭合判断页__20260716.md`
- `A5_adjusted_position_weight_最小通过条件页__20260716.md`
- `A5_G5_输出升格证据总表__20260716.tsv`
- `A5_G5_输出升格顺序裁决页__20260716.md`
- `A5_target_weight_升格证据补齐页__20260716.md`
- `A5_target_weight_升格裁决_多AI前情提要与裁决框架__20260716.md`
- `A5_target_weight_升格裁决_多家AI正式发包稿__20260716.md`
- `A5_target_weight_升格裁决_多家AI回收记录模板__20260716.md`
- `A5_target_weight_升格裁决_多家AI回收记录与主负责人裁决__20260716.md`
- `A5_target_weight_升格裁决_第二轮更窄追问稿__20260716.md`
- `A5_target_weight_最小缺口补齐页__20260716.md`
- `A5_target_weight_verified_with_degraded_risk后续升级判断准备页__20260717.md`
- `A5_target_weight_verified_with_degraded_risk后续升级判断_多AI前情提要与裁决框架__20260717.md`
- `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI正式发包稿__20260717.md`
- `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI回收记录模板__20260717.md`
- `A5_target_weight_verified_with_degraded_risk后续升级判断_多家AI回收记录与主负责人裁决__20260717.md`
- `A5_G5_输出闭合判断页__20260716.md`
- `A5_G5_输出通过条件_多家AI回收记录与主负责人裁决__20260716.md`
- `A5_G5_输出通过条件_多AI前情提要与裁决框架__20260716.md`
- `A5_G5_输出通过条件_多家AI正式发包稿__20260716.md`
- `A5_G5_输出通过条件_多家AI回收记录模板__20260716.md`
- `A5_covariance_model_id_最小集成验证执行页__20260717.md`
- `A5_adjusted_position_weight解除not_output_passed正式边界_运行事实补裁页__20260723.md`
