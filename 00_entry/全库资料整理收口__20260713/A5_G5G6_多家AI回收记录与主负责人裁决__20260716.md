# A5 G5_G6 多家AI回收记录与主负责人裁决

更新时间：2026-07-16

## 临时材料吸收记录

- 临时路径：
  - `d:\Stock\dealer_assistant\暂时存放\粘贴区.md`
- 材料类型：
  - `A5_G5_G6` 这轮多家AI回包临时粘贴汇总
- 是否值得吸收：
  - `yes`
- 正式去向：
  - `00_entry/全库资料整理收口__20260713/A5_G5G6_多家AI回收记录与主负责人裁决__20260716.md`
- 是否允许继续留在暂时存放：
  - `no`
- 删除条件：
  - 本页、README 与门槛页回链完成后，可删除原临时文件

## TASK

- 吸收 `A5_G5_G6` 这轮多家AI回收结果，并把不同模型口径归一化为：
  - 当前最推荐的工作模式
  - `G5` 的清障顺序
  - `G6` 是否继续冻结
  - 主负责人最终裁决

## 当前结论先行

- 本轮真正共识不是：
  - `A5 已可进入实现前准备`
- 本轮真正共识是：
  - 当前最稳模式应为：
    - `方案2：分阶段解锁型`
  - 当前最顺主线应为：
    - 先冻结 `alpha_score` 正式代理合同
    - 再讨论 `covariance_model_id` 是否进入实现前口径
    - `target_weight / portfolio_tracking_error` 暂不前推
    - `G6` 继续冻结

## 一、各模型归一化回收

### 1) GPT

- 阶段判断：
  - 明确认为当前仍停在合同层，不应直接迈入实现前准备
- 工作模式归一化：
  - 主模式：`方案2：分阶段解锁型`
  - 次选：`方案1：保守冻结型`
- `G5` 清障顺序：
  - `alpha_score`
  - `covariance_model_id`
  - `target_weight / portfolio_tracking_error`
- `G6` 判断：
  - `继续冻结`
- 关键价值：
  - 把“先冻结 alpha 合同，再讨论协方差入口”的顺序钉死
- NEED_EVIDENCE：
  - `alpha_score` 是否存在唯一稳定主源
  - `covariance_model_id` 的最小实现前口径

### 2) GLM

- 阶段判断：
  - 明确认为 `G5` 是首要阻塞项，`G6` 默认继续冻结
- 工作模式归一化：
  - 主模式：`方案2：分阶段解锁型`
- `G5` 清障顺序：
  - 先冻结 `alpha_score` 代理合同
  - 再记录 `covariance / target_weight / tracking_error` 的实现前口径需求
- `G6` 判断：
  - `继续冻结`
- 关键价值：
  - 强调先把 `alpha_score` 写成显式 `proxy` 合同，而不是模糊代理量
- NEED_EVIDENCE：
  - 协方差模型正式可实现口径
  - `target_weight` 最小求解链

### 3) Kimi

- 阶段判断：
  - 明确支持“优先判断 `G5`，`G6` 暂时保持冻结”
- 工作模式归一化：
  - 主模式：`方案2：分阶段解锁型`
- `G5` 清障顺序：
  - `alpha_score` 正式代理合同冻结
  - `covariance_model_id` 实现前口径
  - `target_weight` 最小生成链定义
- `G6` 判断：
  - `继续冻结`
- 关键价值：
  - 给出了最清晰的“单缺口族连续推进”与停止规则
- NEED_EVIDENCE：
  - `covariance_model_id` 最小输入 schema
  - `target_weight` 的约束与求解边界

### 4) DeepSeek

- 阶段判断：
  - 明确认为 `G5` 与 `G6` 的最稳排序是：
    - `先 G5`
    - `后 G6`
- 工作模式归一化：
  - 主模式：`方案2：分阶段解锁型`
- `G5` 清障顺序：
  - 立即冻结 `alpha_score` 合同
  - 写死 `G6` 的 `daily_only`
  - 再更新阻塞项拆解表
- `G6` 判断：
  - `继续冻结`
- 关键价值：
  - 给出最明确的依赖链逻辑：
    - `alpha_score -> covariance -> target_weight -> tracking_error`
- NEED_EVIDENCE：
  - `alpha_score` 唯一主源是否存在
  - `minute formal raw catalog` 是否可用

### 5) Qwen

- 阶段判断：
  - `不纳入有效票`
- 原因：
  - 回答明显串回上一轮 `方案A` 的旧题面
  - 输出核心内容仍围绕：
    - `A_REQ_008 / A_REQ_010 / A_REQ_002`
    - `双轨80/20`
  - 没有对本轮 `G5 / G6` 发包边界形成有效回应
- 当前处理：
  - `作废票，不吸收入本轮归一化统计`

## 二、归一化投票结果

### 1. 有效票范围

- 有效：
  - `GPT / GLM / Kimi / DeepSeek`
- 无效：
  - `Qwen`

### 2. 工作模式归一化投票

- `方案2：分阶段解锁型`
  - 支持：
    - `GPT / GLM / Kimi / DeepSeek`
- `方案1：保守冻结型`
  - 次选：
    - `GPT`
- 结果：
  - `4/4 有效票` 支持 `方案2`

### 3. G5 清障顺序共识

- 共识第一步：
  - `alpha_score` 正式代理合同冻结
- 共识第二步：
  - `covariance_model_id` 是否进入实现前口径
- 共识第三步：
  - `target_weight / portfolio_tracking_error` 暂不前推

### 4. G6 判断共识

- 共识：
  - `继续冻结`
- 当前允许写法：
  - `daily_only`
  - `minute = sample_only_with_provenance`
  - `level2 = future_only`

## 三、分歧归纳

| 比较轴 | 是否分歧 | 分歧点 | 当前结论 |
|---|---|---|---|
| 工作模式 | `低分歧` | `GPT` 把方案1列为次选，其余均直推方案2 | 以 `方案2` 为正式主模式 |
| `G5` 第一手动作 | `无实质分歧` | 都把 `alpha_score` 放在第一顺位 | 先冻结 `alpha_score` 合同 |
| `G6` 是否继续冻结 | `无实质分歧` | 无 | 继续冻结 |
| `target_weight / tracking_error` 是否前推 | `无实质分歧` | 无 | 暂不前推 |
| `Qwen` 回包有效性 | `已裁决` | 串题 | 作废票 |

## 四、主负责人裁决

- 当前正式主模式定为：
  - `方案2：分阶段解锁型`
- 当前正式推进顺序定为：
  - 1. 先冻结 `alpha_score` 正式代理合同
  - 2. 再判断 `covariance_model_id` 是否进入实现前口径
  - 3. `target_weight / portfolio_tracking_error` 继续保留 `future_only`
  - 4. `G6` 继续冻结，不升格分钟与 `Level-2`

## 五、不选其它方案的原因

- 不选 `方案1：保守冻结型`
  - 太稳，容易把 `alpha_score` 长期挂在模糊代理态，导致整条 `G5` 链条继续悬空
- 不选 `方案3：平衡推进型`
  - 在 `covariance` 尚未形成实现前口径前，过早扩大讨论面，容易制造伪闭合
- 不选 `方案4：激进推进型`
  - 当前没有任何硬证据支持 `G6` 解冻，风险最高

## 六、当前先做什么

- 当前先做：
  - `A5_alpha_score_正式代理合同页__20260716.md`
- 当前同步回填：
  - `A5_实现前冻结门槛页__20260716.md`
  - `A5_实现阻塞项拆解表__20260716.tsv`
- 当前顺推：
  - `covariance_model_id` 实现前口径判断页
- 当前暂缓：
  - `target_weight` 最小生成链页
  - `portfolio_tracking_error` 风险模型链页

## 六点五、后续执行回填

- 以上裁决落地后，当前已继续顺推完成：
  - `A5_target_weight_最小生成链判断页__20260716.md`
  - `A5_portfolio_tracking_error_风险输出链判断页__20260716.md`
  - `A5_adjusted_position_weight_回溯闭合判断页__20260716.md`
  - `A5_G5主链闭合状态页__20260716.md`
- 当前含义：
  - 本页原始裁决没有被推翻
  - 而是已经被连续执行到 `G5` 主链总状态页

## 七、当前继续保留 NEED_EVIDENCE 的项

- `alpha_score` 是否存在唯一稳定主源
- `covariance_model_id` 的最小实现前口径
- `target_weight` 的最小求解链
- `minute formal raw catalog` 是否可用
- `Level-2` 权限与 fresh-run 实证是否闭合

## 回链

- `A5_G5G6_多家AI正式发包稿__20260716.md`
- `A5_G5G6_多家AI回收记录模板__20260716.md`
- `A5_实现前冻结门槛页__20260716.md`
- `A5_实现阻塞项拆解表__20260716.tsv`
- `A5_alpha_score_正式代理合同页__20260716.md`
- `A5_covariance_model_id_实现前口径判断页__20260716.md`
