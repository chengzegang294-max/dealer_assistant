# A5 A股 P0 首页工作台 Batch1 辅助纯模型护栏页

更新时间：2026-07-22

## 一、这页用途

- 本页只做：
  - 在三段核心状态迁移护栏之后，继续判断 `Batch1` 辅助纯模型是否还存在最短可补缺口
  - 将本轮范围限定为 `applyDecisionDraftChange(...)` 与 `buildSearchActionEcho(...)`
- 本页不做：
  - 重开页面结构讨论
  - 扩大到 hook / 页面 / 浏览器层测试
  - 新增功能

## 二、主负责人裁决

- 当前结论：
  - `applyDecisionDraftChange(...)` 虽属辅助链，但直接决定草稿字段修改是否始终跟随当前 `selectedEventId`
  - `buildSearchActionEcho(...)` 虽不属于主工作区核心状态迁移，但已是纯函数，测试成本极低
  - 因此这轮可顺手把两条辅助纯模型一起补齐，不再留下明显裸奔点
- 当前决定：
  - 继续只在 `workspaceModel.test.ts` 扩一小圈
  - 不触碰 React 层

## 三、为什么现在做这一刀

- 三段核心护栏已在：
  - 切事件
  - 提交路径
  - 重试回退
- 当前剩余未被明确锁住的纯函数里：
  - 草稿修改仍贴着主工作区编辑链
  - 搜索动作虽然边际价值更低，但能以极低成本补齐当前纯模型出口
- 这比回头做结构美化更值钱，也不触发新证据边界。

## 四、本轮允许范围

- 允许修改：
  - `workspaceModel.test.ts`
  - `README.md`
  - `Cursor 同步包`
- 如有必要，允许更新：
  - 本页自身
- 不允许借机扩展：
  - `useHomeWorkspace` 级测试
  - `EventStreamPanel` contract 收口
  - adapter / section 再美化

## 五、本轮最小通过条件

- 满足以下四项即可视为本轮收口：
  1. `workspaceModel.test.ts` 补 `applyDecisionDraftChange(...)` 最小断言
  2. `workspaceModel.test.ts` 补 `buildSearchActionEcho(...)` 最小断言
  3. `npm run test` 通过
  4. `README` 与 `Cursor 同步包` 已回填

## 六、本轮结果

- 当前 `workspaceModel.test.ts` 已补辅助纯模型护栏：
  - `applyDecisionDraftChange(...)`：
    - 修改字段时同步写入当前 `selectedEventId`
    - 只覆盖目标字段，不污染其他草稿字段
  - `buildSearchActionEcho(...)`：
    - 空输入时返回失败提示
    - 有效输入时裁剪空白并返回成功提示
- 当前 `npm run test` 已通过：
  - `1` 个测试文件
  - `11` 个测试用例
- 当前 `Batch1` 前台停点更新为：
  - `首页薄壳 + workspace 不变量 + section props + adapter 分层 + 核心三段/辅助两段最小自动化护栏`

## 七、当前明确不做

- 当前不要求：
  - 搜索动作接入真实跳转
  - 草稿编辑链的 hook 级全覆盖
  - 页面端输入框联动测试

## 八、下一手

- 先补：
  - 草稿修改与搜索动作的纯模型测试
- 再跑：
  - `npm run test`
- 通过后回填：
  - `README`
  - `Cursor 同步包`
