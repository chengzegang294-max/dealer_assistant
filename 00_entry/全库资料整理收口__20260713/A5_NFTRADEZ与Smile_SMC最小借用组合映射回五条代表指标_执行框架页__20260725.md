# A5 NFTRADEZ与Smile_SMC最小借用组合映射回五条代表指标 执行框架页

更新时间：2026-07-25

## 一、这页用途

- 本页只做：
  - 把当前已经冻结的最小借用组合，
    正式接回
    `五条代表指标`
    的下一步执行框架
  - 明确：
    先映射什么，
    怎么映射，
    什么暂时不映射

- 本页不做：
  - 直接替代五条指标逐条细化页
  - 直接做金融有效性判断
  - 直接开回测

## 二、当前前提

- 当前已经冻结的最小借用组合是：
  1. `NFTRADEZ`
     - 只借
       `风险约束语言`
  2. `Smile_SMC`
     - 只借
       `explain / review 结构`
       与
       `失效样式命名`

- 当前不进入第一手的是：
  - `TK`

- 当前已明确：
  - `订单流`
    在仓里有来源，
    但当前只记为
    `候补观察位`
    ，
    不直接升成主线正式对象

## 三、当前真正要做的映射

- 当前真正要做的不是：
  - 把外部体系概念复制进来

- 当前真正要做的是：
  - 把最小借用组合转译成
    当前主线能消费的三类抓手：
  1. `风险约束语言`
  2. `失效解释句式`
  3. `review 复盘骨架`

## 四、五条代表指标的映射清单

- 当前五条代表指标仍然是：
  1. `沪深涨跌停`
  2. `打板资金`
  3. `上榜资金`
  4. `HYDB`
  5. `ZSDB`

## 五、每条指标都先按同一张合同映射

- 每条指标后续都按以下字段映射：
  1. 当前主职责：
     - `方向层 / 触发层 / 过滤层 / 仓位层 / 退出层`
  2. 当前最常见失效方式：
     - 从已冻结
       `5`
       类失效方式中选
  3. 当前更适合套用哪类风险约束语言：
     - 来自
       `NFTRADEZ`
       的风险边界口径
  4. 当前更适合套用哪类 explain/review 骨架：
     - 来自
       `Smile_SMC`
       的解释层结构
  5. 当前应对应哪类处理动作：
     - `直接退出 / 先降权 / 先降级职责 / 回观察位`
  6. 当前 still_need_evidence：
     - 哪部分仍不能写成已证明有效

## 六、当前先映射顺序

- 当前推荐顺序是：
  1. `打板资金`
  2. `上榜资金`
  3. `HYDB`
  4. `ZSDB`
  5. `沪深涨跌停`

- 原因是：
  1. `打板资金 / 上榜资金`
     最容易暴露：
     - 触发层怎么失效
     - 何时该降权
     - 何时该回观察位
  2. `HYDB / ZSDB`
     更适合补：
     - 背景背离型
     - 强度衰减型
     的解释
  3. `沪深涨跌停`
     更像总背景热度与极端状态参照，
     放在最后更容易写稳

## 七、当前明确不映射什么

- 当前不映射：
  - 外部体系原始术语树
  - 订单流对象化
  - 盘口微结构
  - 回测参数
  - 执行规则

- 也就是说：
  - 当前只借
    `语言层`
    和
    `解释层`
  - 不借
    `策略本体`

## 八、当前第一手产物应该长什么样

- 当前第一手产物更适合是：
  - 一张
    `五条代表指标映射草稿页`

- 这张页至少应包含：
  1. 指标名
  2. 当前主职责
  3. 当前高频失效方式
  4. 当前风险约束语言模板
  5. 当前 review 句式模板
  6. 当前处理动作建议
  7. `still_need_evidence`

## 九、主负责人裁决

- 当前正式裁决为：
  1. 订单流当前先不对象化，
     只保留候补观察位
  2. 当前主线继续沿
     `NFTRADEZ + Smile_SMC`
     的最小借用组合推进
  3. 下一手正式执行物，
     应落成：
     `五条代表指标映射草稿页`
  4. 当前先按：
     - `打板资金`
     - `上榜资金`
     - `HYDB`
     - `ZSDB`
     - `沪深涨跌停`
     的顺序往下写

## 十、一句话口径

- 当前主线下一手不是补
  `订单流对象`
  ，
  而是把
  `NFTRADEZ`
  的风险约束语言
  和
  `Smile_SMC`
  的 explain-review 骨架，
  正式映射回五条代表指标。

## 回链

- `A5_订单流在当前主线中的位置_仓内有无与是否缺失判断页__20260725.md`
- `A5_技术指标仓内参考体系最小借用组合页__20260725.md`
- `A5_技术指标失效方式与波动风险约束第一轮内部分类页__20260725.md`
