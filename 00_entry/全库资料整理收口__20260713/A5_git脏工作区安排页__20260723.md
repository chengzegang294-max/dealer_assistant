# A5 git 脏工作区安排页

更新时间：2026-07-23

## 一、这页用途

- 本页只做：
  - 把当前仓库 `git` 脏工作区的处理方式压成正式安排
  - 明确“当前线允许提交什么、当前线外哪些先不卷入”
- 本页不做：
  - 全仓一把梭清理
  - `git add .`
  - 对历史线做未审先删

## 二、主负责人裁决

- 当前最合理的下一手已不是“只冻结分流口径”，而是把已确认成熟的正式包按目录级白名单吸收入库。
- 当前已完成：
  - `7a08fcde`：吸收 `00_entry/全库资料整理收口__20260713/` 正式裁决与治理文档包
  - `b69048ac`：吸收 `02_runtime/a5_g5_*` 运行时验证包
  - `c1ae0660`：吸收 `10_source_library_archive/batch_145~149` 成熟来源包
- 当前补充裁决：
  - `00_entry` 根层旧资料入口中，`全库资料整理总台账 / 参考交易系统补齐状态表 / 最后一轮资料收集拆包路径清单` 已由 `全库资料整理收口__20260713/` 承接
  - `临时资料纳入评估__NF1第一性原理交易体系__20260713.md` 保持临时评估卡定位，不再作为当前正式入口留在仓内根层
  - `20_tools_workspace/a5_p0_home_batch1_frontend` 中残留的 `问答下钻占位页 / Placeholder` 旧措辞已统一压回 `问答结果页`
  - `10_source_library_archive/archive_batch_index_v1.tsv` 中 `batch_145~149` 的状态名已统一从 `working` 收回 `ready`
- 当前执行策略固定为：
  - `KEEP_WHITELIST_ELSE_TEMP`
  - 允许按目录级成熟包做白名单提交
  - 历史 raw snapshot / latest 运行产物继续只分流、不混提

## 三、当前脏区现实

- 当前仓库脏区不是只剩前台线。
- 已确认至少同时分布在：
  - `00_entry/`
  - `02_runtime/`
  - `04_active_main_docs/`
  - `10_source_library_archive/`
  - `20_tools_workspace/`
- 因此：
  - 当前不能把 `git status` 里的全部 `M / ?? / D` 当作同一提交对象
  - 但也不能再把所有非前台路径一概视为“暂不处理”
- 当前新的分档已经固定为：
  - 可直接吸收：目录结构完整、README/执行卡/索引齐的成熟包
  - 可挂起保留：已有正式定位但当前不再续刀的运行包或维护包
  - 保留为历史噪音：`_raw_snapshot`、未入正式索引的 latest 产物、仍在演化中的他线运行物

## 四、本轮已完成的白名单吸收

- `00_entry`
  - 已吸收 `全库资料整理收口__20260713/` 正式文档包
  - 已把 `3` 个旧根层资料入口收回到该目录
  - 根层 `NF1` 临时评估卡已退出正式入口
- `02_runtime`
  - 已吸收 `a5_g5_min_chain_validation`
  - 已吸收 `a5_g5_target_weight_validation`
  - 已吸收 `a5_g5_portfolio_tracking_error_validation`
  - 已吸收 `a5_g5_adjusted_position_weight_validation`
  - 已吸收 `a5_g5_covariance_bodyrun`
  - 已吸收 `a5_g5_financial_validity_gate_v0`
- `10_source_library_archive`
  - 已吸收 `batch_145_public_batch9_n02_initial_balance_evidence_absorb__20260713`
  - 已吸收 `batch_146_public_batch9_n01_gainzalgo_agpro_evidence_absorb__20260713`
  - 已吸收 `batch_147_ytc_sample_intake_absorb__20260713`
  - 已吸收 `batch_148_chzl_bsd_sample_intake_absorb__20260713`
  - 已吸收 `batch_149_tdx_custom_terminal_external_folder_absorb__20260719`

## 五、当前线外分流口径

### 1. `02_runtime`

- 当前状态：
  - `a5_g5_*` 成熟运行包已完成目录级吸收
  - 仍可见少量继续演化中的其他运行线产物
- 当前处理：
  - `ashare_p0_first_round_validation` 继续保留为独立运行线
  - `butler_r0_ohlcv_object_cards` 的 latest 输出不与当前主线混提

### 2. `04_active_main_docs`

- 当前状态：
  - 顶层入口已完成去漂移与最新口径压缩
- 当前处理：
  - 只保留极短顶层同步
  - 若剩余只是历史追溯层旧段，不再继续扩写

### 3. `10_source_library_archive`

- 当前状态：
  - `batch_145~149` 成熟吸收包已完成目录级入库
  - 仍存在大量 `_raw_snapshot_batch09` 历史追溯层
- 当前处理：
  - `batch_145~148` 只保留冻结补强/样本包定位
  - `batch_149` 只保留后台维护态定位
  - `_raw_snapshot_batch09` 继续保持历史噪音/追溯层定位
  - 不把 snapshot 大树混进当前主线提交

### 4. `00_entry` 历史 untracked 大堆

- 当前状态：
  - 当前高价值正式页已在 `全库资料整理收口__20260713/` 内统一吸收
  - 根层旧资料入口已压回子目录
- 当前处理：
  - 后续只对白名单明确、仍承担正式入口的对象继续处理
  - 不再把“整目录不准加”当作绝对口径

## 六、红线

- 禁止：
  - `git add .`
  - 未经分档就混提历史 snapshot / raw_assets / latest 运行产物
  - 把继续演化中的他线 runtime 与当前收口线混交
  - 看到 `??` 就默认删

## 七、当前建议提交顺序

1. 先吸收成熟正式包与已确认的根层迁移删项
2. 再复核顶层入口是否仍有误导当前主线的旧口径
3. 若剩余只是不成体系的历史噪音或最新运行产物，则停止，不再人为扩线

## 八、一句话口径

- 当前 `git` 收尾线已从“前台白名单分流阶段”推进到“成熟包目录级吸收阶段”；已确认成熟的正式包应按白名单入库，剩余 raw snapshot / latest 产物继续保留为历史噪音或独立运行线，不做全仓一把梭。
