# A5 未完线总审计与收尾顺序页

更新时间：2026-07-23

## 一、这页用途

- 本页只做：
  - 把当前仓库里“开了头但没收口”的线重新盘清
  - 明确哪些是真未完线，哪些只是 backlog
  - 给出统一收尾顺序与停止规则
- 本页不做：
  - 重开全部旧方向
  - 把 backlog 全部升成当前主线
  - 把 git 脏区一次性无差别清空

## 二、主负责人裁决

- 当前真正需要收尾的工作线，按“可执行线”计数是 `5` 条。
- 其中：
  - `1` 条是当前前台硬主线
  - `2` 条是仓务/资料收尾线
  - `2` 条是旧线挂起未闭合线
- 另有 `1` 类治理 backlog：
  - `playbook NEXT_ITERATION`
  - 这类不算当前必须收尾线

## 三、五条未完线

### 1. `Batch2 前台最终收口线`

- 当前状态：
  - 已完成 `Home -> Stock -> QA -> Stock -> Home`
  - 已完成 `首页返回链保留最近事件上下文`
  - 已完成 `首页 query 直达与刷新恢复`
- 已完成：
  - `无效 eventId / 空 eventId` 的显式降级口径
- 当前裁决：
  - `Batch2` 前台最小闭环已冻结，不再继续以 polish 名义续刀
- 证据入口：
  - `A5_A股P0标的页Batch2停点冻结页__20260722.md`
  - `A5_A股P0标的页Batch2首页query直达与刷新恢复页__20260723.md`
  - `A5_A股P0标的页Batch2首页无效eventId降级页__20260723.md`

### 2. `git 脏工作区收尾线`

- 当前状态：
  - 仓内仍有大量历史脏文件，分布在 `00_entry / 02_runtime / 10_source_library_archive`
  - 但前台线已经连续使用白名单提交，说明“当前线可吃、全仓未清”
- 当前收口原则：
  - 不做 `git add .`
  - 不做全仓一把梭
  - 按 `KEEP_WHITELIST_ELSE_TEMP` 与既有 split plan 收
- 当前执行入口：
  - `00_entry/GIT_SUBMISSION_SPLIT_PLAN__20260707.md`
  - `00_entry/OLD_REPO_FILE_SWEEP_TASKBOARD.md`
  - `A5_git脏工作区安排页__20260723.md`

### 3. `截图区五任务吸收线`

- 当前对象已写死为 `5` 条代表性公式：
  - `沪深涨跌停`
  - `打板资金`
  - `上榜资金`
  - `HYDB行业对比`
  - `ZSDB指数对比`
- 当前现状：
  - `5` 条都已有正式 `txt` 锚点
  - `3` 条已正式吸收到 `batch_149` 下的 `png + txt` 双证据
  - `2` 条当前只见 `txt`，还没看到截图
- 当前收口原则：
  - 先吸收到正式路径
  - 再删除 `暂时存放`
  - 达到 `3` 条高质量双证据即可停，不为凑数硬扩
- 当前执行入口：
  - `A5_代表性可编辑指标源码导出清单与要求__20260722.md`
  - `A5_截图区五任务首批三条双证据吸收页__20260723.md`

### 4. `活跃文档去漂移线`

- 当前状态：
  - `memory` 已切到 `Batch1/Batch2`
  - 但 `04_active_main_docs/batch_01_selected/03_阶段二_当下计划_执行清单.md`
    顶部仍停在 `FV gate evidence packet v0`
- 当前问题：
  - 默认入口继续把人带回旧主线
  - 仓内真实推进状态与活跃入口口径不一致
- 当前收口原则：
  - 不重写长文历史层
  - 只压顶部合同层 / 默认入口 / 当前动作

### 5. `FV gate active_underperformance 证据挂起线`

- 当前状态：
  - 已正式转入挂起等待态
  - 当前继续保持 `still_need_evidence`
- 当前问题：
  - 这条线没有正式宣告“关闭”，只是让位给前台 P0
- 当前收口原则：
  - 无新运行事实前不升级状态名
  - 当前先保留正式挂起说明，不重开新证据类型
- 当前执行入口：
  - `A5_FV_gate_active_underperformance挂起说明页__20260723.md`

## 四、收尾顺序

1. `git 脏工作区安排页 + 当前线外脏区分流口径`
2. `活跃文档去漂移`
3. `FV gate still_need_evidence` 挂起等待态已落盘，后续仅在新运行事实出现时重开

## 五、停止规则

- `Batch2`：
  - 当前已冻结
  - 不再继续开体验细刀
- `git 脏区`：
  - 先形成“当前线结束后怎么吃”的安排页，不要求本轮清完整仓
- `截图区 5 任务`：
  - 当前已达到 `3` 条 `txt + 截图` 双证据
  - 本轮可停
- `活跃文档去漂移`：
  - 只修默认入口与当前动作，不重写追溯层
- `FV gate`：
  - 没有新运行事实前，不升级状态名

## 六、一句话口径

- 当前仓库不是“只有一条前台线没做完”，而是共有 `5` 条未完线；其中应先收 `Batch2 最后一刀`，再顺手把 `git / 截图 / 活跃入口漂移 / FV gate 挂起口径` 依次收尾。
