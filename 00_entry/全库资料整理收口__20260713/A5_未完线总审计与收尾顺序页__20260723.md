# A5 未完线总审计与收尾顺序页

更新时间：2026-07-23

## 一、这页用途

- 本页只做：
  - 把当前仓库里“开了头但没收口”的线重新盘清
  - 明确哪些是真未完线，哪些只是 backlog
  - 给出统一收尾顺序与停止规则
- 本页不做：
  - 重开全部旧方向
  - 把 backlog 全部升成当前主线
  - 把 git 脏区一次性无差别清空

## 二、主负责人裁决

- 本轮点回时，当前真正需要收尾的工作线按“可执行线”计数是 `5` 条。
- 这 `5` 条现已全部落盘收口：
  - `Batch2 前台最终收口线`
  - `git 脏工作区收尾线`
  - `截图区五任务吸收线`
  - `活跃文档去漂移线`
  - `FV gate active_underperformance 证据挂起线`
- 当前已不再存在“必须先吃掉的前台硬缺口”。
- 当前顺推改写为：
  - `3` 条治理补页已全部落盘
  - `batch149` 后台维护态顺序已冻结
  - `git` 成熟包吸收与 `00_entry` 根层迁移删项已补齐
  - `Batch2` 前台残留 `Placeholder / 占位页` 旧措辞已完成术语收尾
  - `batch_145~149` 在来源库总索引中的旧 `working` 状态名已完成收尾
  - 剩余 `ashare_p0_first_round_validation / butler latest / _raw_snapshot_batch09` 统一降级为独立运行线或历史噪音，不再升成默认主线

## 三、五条未完线

### 1. `Batch2 前台最终收口线`

- 当前状态：
  - 已完成 `Home -> Stock -> QA -> Stock -> Home`
  - 已完成 `首页返回链保留最近事件上下文`
  - 已完成 `首页 query 直达与刷新恢复`
- 已完成：
  - `无效 eventId / 空 eventId` 的显式降级口径
- 当前裁决：
  - `Batch2` 前台最小闭环已冻结，不再继续以 polish 名义续刀
- 证据入口：
  - `A5_A股P0标的页Batch2停点冻结页__20260722.md`
  - `A5_A股P0标的页Batch2首页query直达与刷新恢复页__20260723.md`
  - `A5_A股P0标的页Batch2首页无效eventId降级页__20260723.md`

### 2. `git 脏工作区收尾线`

- 当前状态：
  - 已完成 `00_entry/全库资料整理收口__20260713/` 正式文档包吸收
  - 已完成 `02_runtime/a5_g5_*` 成熟运行包吸收
  - 已完成 `10_source_library_archive/batch_145~149` 成熟来源包吸收
  - 已确认 `00_entry` 根层 `3` 个旧资料入口迁回子目录，`NF1` 临时评估卡退出正式入口
- 当前收口原则：
  - 不做 `git add .`
  - 不做全仓一把梭
  - 按 `KEEP_WHITELIST_ELSE_TEMP` 与既有 split plan 收
  - 剩余 `_raw_snapshot / raw_assets / latest` 产物不再冒充默认当前刀
- 当前执行入口：
  - `00_entry/GIT_SUBMISSION_SPLIT_PLAN__20260707.md`
  - `00_entry/OLD_REPO_FILE_SWEEP_TASKBOARD.md`
  - `A5_git脏工作区安排页__20260723.md`

### 3. `截图区五任务吸收线`

- 当前对象已写死为 `5` 条代表性公式：
  - `沪深涨跌停`
  - `打板资金`
  - `上榜资金`
  - `HYDB行业对比`
  - `ZSDB指数对比`
- 当前现状：
  - `5` 条都已有正式 `txt` 锚点
  - `3` 条已正式吸收到 `batch_149` 下的 `png + txt` 双证据
  - `2` 条当前只见 `txt`，还没看到截图
- 当前收口原则：
  - 先吸收到正式路径
  - 再删除 `暂时存放`
  - 达到 `3` 条高质量双证据即可停，不为凑数硬扩
- 当前执行入口：
  - `A5_代表性可编辑指标源码导出清单与要求__20260722.md`
  - `A5_截图区五任务首批三条双证据吸收页__20260723.md`

### 4. `活跃文档去漂移线`

- 当前状态：
  - `00 / 03 / 关于日活` 顶部已同步到：
    - `Batch2` 冻结
    - `5` 条硬收尾已完
    - `3` 条治理补页当前入口
- 当前问题：
  - 已不再是入口漂移
  - 当前只剩维护态同步，不再算活跃未完线
- 当前收口原则：
  - 不重写长文历史层
  - 只压顶部合同层 / 默认入口 / 当前动作

### 5. `FV gate active_underperformance 证据挂起线`

- 当前状态：
  - 已正式转入挂起等待态
  - 当前继续保持 `still_need_evidence`
- 当前问题：
  - 当前不是“没挂起”，而是“有挂起说明、但仍无新运行事实”
- 当前收口原则：
  - 无新运行事实前不升级状态名
  - 当前先保留正式挂起说明，不重开新证据类型
- 当前执行入口：
  - `A5_FV_gate_active_underperformance挂起说明页__20260723.md`

## 四、当前顺推顺序

1. `无新增必做主线（当前停点）；若只剩历史噪音或 latest 产物，则不再续刀`
2. `经验型视频补充触发与排除页（已完成）`
3. `视频资料提取与多 AI 交叉校对流程页（已完成）`
4. `仓库重要文件入口与跨机迁移说明页（已完成）`
5. `batch149 方案A 最终补采优先级与停止规则页（已完成）`

## 五、停止规则

- `Batch2`：
  - 当前已冻结
  - 不再继续开体验细刀
- `git 脏区`：
  - 当前成熟包与根层迁移删项已按白名单吸收
  - 仍不做全仓一把梭
  - 若剩余只是不成体系的历史噪音或 latest 产物，则停
- `独立运行线`：
  - `ashare_p0_first_round_validation` 继续保留为独立 runtime 线
  - `butler_r0_ohlcv_object_cards` 继续保留正式入口，latest 验收输出不作为当前旧线续刀对象
- `来源库尾巴`：
  - `batch_145~148` 当前只保留冻结补强/样本包定位
  - `batch_149` 当前只保留后台维护态定位
  - `_raw_snapshot_batch09` 统一视为历史追溯层，不再按“旧线”逐项清理
- `截图区 5 任务`：
  - 当前已达到 `3` 条 `txt + 截图` 双证据
  - 本轮已停
- `活跃文档去漂移`：
  - 顶部入口已修
  - 不重写追溯层
- `FV gate`：
  - 没有新运行事实前，不升级状态名
- `治理补页`：
  - 三页落盘并回填短入口后即停
  - 不顺手扩成新一轮资料大扫库
- `历史入口残留`：
  - 只在仍会误导当前主线时才修顶层短入口
  - 若剩余只是历史文件名或追溯层旧段，则直接停，不再继续扩写

## 六、治理补页入口

- `A5_经验型视频补充触发与排除页__20260723.md`
- `A5_视频资料提取与多AI交叉校对流程页__20260723.md`
- `A5_仓库重要文件入口与跨机迁移说明页__20260723.md`
- `A5_batch149方案A最终补采优先级与停止规则页__20260723.md`

## 七、一句话口径

- 当前 `5` 条硬收尾线、`3` 条治理补页、`batch149` 维护态裁决、`git` 成熟包吸收、Batch2 旧措辞收尾与来源库旧状态名收尾均已收口；剩余仅保留独立运行线与历史噪音定位，本轮不再人为重开新主线。
