# A5 A股 P0 Batch2 首页返回链保留事件上下文走查记录

更新时间：2026-07-23

## 一、走查目标

- 验证 `Home -> Stock -> QA -> Stock -> Home` 返回链已不再掉回首页空态。
- 验证最近一次 `eventId` 能从标的页带回首页。
- 验证首页可按 `eventId` 恢复最近事件上下文。

## 二、走查环境

- 本地地址：`http://localhost:5174/`
- 走查标的：`300750`
- 走查方式：桌面端真实点击流

## 三、走查路径

- 首页点击 `个股出现启动候选信号 / 宁德时代`
- 进入 `/stock/300750?eventId=stock-trigger-point-20260720-300750`
- 进入问答页 `/stock/300750/qa?eventId=stock-trigger-point-20260720-300750`
- 从问答页返回标的页
- 从标的页返回首页

## 四、走查结果

- 通过：进入标的页时，URL 已保留首页当前事件的 `eventId`
- 通过：进入问答页后，URL 继续保留同一 `eventId`
- 通过：从标的页返回首页后，首页地址为：
  - `/?eventId=stock-trigger-point-20260720-300750`
- 通过：首页主工作区仍展示 `个股出现启动候选信号 / 宁德时代 · 13:14`
- 通过：首页没有回到 `先从左侧选一条今日事件` 的空态
- 通过：本轮未见业务控制台报错、回链丢失或页面空白

## 五、主负责人结论

- `Batch2` 当前已形成更顺的返回链：
  - `首页选中事件 -> 标的页 -> 问答页 -> 标的页 -> 首页仍保留最近事件上下文`
- 当前可以把这条链正式冻结为：
  - `基于 query eventId 的最小返回链保留`
- 当前仍明确不含：
  - `localStorage/sessionStorage`
  - `未提交草稿恢复`
  - `复杂返回栈管理`
