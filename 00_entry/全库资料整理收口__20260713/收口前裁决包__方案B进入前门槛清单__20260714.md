# 收口前裁决包 方案B进入前门槛清单

更新时间：2026-07-14

## 用途

- 这份清单只回答一件事：
  - 现在能不能正式进入 `方案B`
- 当前不是讨论 `方案B` 具体怎么做功能，而是先定义：
  - 哪些条件必须先过
  - 哪些条件只要有显式降级口径就能过
  - 哪些事项不再作为入场门槛

## 方案B 当前定义

- 延续既有裁决口径：
  - `方案B：Batch9 + 可跑对象双线补采`
- 进入 `方案B` 的含义是：
  - 资料整理主线从“全库大范围收口”切到“首批补采闭环 + 下一阶段使用准备”
- 当前仍明确不包含：
  - runtime 新开发
  - 默认执行逻辑扩展
  - 回测与自动化实现

## 门槛分级

### 一、硬门槛

- 不满足就不建议正式切入 `方案B`

### 二、软门槛

- 最好满足
- 若未满足，必须已经有显式尾项说明和不误导的降级口径

### 三、非门槛

- 可以继续挂在后续待办
- 不应因为这些尾项未补完，就无限拖延 `方案B`

## 门槛清单

| 门槛项 | 类型 | 为什么重要 | 当前状态 | 通过标准 | 当前判断 |
|---|---|---|---|---|---|
| 首批补采 `4` 项的剩余缺口已经统一成表 | `硬门槛` | 进入下一阶段前必须知道还差什么，而不是继续凭印象推进 | 已通过 | 有统一总表，且四项区分硬缺口/尾口 | `通过` |
| `batch_147` 的 intraday 实物缺口已闭合或已被正式保留为“未通过门槛” | `硬门槛` | 这是首批补采里唯一真实样本硬缺口，直接影响 `YTC` 最小运行线闭环 | 已通过 | `5m/60m` 样本与 provenance/index 已补齐，或明确继续停在门外 | `通过` |
| `MFLOW vs INSTB` 的多AI讨论前情提要、边界与主负责人预裁决已形成 | `硬门槛` | 方案B 后半段若要决定第二批观察对象，必须先有可讨论框架和初步拍板 | 已通过 | 有统一发包模板、评分轴与“先审 MFLOW、INSTB 继续观察”的预裁决口径 | `通过` |
| `ATRATIO` 是否继续冻结已经写死 | `软门槛` | 避免进入方案B后又被新线索打断 | 已通过 | 明确保留 `future_only` | `通过` |
| `batch_145 / batch_146` 的证据尾口已被显式定义为“证据补强尾口”而非“未起步” | `软门槛` | 避免把已落网页补证的对象重新当成大缺口 | 已通过 | 明确保留 `partial`，并写清仍缺源码硬证据 | `通过` |
| `batch_148` 已明确属于“校验尾口”而非“结构缺位” | `软门槛` | 避免第二只 seed 已落盘后仍被错误判成未成形 | 已通过 | 明确当前只差更强校验记录 | `通过` |
| `NFTRADEZ / Smile / TK` 与 second-hop 尾项不再被当成入场门槛 | `非门槛` | 这些尾项继续补只会拖慢切换，不解决首批补采核心问题 | 已确认 | 继续留在待办，不阻塞阶段切换 | `不阻塞` |

## 当前裁决

- 现在进入 `方案B` 的硬门槛已经全部通过。
- 但当前最稳的顺序仍不是：
  - 立刻口头宣布“已经正式进入 `方案B`”
- 当前更稳的顺序是：
  - 先把这份“收口前裁决包”作为阶段尾动作收住
  - 包括首批补采尾口复核
  - 以及 `MFLOW vs INSTB` 的主负责人预裁决
  - 然后再正式切 `方案B`

## 进入条件的最小版本

- 若要用最保守口径进入 `方案B`：
  - 当前条件已满足
  - 但建议先完成本轮裁决包复核与预裁决收口，再正式切入
- 若仍想保持更谨慎口径：
  - 可以先停在“硬门槛已过、裁决包待收口”
  - 但不应再把 `batch_147` 作为未通过门槛继续挂起

## 当前不再继续纠缠的事项

- `NFTRADEZ` 是否还要继续多补几张映射页
- `Smile_SMC` 的 `video_screenshots` 是否再加深
- `TK` 是否继续补更细 candle/continuation 示例
- `00_交易系统书籍 / 00_大隐体系 / 02_原子化拆解文件` 的 second-hop 细表

## 主负责人结论

- 当前门槛判断：
  - `方案B` 的硬门槛已正式通过
- 当前最小下一步：
  - 先按现有裁决包收住：
    - 首批补采尾口复核
    - `MFLOW` 先审、`INSTB` 继续观察 的预裁决
  - 然后再正式切入 `方案B`
- `Tushare stk_mins` 阻塞：
  - 当前仅保留为历史失败路线说明，不再阻塞阶段判断

## 回链

- `收口前裁决包__首批补采剩余缺口总表__20260714.md`
- `参考交易系统补采优先级表__20260713.md`
- `首批补采对象批次级执行清单__20260713.md`
