# A5 信息直播间文字抓取与核心留存优化页

更新时间：2026-08-07

## 一、这页用途

- 基于第一批四房（格兰投研 / 周期女王 / 天机 / 龙头交易猿）已收录证据，
  冻结「文字怎么抓、核心怎么留」的优化口径。
- 先把这四个模板房脚本改稳，再扩跑其他置顶房。

## 二、四房证据暴露的问题

| 房间 | 滚动/到尽 | 文字核心 | 房间锚点 |
|------|-----------|----------|----------|
| 格兰投研 | 到尽通过 | 偶发超长粘连卡（多条拼成一条） | 易误写 `k神` |
| 周期女王 | `scroll_end` 通过 | 正文大体可用 | 易误写 `浮光`/`k神`（混标签） |
| 天机 | `scroll_end` 通过 | **广告头盖正文**（几乎只剩微信引流句） | 易误写 `先知` |
| 龙头交易猿 | `scroll_end` 通过 | 正文可用；`陈子瞻` 前缀 | 本轮正确（别名已生效） |

成因备注：直播间常见机器人扒取痕迹，侧栏/正文可能混入他人标签；验收房间身份仍以人工确认 + 置顶名单为准。

## 三、优化目标（先四房）

1. **抓住核心正文**：去掉引流壳，优先最长实质段落
2. **抗混标签**：短漂移名（`k神`/`先知`/`浮光`）降权；优先模板房名单
3. **粘连卡可拆**：超长卡按日期时间边界拆成多条
4. **可强制纠偏**：支持 `forced_room_anchor`，置顶名单未冻结前先人工保底
5. **留存可验收**：导出 `export_version` 升版，heuristics 记下是否启用剥离/拆分

## 四、脚本版本

- 当前页：`live_info_current_page_export_v1.js` → **v1.5**
- 增量：`live_info_incremental_export_v1.js` → **v1.3**（跟随当前页结果；同步别名/优先房/漂移降权）

## 五、核心规则（冻结）

### 5.1 正文

1. 叶子文本先滤 UI 壳，再滤广告壳：
   - `更多资讯加微信好友…`
   - `防止失联QQ群…`
   - 纯表情标签（捂脸等）
2. 多叶子时：优先最长实质段；短广告段不进正文
3. 叶子几乎全是广告时：回退 `card.innerText` 并剥离广告碎片；仍只剩短广告则丢弃该卡
4. 单卡文本过长（≥600）且含 ≥2 个日期时间戳：按时间边界拆成多条留存

### 5.2 房间锚点

优先级：

1. `forced_room_anchor`（人工指定）
2. 说话人别名（`至尊宝→孙悟空金牌`，`陈子瞻→龙头交易猿`）
3. 侧栏/标题已命中优先房（格兰/女王/天机/龙头/孙悟空）→ **不让混标签覆盖**
4. 正文 `#房间名#`（降权漂移名；优先名单加权）
5. 其余回退

房间名归一：

- `天机短线试更新` → `天机`
- `周期女王（新一期…）` / `周期女王…` → `周期女王`

### 5.3 侧栏选取

- 不再「越短越好」硬规则
- `active/selected` + 优先房加分；`k神/先知/浮光/知行合一` 减分

## 六、复跑验收进度

| 房间 | v1.5/v1.3 复跑 | 证据 | 结论 |
|------|----------------|------|------|
| 周期女王 | **已过** | `171611` + `171846`（`scroll_end`/267，锚点正确，无 forced） | 通过 |
| 格兰投研 | 待跑 | — | — |
| 天机 | 待跑 | — | 重点看广告壳是否还盖正文 |
| 龙头交易猿 | 待跑 | — | — |

## 七、复跑口令（四房验收）

每房：

```js
// 可选：侧栏仍飘时强制
window.__infoLiveCurrentPageExportV1Options = {
  forced_room_anchor: "周期女王" // 或 格兰投研 / 天机 / 龙头交易猿
};
// 1) 粘贴 live_info_current_page_export_v1.js（确认 console 为 v1.5）
window.__infoLiveIncrementalExportV1Options = {
  reset_checkpoint: true,
  scroll_direction: "up",
  start_position: "keep",
  max_rounds: 80,
  forced_room_anchor: "周期女王" // 与上保持一致
};
// 2) 粘贴 live_info_incremental_export_v1.js（确认 console 为 v1.3）
```

### 验收清单

1. `export_version` = `..._v1.5` / 增量 `..._v1.3`
2. `room_anchor` = 目标正式名（或你 forced 的名）
3. `excerpt` / `visible_messages[].text` **不是**单独一句微信引流
4. 龙头：可见 `陈子瞻` 前缀正文，房间仍为 `龙头交易猿`
5. 天机：若页面上肉眼有市场正文，导出里应出现非广告核心；若 DOM 真无正文只剩广告，记「图片/外链型」另案，不硬编
6. JSON 仍回落到 `batch_05/.../priority_rooms/<房间>/`

## 八、扩跑其他置顶房的前置

四房 v1.5/v1.3 复跑确认没问题后，才：

1. 冻结完整置顶名单（写入 `PRIORITY_ROOM_NAMES`）
2. 补说话人别名表
3. 再批量抓其他置顶房

## 九、一句话口径

- 先优化「去广告壳 + 抗混标签 + 粘连拆分」；四房复跑过线后，再扩置顶名单。周期女王 v1.5 已先过。
