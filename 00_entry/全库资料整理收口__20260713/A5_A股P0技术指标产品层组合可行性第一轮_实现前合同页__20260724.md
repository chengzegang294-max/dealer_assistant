# A5 A股 P0 技术指标产品层组合可行性第一轮 实现前合同页

更新时间：2026-07-24

## 一、这页用途

- 本页只做：
  - 把
    `市场宽度 / 资金活跃 / 行业-指数参照`
    这
    `3`
    个语义组，
    压成当前阶段的
    `实现前合同`
- 本页不做：
  - 前端大改版
  - 直接把页面改成按组展示
  - 金融真值判断

## 二、当前主线

- 当前主线是：
  - `A股 P0 技术指标产品层组合可行性第一轮`
- 当前不是：
  - `FV gate`
  - 技术指标组合回测
  - 问答页按组重写

## 三、当前最值钱的一步

- 当前最值钱的一步是：
  - 先把语义组能力收进
    `共享 helper`
  - 让底层代码知道：
    - 哪个
      `sourceCard`
      属于哪个语义组
    - 哪个语义组更适合哪个页面
  - 但前台仍保持：
    - `sourceCard`
      为第一锚点

## 四、主负责人裁决

- 当前正式裁决：
  - 先做
    `共享语义组映射层`
  - 不直接做
    `按组展示改版`
- 当前实现顺序固定为：
  1. 共享语义组 helper
  2. 共享测试
  3. 入口回填

## 五、共享 helper 合同

- 当前 helper 至少要统一提供：
  1. `sourceCard -> groupKey`
  2. `sourceCard -> groupLabel`
  3. `groupKey -> 页面边界`
  4. `groupKey -> 成员列表`

## 六、当前不改什么

- 当前不改：
  - 首页解释卡文案主结构
  - 标的页标签展示主结构
  - 问答页问题生成主结构
  - 问答页按组回答

## 七、为什么先这样做

- 当前这样做的原因是：
  1. 最贴现有代码边界
  2. 不会让
     `文档层按组`
     与
     `代码层按 sourceCard`
     继续各写各的
  3. 为下一手最小实现留出稳定锚点

## 八、下一手唯一执行物

- 当前这一页之后，
  下一手唯一应落的是：
  - `src/features/indicator/utils/semanticGroups.ts`
- 同时必须补：
  - 对应测试

## 九、一句话口径

- 当前最准确的一句话是：
  - 组合可行性第一轮当前先不改前台展示，
    只先把
    `3`
    个语义组压进共享 helper，
    让底层实现先具备稳定分组能力。
