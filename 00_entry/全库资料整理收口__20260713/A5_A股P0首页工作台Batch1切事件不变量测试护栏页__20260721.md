# A5 A股 P0 首页工作台 Batch1 切事件不变量测试护栏页

更新时间：2026-07-22

## 一、这页用途

- 本页只做：
  - 吸收 `Cursor` 最新实现向审校回包
  - 冻结当前最值钱的一手从“继续做 section contract 美化”切到“给切事件不变量补最小自动化护栏”
  - 写死本轮允许落地的最小测试范围与停止规则
- 本页不做：
  - 重开 `A股 P0` 产品方向
  - 重开字段合同
  - 把测试范围扩大成整套页面交互测试

## 二、主负责人吸收结论

- `Cursor` 裁决：
  - `可继续推进`
  - 无硬阻塞
  - 当前最高优先不是继续拆 UI，而是锁住切事件不变量
- 主负责人吸收：
  - 接受该判断
  - 当前暂停继续美化 `EventStreamPanel` contract
  - 这一手已完成：
    - 把“切事件时重置 `homeRecordDraft` 并清空 `latestSubmitEcho`”下沉成 `workspaceModel` 纯函数
    - 已补最小自动化测试
  - 当前裁决改为：
    - 先把 `Batch1` 这一段正式停点收口
    - 不在此刻继续补提交路径护栏

## 三、为什么当前最值钱

- 当前首页结构已经足够薄：
  - 页面壳
  - 页面级 hook
  - section contract
  - adapter
  - workspace model
- 当前真正仍缺的不是结构层次，而是：
  - 切事件不变量尚无自动化护栏
- 若继续只做 contract 对齐：
  - 边际收益下降
  - 容易挤占真正该锁的回归风险点

## 四、本轮允许范围

- 允许修改：
  - `workspaceModel.ts`
  - `useHomeWorkspace.ts`
  - `package.json`
  - 最小测试配置文件
  - `workspaceModel` 对应测试文件
- 允许新增：
  - `applySelectEvent(...)` 或等价纯函数
  - 最小 `vitest` 运行脚本
  - 只覆盖切事件不变量的最小单测
- 不允许借机扩展：
  - 整页组件测试
  - 浏览器自动化大改
  - 新功能实现

## 五、本轮最小通过条件

- 满足以下五项即可视为这一小段收口：
  1. 切事件逻辑下沉为 `workspaceModel` 纯函数
  2. `useHomeWorkspace` 消费该纯函数，不再手写散落状态重置
  3. 至少有一组自动化测试锁住：
     - `selectedEventId` 更新
     - `homeWorkspaceState` 进入 `selected`
     - `homeRecordDraft` 被重建
     - `latestSubmitEcho` 被清空
     - `formError` 被清空
  4. `npm run build` 通过
  5. `npm run lint` 与 `npm run test` 通过

## 六、本轮结果

- 当前代码侧已对上：
  - `applySelectEvent(...)` 已位于 `workspaceModel.ts`
  - `useHomeWorkspace.ts` 已消费该纯函数
  - `workspaceModel.test.ts` 已锁住切事件两条核心断言及相关清空行为
- 当前这一小段已满足收口条件：
  - 结构稳定
  - 切事件不变量已有最小护栏
  - 不再需要继续外扩测试范围才能判定本段完成
- 当前正式停点写死为：
  - `首页薄壳 + workspace 不变量 + section props + adapter 分层 + 切事件最小自动化护栏`

## 七、当前明确不做

- 当前不要求：
  - `useHomeWorkspace` hook 级测试全覆盖
  - 提交成功/失败最小护栏
  - `EventStreamPanel` 继续 contract 美化
  - 搜索区与右栏再做新一轮结构调整
  - 新一轮 adapter / section 美化

## 八、下一手

- 当前不继续扩测。
- 当前先停在：
  - `Batch1` 结构稳定点
- 之后若要再开下一刀，优先重新判断是否真的需要补提交路径护栏，而不是顺手继续美化结构。
