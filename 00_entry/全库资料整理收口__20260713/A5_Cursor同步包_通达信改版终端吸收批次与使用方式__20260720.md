# A5 Cursor 同步包 通达信改版终端吸收批次与使用方式

更新时间：2026-07-23

## 一、这页用途

- 这页只做一件事：
  - 把 `Trae` 已完成的 `通达信改版终端吸收批` 结果同步给 `Cursor`
- 这页不做：
  - 让 `Cursor` 重新从零猜这套软件是什么
  - 让 `Cursor` 重开产品大方向
  - 让 `Cursor` 直接替代主负责人裁决

## 二、当前已完成什么

- `Trae` 已正式创建：
  - `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/`
- 已补齐：
  - `README.md`
  - `provenance.md`
  - `manifest_v1.tsv`
  - `BATCH_149_EXECUTION_CARD.md`
  - `BATCH_149_ARTIFACT_INDEX_v1.md`
  - `01_index/family_entry_map_v1.tsv`
  - `02_absorb_index/tdx_custom_terminal_layered_absorb_index_v1.md`
  - `02_absorb_index/tdx_custom_terminal_rebuild_fit_notes_v1.md`
- 已把代表性原件按四层吸进 `00_raw_snapshot/`：
  - `config`
  - `pages`
  - `formula`
  - `f11`
  - `plugin`
- 已补第一组高价值原位件：
  - `files/f11/*`
  - `HGPlugins/JiuCaisd/Config/*`
- 已新增：
  - `02_absorb_index/tdx_custom_terminal_visible_effect_source_triage_v1.md`

## 三、当前主负责人结论

- 这不是“几个指标公式文件”的小包。
- 这是一套：
  - `通达信终端底座`
  - `配置壳`
  - `页面聚合层`
  - `公式索引 / 交易脚本层`
  - `插件层`
  叠起来的改版终端。
- 当前最重要的吸收不是公式抄写，而是：
  - 识别它的真实分层
  - 判断哪些东西值得后续复刻

## 四、已经拿到的硬证据

- `embui.cfg`
  - 证明菜单、顶栏、侧栏、本地页面挂载和个股入口都被高度定制
- `Connect.cfg`
  - 证明主机、网页、工具、云端服务入口被统一配置
- `company/Page.html`
  - 证明个股研究页是 `占位符替换 + iframe 聚合` 结构
- `company/kpl-K线图.html`
  - 证明一类“指标效果”其实是 `ECharts + Longhuvip API` 的网页载体
- `company/东财K线+资金图.html`
  - 证明另一类“指标效果”其实是 `ECharts + Eastmoney API` 的网页载体
- `funcs_std.ini / funcs_jy.ini`
  - 证明存在功能/公式索引层
- `flatjy.lua`
  - 证明至少交易面板一侧存在真实逻辑脚本
- `OtherPlu.cfg / duilibstyle.xml`
  - 证明插件注册与插件 UI 样式是独立层

## 五、当前不要误判的地方

- 不要把 `页面载体` 误认成 `公式源码`。
- 不要把 `ini 索引` 误认成 `完整指标源码库`。
- 不要把“用户确认可编辑”误写成“我们已经吸收了全部可编辑指标源码”。
- 不要把这批材料直接等同于我方产品合同。

## 六、当前我希望 Cursor 怎么用这批材料

- `Cursor` 现在最适合做的，不是重讲方向，而是做编码向/结构向统领：
  - 看当前分层判断有没有硬冲突
  - 看哪些层最适合下一步继续吸收
  - 看 `可见效果 -> 来源三分表` 是否成立
- `Cursor` 当前不需要做：
  - 终局裁决
  - 金融有效性判断
  - 主线切换

## 七、建议 Cursor 阅读顺序

- 1. `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/README.md`
- 2. `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/BATCH_149_EXECUTION_CARD.md`
- 3. `00_entry/全库资料整理收口__20260713/A5_batch149方案A最终补采优先级与停止规则页__20260723.md`
- 4. `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/02_absorb_index/tdx_custom_terminal_layered_absorb_index_v1.md`
- 5. `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/02_absorb_index/tdx_custom_terminal_rebuild_fit_notes_v1.md`
- 6. 若要核证据，再看：
  - `10_source_library_archive/batch_149_tdx_custom_terminal_external_folder_absorb__20260719/BATCH_149_ARTIFACT_INDEX_v1.md`

## 八、我希望 Cursor 回答的唯一问题

- 当前这条线已不要求 `Cursor` 再判断“下一手是不是三分表”。
- 当前只需要 `Cursor` 接受已冻结口径：
  - `batch_149` 已转后台维护态
  - 若未来重开 `方案A`，顺序固定为：
    1. `F11 联动解释`
    2. `company 高价值页面补进三分表`
    3. `极窄公式截图链补洞`
- `Cursor` 当前不需要再对这条线重新发散新方向。

## 九、一句话同步口径

- `Trae` 已把这套朋友给的通达信改版软件收成正式吸收批，并确认它是“终端级改版包”，不是“几条独立指标”；当前这条线已转后台维护态，若未来再开 `方案A`，已冻结为 `F11 联动解释 -> company 高价值页面三分表 -> 极窄公式截图链补洞`，不直接重开源码导出或 DLL 深挖。

## 十、Cursor 回包吸收

- `Cursor` 已回包确认三点：
  - `终端级改版包，不是几条独立指标`
  - `funcs/*.sp` 更像页面/功能编排，不等同公式源码
  - `files/f11` 与 `HGPlugins/JiuCaisd/Config` 是高价值层
- 主负责人已吸收该回包，并修正旧口径：
  - `代表性指标源码导出`
  - 由“当前唯一下一手”调整为“后置下一手”
- 当前又继续向前补裁为：
  - `batch_149` 不再作为默认当前刀
  - 后台维护态的 `方案A` 顺序已另行冻结到：
    - `A5_batch149方案A最终补采优先级与停止规则页__20260723.md`
  - 其中：
    - `F11 联动解释` 为第一优先
    - `DLL 行为` 继续保持 `NEED_EVIDENCE`
