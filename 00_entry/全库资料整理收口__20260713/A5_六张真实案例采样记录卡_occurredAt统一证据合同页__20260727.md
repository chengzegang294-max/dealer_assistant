# A5 六张真实案例采样记录卡 occurredAt 统一证据合同页

更新时间：2026-07-27

## 一、这页用途

- 本页只做：
  - 把六张真实案例采样记录卡的 `occurredAt` 证据层级统一冻结
  - 解决当前“日期已确认，但精确时刻未确认”时，六张卡该怎么统一写

## 二、当前统一裁决

- 当前统一裁决为：
  1. 六张卡的
     `sample_date`
     已统一确认为：
     `2026-07-20`
  2. 六张卡当前允许统一确认：
     - `occurredAt_window = 2026-07-20 09:00-15:15`
  3. 六张卡当前仍不允许伪写：
     - 精确 `hh:mm:ss`
     - 单事件精确分钟级时刻

## 三、当前证据链

### 3.1 对象卡层

- 六张对象卡当前统一写有：
  - `occurredAt : 当次触发时间`

- 这说明：
  - 事件层时间字段确实存在
  - 但对象卡没有把精确时刻直接落成具体数值

### 3.2 字段桥层

- `batch149_formula_semantics_to_batch1_field_bridge_v1.md`
  当前明确冻结：
  - `occurredAt`
    用当次触发时间

- `batch149_six_card_event_field_bundle_v1.md`
  当前明确冻结：
  - `事件卡时间 = EventSummary.occurredAt`
  - 与首页事件流时间轴一致

### 3.3 原始页面层

- `00_raw_snapshot/pages/市场情绪.html`
  当前明确出现：
  - `time_range("9:00","15:15")`
  - `tt = data2.match(/"date":"(\S*)","time"/)[1];`

- 这说明：
  1. 页面运行时明确存在
     `9:00-15:15`
     的当日刷新窗口
  2. 页面本身会从接口数据中提取
     `date`
  3. 当前能够确认：
     - 案例日期
     - 同日运行时窗口
  4. 当前仍不能直接确认：
     - 单事件精确时刻

## 四、当前统一允许怎么写

- 六张卡当前统一允许写成：

```text
- sample_date: 2026-07-20
- occurredAt: date_level_confirmed__runtime_window_confirmed
- occurredAt_window: 2026-07-20 09:00-15:15
- occurredAt_exact: still_need_evidence
```

## 五、当前统一禁止怎么写

- 当前统一禁止写成：
  1. 一个具体
     `hh:mm:ss`
  2. 一个具体分钟级时点
  3. “已经确认单事件精确触发时刻”

## 六、主负责人裁决

- 当前正式裁决为：
  1. 六张卡的 `occurredAt` 当前正式推进到：
     `日期粒度 + 运行时窗口粒度`
  2. 当前剩余唯一时间边界收敛为：
     `单事件精确时刻`
  3. 后续若继续补时间，
     只补这一层，
     不再反复重写日期层和窗口层

## 七、一句话口径

- 六张真实案例采样记录卡当前已经不是“完全没有 occurredAt”，
  而是正式确认到：
  `2026-07-20 的 9:00-15:15 运行时窗口`
  ；
  还没确认的，只剩单事件精确时刻。
