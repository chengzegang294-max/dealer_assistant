# A5 第二日期计划行回填与验收输出命名页

更新时间：2026-07-30

## 一、这页用途

- 本页只做：
  - 冻结第二日期样本接入时，
    `a5_input_pack_sample_plan_v1.tsv`
    应该怎么回填
  - 冻结验收输出 JSON 的命名口径

## 二、当前计划行回填规则

- 第二日期样本当前统一回填到：

```text
02_runtime/a5_financial_testing_standardization/a5_input_pack_sample_plan_v1.tsv
```

- 当前统一回填字段顺序冻结为：

```text
input_pack_id
sample_date
source_family
source_root
required_files
optional_files
acceptance_output_json
```

## 三、当前统一写法

### 3.1 input_pack_id

- 当前统一写法：

```text
A5_R2_EASTMONEY_SCREENSHOT_INPUT_PACK_<sample_date>_V1
```

### 3.2 acceptance_output_json

- 当前统一写法：

```text
02_runtime/a5_financial_testing_standardization/acceptance_outputs/a5_input_pack_acceptance_<source_family>_<yyyymmdd>.json
```

## 四、当前回填动作

- 第二日期样本到位后，
  当前推荐顺序冻结为：

1. 先用计划行渲染脚手架生成一行 TSV
2. 再用计划行回填脚手架写入 sample plan
3. 再运行单输入包验收
4. 最后重跑两日期门槛

## 五、主负责人裁决

- 当前正式裁决为：
  1. 第二日期计划行回填，
     不再手工直接改 TSV
  2. 先生成行，
     再用脚手架回填
  3. 验收输出文件名继续按统一模板写，
     不允许每次随手改名

## 六、一句话口径

- 第二日期当前先按固定字段顺序回填 sample plan，
  再按统一 JSON 命名跑验收，
  不再手工拼列和改输出名。
