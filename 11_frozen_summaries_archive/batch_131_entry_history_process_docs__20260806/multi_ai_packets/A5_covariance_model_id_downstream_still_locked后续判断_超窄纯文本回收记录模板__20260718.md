# A5 covariance_model_id downstream_still_locked 后续判断 超窄纯文本回收记录模板

更新时间：2026-07-18

## 一、回收总表

| 模型 | 有效性 | 第 1 行结论 | 第 2 行一句话原因 | 第 3 行唯一锁定原因 / 条件 | 第 4 行禁止误写提醒 | 第 5 行唯一焦点 | 备注 |
|---|---|---|---|---|---|---|---|
| `GPT` | `valid` | `conditional` | `剩余锁定原因可再收缩但未裁定` | `唯一剩余锁定原因需收敛为单条正式边界` | `仍未到 risk_model_ready` | `N/A` | `是当前最稳主模式票之一` |
| `DeepSeek` | `valid` | `conditional` | `下游 no 票已压实锁定原因` | `需补 downstream 最小解锁条件书面定义` | `本体未实跑，仍缺失败模式验证` | `N/A` | `与 GPT 方向一致，偏向书面定义化` |
| `Kimi` | `weak_valid` | `conditional` | `集成验证通过但下游两段未解锁` | `portfolio_tracking_error 与 adjusted_position_weight 需先解除 not_output_passed` | `ready 需下游两段先解锁` | `N/A` | `条件写得偏结果化，存在循环解释风险` |
| `GLM` | `valid` | `conditional` | `最小集成验证已过但下游仍锁` | `需确认下游锁定原因是否已实质收缩` | `仍处降级风险且协方差未闭合` | `N/A` | `方向与 GPT 接近，但条件稍泛` |
| `Qwen` | `weak_valid` | `conditional` | `仍需验证下游输出段` | `下游输出段通过验证` | `下游未完全解锁` | `N/A` | `格式合格，但条件过粗，解释力较弱` |

## 二、逐模型记录

### 1. `GPT`

- 有效性：
  - `valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `剩余锁定原因可再收缩但未裁定`
- 第 3 行唯一锁定原因 / 条件：
  - `唯一剩余锁定原因需收敛为单条正式边界`
- 第 4 行禁止误写提醒：
  - `仍未到 risk_model_ready`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `是当前最稳主模式票之一`

### 2. `DeepSeek`

- 有效性：
  - `valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `下游 no 票已压实锁定原因`
- 第 3 行唯一锁定原因 / 条件：
  - `需补 downstream 最小解锁条件书面定义`
- 第 4 行禁止误写提醒：
  - `本体未实跑，仍缺失败模式验证`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `与 GPT 方向一致`
  - `偏向把剩余锁定原因书面定义化`

### 3. `Kimi`

- 有效性：
  - `weak_valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `集成验证通过但下游两段未解锁`
- 第 3 行唯一锁定原因 / 条件：
  - `portfolio_tracking_error 与 adjusted_position_weight 需先解除 not_output_passed`
- 第 4 行禁止误写提醒：
  - `ready 需下游两段先解锁`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `条件写得偏结果化`
  - `存在循环解释风险`

### 4. `GLM`

- 有效性：
  - `valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `最小集成验证已过但下游仍锁`
- 第 3 行唯一锁定原因 / 条件：
  - `需确认下游锁定原因是否已实质收缩`
- 第 4 行禁止误写提醒：
  - `仍处降级风险且协方差未闭合`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `方向与 GPT 接近`
  - `但条件表述稍泛`

### 5. `Qwen`

- 有效性：
  - `weak_valid`
- 第 1 行结论：
  - `conditional`
- 第 2 行一句话原因：
  - `仍需验证下游输出段`
- 第 3 行唯一锁定原因 / 条件：
  - `下游输出段通过验证`
- 第 4 行禁止误写提醒：
  - `下游未完全解锁`
- 第 5 行唯一焦点：
  - `N/A`
- 备注：
  - `格式合格`
  - `但条件过粗，解释力较弱`

## 三、主负责人预留

- 当前有效票面：
  - `GPT = conditional`
  - `DeepSeek = conditional`
  - `GLM = conditional`
  - `Kimi = weak conditional`
  - `Qwen = weak conditional`
- 当前正式裁决：
  - `继续维持 ready_judgement_conditional__downstream_still_locked`
  - `当前题正式裁为 conditional`
- 当前为什么选这个：
  - `本轮没有 yes 或 no 票分歧，票面集中为 conditional`
  - `最稳共同方向是：剩余锁定原因还能继续收缩，但还未收敛成单条正式边界`
- 当前为什么不选另外几个：
  - `不直接写成 yes，因为没有一票支持 ready 或解锁`
  - `不直接写成 no，因为多数票承认锁定原因可再正式收缩`
  - `不采 Kimi 为主模式，因为其条件写法存在循环解释风险`
- 当前先做什么：
  - `把唯一附加条件冻结为：唯一剩余锁定原因需收敛为单条正式边界`
  - `起下一轮只问“单条正式边界到底是哪一条”的超窄发包`
- 当前暂缓什么：
  - `直接改写 covariance_model_id 正式状态名`
  - `直接宣称下游已解锁`
