# A5 FV gate window consistency v0 后续方向 多家AI回收记录模板

更新时间：2026-07-19

## 一、回收总表

| 模型 | 有效性 | 保守方案 | 平衡方案 | 激进方案 | 最推荐方案 | 核心理由 | 主要风险 | NEED_EVIDENCE | 当前最小下一步 | 停止规则 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `GPT` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` |
| `DeepSeek` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` |
| `Kimi` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` |
| `GLM` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` |
| `Qwen` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` | `待回填` |

## 二、单模型吸收

### 1. `GPT`

- 有效性：
  - `待回填`
- 保守方案：
  - `待回填`
- 平衡方案：
  - `待回填`
- 激进方案：
  - `待回填`
- 最推荐方案：
  - `待回填`
- 核心理由：
  - `待回填`
- 主要风险：
  - `待回填`
- `NEED_EVIDENCE`：
  - `待回填`
- 当前最小下一步：
  - `待回填`
- 停止规则：
  - `待回填`

### 2. `DeepSeek`

- 有效性：
  - `待回填`
- 保守方案：
  - `待回填`
- 平衡方案：
  - `待回填`
- 激进方案：
  - `待回填`
- 最推荐方案：
  - `待回填`
- 核心理由：
  - `待回填`
- 主要风险：
  - `待回填`
- `NEED_EVIDENCE`：
  - `待回填`
- 当前最小下一步：
  - `待回填`
- 停止规则：
  - `待回填`

### 3. `Kimi`

- 有效性：
  - `待回填`
- 保守方案：
  - `待回填`
- 平衡方案：
  - `待回填`
- 激进方案：
  - `待回填`
- 最推荐方案：
  - `待回填`
- 核心理由：
  - `待回填`
- 主要风险：
  - `待回填`
- `NEED_EVIDENCE`：
  - `待回填`
- 当前最小下一步：
  - `待回填`
- 停止规则：
  - `待回填`

### 4. `GLM`

- 有效性：
  - `待回填`
- 保守方案：
  - `待回填`
- 平衡方案：
  - `待回填`
- 激进方案：
  - `待回填`
- 最推荐方案：
  - `待回填`
- 核心理由：
  - `待回填`
- 主要风险：
  - `待回填`
- `NEED_EVIDENCE`：
  - `待回填`
- 当前最小下一步：
  - `待回填`
- 停止规则：
  - `待回填`

### 5. `Qwen`

- 有效性：
  - `待回填`
- 保守方案：
  - `待回填`
- 平衡方案：
  - `待回填`
- 激进方案：
  - `待回填`
- 最推荐方案：
  - `待回填`
- 核心理由：
  - `待回填`
- 主要风险：
  - `待回填`
- `NEED_EVIDENCE`：
  - `待回填`
- 当前最小下一步：
  - `待回填`
- 停止规则：
  - `待回填`

## 三、主负责人裁决

- 当前有效票面：
  - `待回填`
- 当前正式裁决：
  - `待回填`
- 当前为什么选这个：
  - `待回填`
- 当前为什么不选另外几个：
  - `待回填`
- 当前先做什么：
  - `待回填`
- 当前暂缓什么：
  - `待回填`
