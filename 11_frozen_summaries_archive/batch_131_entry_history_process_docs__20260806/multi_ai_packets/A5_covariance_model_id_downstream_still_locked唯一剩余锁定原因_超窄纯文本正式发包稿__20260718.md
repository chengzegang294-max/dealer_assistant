# A5 covariance_model_id downstream_still_locked 唯一剩余锁定原因 超窄纯文本正式发包稿

更新时间：2026-07-18

【TEXT PAYLOAD START】
你现在只能回答一个新题。你只能原样输出 5 行，不得多于 5 行，不得少于 5 行，不得写标题，不得写解释，不得写 Markdown。

【旧题（已裁定，禁止再答）】
1. `covariance_model_id` 是否已能写成 `ready`
   已裁定：不能，当前仅为 `ready_judgement_conditional__downstream_still_locked`
2. 当前是否已足以把 `downstream_still_locked` 的剩余锁定原因再正式收缩一档
   已裁定：CONDITIONAL
   唯一附加条件已冻结：`唯一剩余锁定原因需收敛为单条正式边界`
3. `portfolio_tracking_error` 与 `adjusted_position_weight` 是否已自动解锁
   不是本轮题目，禁止回答

【新题（你必须回答的唯一问题）】
在以下前提下：
`covariance_model_id = ready_judgement_conditional__downstream_still_locked`
`covariance_model_id` 最小集成验证执行已通过
`target_weight = verified_with_degraded_risk__upstream_min_integration_verified__not_output_passed`
`portfolio_tracking_error = pass_conditions_frozen__not_output_passed`
`portfolio_tracking_error frozen 后续升级判断 = no`
`adjusted_position_weight = pass_conditions_frozen__not_output_passed`
`risk_mode = degraded_risk_handling`

`downstream_still_locked` 的唯一剩余锁定原因，最稳正式写法到底是哪一条？

你只能在三个答案里选一个：
A. `下游最小解锁条件尚未书面定义`
B. `下游单段仍未形成可解除 not_output_passed 的正式边界`
C. `以上两者都不对，应改写为：<你给出的唯一更稳单条正式边界，不超过 18 个字>`

【强制输出合同】
第 1 行只能是以下三种之一：
1) A
1) B
1) C

第 2 行：
2) <不超过 20 个字的一句话原因>

第 3 行：
如果第 1 行是 `A` 或 `B`，这里重复该选项原文
如果第 1 行是 `C`，这里写你唯一更稳的正式边界，不超过 18 个字

第 4 行：
4) <为什么仍不能写成 ready，不超过 20 个字>

第 5 行：
5) N/A

【无效判定】
出现以下任一情况，直接判无效：
1. 回答 `portfolio_tracking_error`
2. 回答 `adjusted_position_weight`
3. 重新讨论 `covariance_model_id` 是否已 ready 这个旧题
4. 不在 `A / B / C` 三选一里选
5. 不是刚好 5 行
6. 任意一行不以 `1)` `2)` `3)` `4)` `5)` 开头
7. 同时给出两个及以上原因
8. 把题目泛化成继续观察、继续验证、继续推进而不冻结单条正式边界

【禁止展开】
不要讨论回测
不要讨论最终组合实现
不要讨论输出段是否自动解锁
不要讨论 `ready` 旧题
【TEXT PAYLOAD END】
