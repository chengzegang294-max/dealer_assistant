# A5 技术指标外部成熟体系辅助输入 仓内参考体系优先 多家AI正式发包稿

更新时间：2026-07-25

你现在参与的是一个多AI讨论，
不是自由闲聊，
也不是金融真值裁判。

你现在要回答的唯一主问题是：
- 当前
  `技术指标通用性分析与使用场景细化`
  主线在第二轮复核后仍未通过门槛，
  现在应先从
  `NFTRADEZ / Smile_SMC / TK`
  这三个仓内参考体系里，
  借哪一层、
  借几个，
  来作为
  `辅助案例输入`
  最稳。

## 一、已知前提

当前已经有：
- `第一批场景矩阵 v0`
- `仓位层第一轮讨论页`
- `退出层与失效方式第一轮讨论页`
- `失效方式与波动风险约束第一轮内部分类页`
- 第二轮门槛复核裁决页

当前已知第二轮结论为：
- `2`
  个
  `yes_can_continue`
- `3`
  个
  `conditional`
- 主负责人已正式裁：
  - 当前仍未通过进入门槛
  - 当前正式触发
    `外部成熟体系辅助输入`
    支线
  - 但先只看仓内三条参考体系：
    - `NFTRADEZ`
    - `Smile_SMC`
    - `TK`

## 二、你必须先回答的门槛题

你必须先在以下三者里只选一个：

1. `borrow_nftradez_first`
2. `borrow_smile_or_tk_first`
3. `borrow_minimal_combo_first`

## 三、当前真正要补的缺口是什么

当前真正要补的不是：
- 指标名字
- 页面层
- 回测层

当前更像要补的是：
- `风险约束语言`
- `失效样式命名`
- `explain / review 结构`
- `例外 / 降级判据`

## 四、如果你判断最稳是组合借用，必须继续回答

请继续明确：
1. 组合只允许：
   - `NFTRADEZ + Smile_SMC`
   - 或
     `NFTRADEZ + TK`
2. 不能把三者全部一股脑都拉进来
3. 必须写清：
   - 各借哪一层
   - 为什么这就是最小组合

## 五、本轮不允许展开什么

不允许展开：
- 页面怎么改
- 直接开什么回测
- 哪个体系能直接赚钱
- 把参考体系整体并入主线本体
- 把任何结论写成已证明金融有效或可直接交易

若触到这些区域，
必须写：
- `still_need_evidence`

## 六、输出格式要求

请至少给出三个方案：

### 方案 A：先只借 `NFTRADEZ` 一层
- 核心思路
- 适用条件
- 优点
- 缺点
- 风险
- `still_need_evidence`

### 方案 B：先只借 `Smile_SMC` 或 `TK` 的一层
- 核心思路
- 适用条件
- 优点
- 缺点
- 风险
- `still_need_evidence`

### 方案 C：借一个最小组合
- 核心思路
- 适用条件
- 优点
- 缺点
- 风险
- `still_need_evidence`

## 七、必须补的判断

你必须额外明确回答：
1. 你的结论：
   - `borrow_nftradez_first / borrow_smile_or_tk_first / borrow_minimal_combo_first`
2. 你最推荐哪个方案
3. 不推荐另外两个方案的原因
4. 你推荐先借到哪一层：
   - `风险约束语言`
   - `失效样式命名`
   - `explain / review 结构`
   - `例外 / 降级判据`
5. 一句话写出：
   - `当前主线最小下一步是什么`

## 八、硬限制

禁止把任何结论写成：
- 已证明能赚钱
- 已证明金融有效
- 可直接作为交易门禁
- 可直接进入执行级回测
- 可直接把参考体系平移成当前主策略
