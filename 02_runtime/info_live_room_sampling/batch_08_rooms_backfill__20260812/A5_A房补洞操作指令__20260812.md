# A 房 Raw 补洞 · 操作指令（只干这 7 间）

更新：2026-08-12  
目标：下列 7 间 A 房各至少 1 份 incremental JSON（建议 `"fresh"`）  
中转篮：  
`d:\Stock\dealer_assistant\02_runtime\info_live_room_sampling\batch_08_rooms_backfill__20260812\00_raw\`

**本阶段先不做：** B 房、盯梢、NOTES 吸收。

**不要手建 rooms 文件夹。** JSON 先进中转篮。

---

## ① 准备（只做 1 次）

### 1.1 打开两个路径

| 用途 | 路径 |
|------|------|
| 脚本 | `d:\Stock\dealer_assistant\20_tools_workspace\batch_07_info_live_room_tools\` |
| 丢 JSON | `d:\Stock\dealer_assistant\02_runtime\info_live_room_sampling\batch_08_rooms_backfill__20260812\00_raw\` |

需要用的 JS（Windows 资源管理器打开后用记事本/Cursor 全选复制）：

1. `live_info_current_page_export_v1.js`（v1.6）— 装快捷 + 可选当前页  
2. `live_info_incremental_export_v1.js`（v1.4）— **每房主导出**

### 1.2 浏览器

1. Chrome / Edge（**不是微信内嵌**）  
2. 地址：`https://mx2025.hhhuu.com/#/`  
3. 登录成功  
4. `F12` → **Console** 面板  
5. 如提示粘贴限制：先输入 `allow pasting` 回车  

### 1.3 装快捷（整晚只做 1 次）

1. 打开：`live_info_current_page_export_v1.js` → **Ctrl+A → Ctrl+C**  
2. Console → **Ctrl+V 回车**  
3. 应看到类似：`live_info_current_page_export_v1.6 roster ready`  
4. 可忽略自动下载的那份当前页（或删掉）

若粘贴被 CSP/截断：用 `Ctrl+A` 全选文件，确认不是只复制了半屏。

---

## ② 每个 A 房固定 7 步（复制节奏）

对下面表格 **从上到下** 一间一间做：

| # | 正式房名 | Console 专用指令 |
|---|----------|------------------|
| 1 | 复盘哥 | `__infoLiveApplyIncrementalForRoom("复盘哥", "fresh")` |
| 2 | 独家老师5号 | `__infoLiveApplyIncrementalForRoom("独家老师5号", "fresh")` |
| 3 | 独家短线老师6号 | `__infoLiveApplyIncrementalForRoom("独家短线老师6号", "fresh")` |
| 4 | 梅森 | `__infoLiveApplyIncrementalForRoom("梅森", "fresh")` |
| 5 | 顺势而为 | `__infoLiveApplyIncrementalForRoom("顺势而为", "fresh")` |
| 6 | 混江龙 | `__infoLiveApplyIncrementalForRoom("混江龙", "fresh")` |
| 7 | 天赢居 | `__infoLiveApplyIncrementalForRoom("天赢居", "fresh")` |

**单房流程：**

1. 左侧点进该房（名字与表里一模一样）  
2. 点 **历史记录**  
3. 切 **倒序观看**（最新在底，往上滚历史）  
4. Console 执行「专用指令」那一行 → 应打印 `options set`  
5. 打开 `live_info_incremental_export_v1.js` → **Ctrl+A → Ctrl+C**  
6. Console **Ctrl+V 回车**  
7. 看日志：  
   - 有滚动、条数上涨  
   - Console 打印版本 `live_info_incremental_export_v1.4`  
   - 直到 `scroll_end` / 多轮无新 / 达 max_rounds  
8. 浏览器会下载 `info_live_incremental_export__YYYYMMDD_HHMMSS.json`  
9. 把该文件 **剪切/复制** 到中转篮 `00_raw\`  
10. 勾掉一房，做下一间  

**口诀：先房内设置 → 再粘整份 incremental.js。** 只改 Options 不重粘脚本 = **不会重新导出**。

---

## ③ 复盘哥 · 全文示范（第一间照着点）

```
1) 点「复盘哥」
2) 历史记录 → 倒序观看
3) Console 粘贴并回车：
```

```js
__infoLiveApplyIncrementalForRoom("复盘哥", "fresh")
```

```
4) 整文件粘贴：live_info_incremental_export_v1.js（Ctrl+A 复制文件全文）
5) 等它滚完并下载 JSON
6) 文件挪到：
   d:\Stock\dealer_assistant\02_runtime\info_live_room_sampling\batch_08_rooms_backfill__20260812\00_raw\
```

若 Console 报 `__infoLiveApplyIncrementalForRoom is not defined`：  
再粘贴一次 `live_info_current_page_export_v1.js` **或** 直接粘一次 `live_info_incremental_export_v1.js` 装快捷，然后 **重做第 3) 再粘 incremental**。

---

## ④ 七房速贴表（专用指令已写死）

### 1 复盘哥

```js
__infoLiveApplyIncrementalForRoom("复盘哥", "fresh")
```
→ 再粘贴 `live_info_incremental_export_v1.js`

### 2 独家老师5号

```js
__infoLiveApplyIncrementalForRoom("独家老师5号", "fresh")
```
→ 再粘贴 incremental

### 3 独家短线老师6号

```js
__infoLiveApplyIncrementalForRoom("独家短线老师6号", "fresh")
```
→ 再粘贴 incremental

### 4 梅森

```js
__infoLiveApplyIncrementalForRoom("梅森", "fresh")
```
→ 再粘贴 incremental

### 5 顺势而为

```js
__infoLiveApplyIncrementalForRoom("顺势而为", "fresh")
```
→ 再粘贴 incremental

### 6 混江龙

```js
__infoLiveApplyIncrementalForRoom("混江龙", "fresh")
```
→ 再粘贴 incremental

### 7 天赢居

```js
__infoLiveApplyIncrementalForRoom("天赢居", "fresh")
```
→ 再粘贴 incremental

---

## ⑤ 可选：当前页锚点（非必须）

想先留一页真值再增量时：

```js
__infoLiveApplyCurrentPageForRoom("复盘哥")
```
→ 再粘贴 `live_info_current_page_export_v1.js`  
→ 再跑上面的 `"fresh"` 增量

---

## ⑥ 进度勾

- [ ] 复盘哥  
- [ ] 独家老师5号  
- [ ] 独家短线老师6号  
- [ ] 梅森  
- [ ] 顺势而为  
- [ ] 混江龙  
- [ ] 天赢居  

中转篮里应至少有 **7 个** `info_live_*export*.json`（可多，别少）。

---

## ⑦ 做完后对我说（复制整段）

```text
A房 Raw 已入档。
中转路径：d:\Stock\dealer_assistant\02_runtime\info_live_room_sampling\batch_08_rooms_backfill__20260812\00_raw
请 migrate --apply + 刷新 SENTINEL，回报 A7 各房 Raw 数。
```

我执行入库。**你现在不用跑 Python。**

---

## ⑧ 故障速查

| 现象 | 处理 |
|------|------|
| 无滚动 | 确认在「历史记录」区、倒序；刷新后重跑 `"fresh"` |
| room 名漂到别的房 | 指令里已 forced；仍漂则看下载 JSON 的 `room_anchor` 是否目标名 |
| 粘贴太长被吃 | 换 Edge；或 Console 右键禁用截断；文件必须 **全文** |
| 下不动 JSON | 浏览器允许下载；看是否被拦 |
| 登录掉线 | 重新登录后从当前未完成的那一间继续 |

---

## ⑨ JS 绝对路径（复制到资源管理器地址栏）

```
d:\Stock\dealer_assistant\20_tools_workspace\batch_07_info_live_room_tools\live_info_current_page_export_v1.js
d:\Stock\dealer_assistant\20_tools_workspace\batch_07_info_live_room_tools\live_info_incremental_export_v1.js
```

站点：

```
https://mx2025.hhhuu.com/#/
```

中转篮：

```
d:\Stock\dealer_assistant\02_runtime\info_live_room_sampling\batch_08_rooms_backfill__20260812\00_raw
```
