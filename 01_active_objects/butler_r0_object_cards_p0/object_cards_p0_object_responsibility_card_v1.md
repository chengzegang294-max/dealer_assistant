# Object Cards P0 责任卡 v1

## 对象范围

- 本对象包负责“12 张对象卡”的内部重做与落地，目标是可跑可验收。

## 输出（必须交付）

- 每张对象卡至少交付：
  - 字段合同（输入/输出/依赖数据/降级策略）
  - 最小验收样本（输入样本 + 期望输出）
  - 可复现实跑入口（命令/脚本）

## 禁止事项

- 不把 Kimi 原文直接当作最终合同层（只能作参考与来源证据）。
- 不把不可复现的叙事/心法写进对象卡硬逻辑。
- 不在未验收前把对象卡接入投票池或对外输出“结论型建议”。

## 验收口径（最小）

- 可复现：同输入同输出；记录参数与数据版本（hash）。
- 诚实性：避免未来信息泄露；样本外/稳健性检查有明确门槛与失败处理。
- 降级：缺数据必须降级，不允许 silent failure。

## 依赖

- 来源库：
  - `10_source_library_archive/batch_110_external_folder_absorb__20260708/`
- 日常入口合同层：
  - `00_entry/DIRECTORY_ROLE_CONTRACT_v1.md`

