# Cross-Line Frozen super27 链路索引（mirror）

更新时间：2026-07-07

- ARCHIVE_ONLY: 本文件是旧链路 `cross_line_frozen` 的短命名镜像入口，不作为默认入口。
- 旧链路原位文件：见 `cross_line_frozen_super27_legacy_file_map_v1.tsv`（step=`chain_index`）

## 维护态抽查要点

- `candidate_ids = ["RSJ_STATE_P0", "PV_CORR_STATE_P0"]`
- `write_attempted = false`
