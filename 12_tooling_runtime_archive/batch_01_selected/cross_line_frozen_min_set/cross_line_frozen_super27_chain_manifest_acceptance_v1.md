# Cross-Line Frozen super27 Chain Manifest Acceptance (mirror)

更新时间：2026-07-07

- ARCHIVE_ONLY: 本文件是旧链路 `cross_line_frozen` 的短命名镜像入口，不作为默认入口。
- 旧链路原位文件：见 `cross_line_frozen_super27_legacy_file_map_v1.tsv`（step=`chain_manifest_acceptance`）

## 维护态抽查要点

- `rows_match = true`
- `write_attempted = false`
