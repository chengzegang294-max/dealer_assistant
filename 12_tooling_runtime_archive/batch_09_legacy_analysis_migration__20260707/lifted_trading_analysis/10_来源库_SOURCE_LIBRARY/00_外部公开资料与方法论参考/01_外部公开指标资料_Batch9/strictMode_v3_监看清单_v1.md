# strictMode v3 监看清单 v1

## 作用

- 用于明确说明：当前“继续盯 `strictMode v3`”到底在盯什么。
- 防止后续把“监看”误解成泛泛等回帖，而没有固定要吸收的判断项。

## 当前背景

- `strictMode v3` 问题包已经创建：
  - `batch9_sources_kimi\N01\strictMode_kimi_followup\OUTBOUND__strictMode_mechanism_v3.md`
- 这里说的“回帖”不是系统自动收到谁的消息。
- 当前准确含义是：
  - 若用户把外部 AI（例如 Kimi/豆包/其他已在跑的讨论对象）的回复贴回原问题包
  - 我再把新增判断并回 `N01` 主线文档
- 当前已冻结前提：
  - `strictMode != close-overlap sensitivity`
  - `close-overlap = feature-level sensitivity`
  - `strictMode = policy-level gating / qualify strictness`
  - `close-overlap(feature sensitivity) + strictMode(policy gating) -> shared pocket qualify -> contained pocket -> Mature`

## 当前真正要盯的最小缺口

- 当前不是再去问：
  - `strictMode` 是否存在
  - `close-overlap` 是否独立
  - `strictMode` 是否更像 gating
- 这些已经冻结。
- 当前真正要继续盯的是：
  - `strictMode` 更像 `threshold shift`
  - 还是更像 `additional qualify conditions`

## 一旦有 v3 回帖，要优先吸收的 4 个判断

### 1. 先看 Q1

- 目标：
  - 判断当前更优先是：
    - `A = threshold shift`
    - `B = additional qualify conditions`
    - `C = score formula / weights rewrite`
    - `D = NEED_EVIDENCE`
- 当前最想要的新信息：
  - 是否终于能把 `A` 与 `B` 的先后顺序再压窄一层

### 2. 再看 Q2

- 目标：
  - 确认最不优先机制是否仍是：
    - `score formula / weights rewrite`
- 若 v3 回帖改变这一点：
  - 需要立刻回查当前 N01 notes/acceptance/gaps 是否要修口径

### 3. 再看 Q3

- 目标：
  - 确认最稳保守写法是否仍是：
    - `strictMode` 更像 `pocket/state qualify strictness`
    - 且上游于 `Mature`
- 若外部回帖开始把它推到：
  - `Action labels`
  - 或 `weights rewrite`
  则需要判为冲突意见，不直接吸收

### 4. 最后看 bullets 里的证据句

- 只接受基于现有 evidence pack 的新收窄。
- 重点看它是否能把下面任一条说得更窄：
  - `threshold shift` 是否更像直接抬 `compressionThreshold`
  - 是否还会连带更严 `matureThreshold`
  - `additional qualify conditions` 是否更像：
    - `noise/containment confirm`
    - `pocket qualify`
    - 或 `contained pocket` 前置过滤

## 当前不应被 v3 回帖带偏的方向

- 不应因为新回帖就倒推：
  - `releaseUp / releaseDown` 是前置条件
- 不应因为新回帖就直接写成：
  - `strictMode` 改四项权重
  - `strictMode` 改总分公式
- 不应把模糊措辞直接升级成源码等价

## 一旦有回帖，回写顺序

- 第一层：
  - `n01_p0_runtime_notes_v1.md`
- 第二层：
  - `n01_p0_runtime_append_acceptance_v1.md`
  - `n01_p0_runtime_gaps_v1.md`
- 第三层：
  - `REOPEN_B9_N01_VOL_STATE_P0_批次推进记录_v1.md`
  - `03_阶段二_当下计划_执行清单.md`
  - `关于日活.md`

## 当前无回帖时的冻结口径

- 当前最小缺口继续写成：
  - `threshold shift`
  - vs `additional qualify conditions`
- 当前继续保持：
  - `score formula / weights rewrite` 最不优先
- 当前继续不写死：
  - `threshold shift` 与 `additional qualify conditions` 的先后顺序
  - 二者是否并用
