你现在处理的是 `NFTRADEZ` 第一轮“概念词典收缩”，不是做交易建议，不是做胜率宣传，也不是自由发挥。

请只处理这 6 个对象：
- `NFTZ_G001`
- `NFTZ_G002`
- `NFTZ_G003`
- `NFTZ_G004`
- `NFTZ_G005`
- `NFTZ_G006`

本轮任务：
1. 把每个对象收成一个“最小概念词典条目”。
2. 每个条目必须区分：
   - `plain_definition`
   - `core_mechanics`
   - `must_have_conditions`
   - `invalid_when`
   - `confusable_with`
   - `best_role`
3. 不要把概念写成策略，不要补没有证据的细节。

输出要求：
1. 每个对象保留 `object_id`。
2. 每个对象最多 `8` 行核心要点，优先收“最小可复用定义”。
3. 必须显式写出“不能怎么用”，避免后续误量化。
4. 若正文只支持“偏见工具 / 解释层”，就直接写出来，不要强行升级。

特别边界：
- `NFTZ_G002 / G005`：必须保留“收盘确认 / 流动性清扫 / 位移 / 50%中点”这些边界。
- `NFTZ_G006`：必须明确 `SMT` 只是偏见工具，不是直接入场信号。
- `NFTZ_G004`：必须明确孤立 `FVG` 全是噪音，必须有背景叙事。

最后请附一段：
- `glossary_crosswalk`
  - 说明这 6 个对象之间最自然的学习顺序
  - 说明哪些概念容易混淆
