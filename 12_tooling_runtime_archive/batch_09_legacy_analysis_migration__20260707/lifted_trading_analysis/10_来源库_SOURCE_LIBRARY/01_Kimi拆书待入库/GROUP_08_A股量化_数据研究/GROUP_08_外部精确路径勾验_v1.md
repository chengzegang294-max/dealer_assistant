# GROUP_08 外部精确路径勾验 v1

更新时间：2026-06-22

## 本轮做了什么

- 已对 `D:\Stock\cut_file\S\03_券商研报` 的真实 `pdf` 文件名做一次自动化规范化比对。
- 比对输入：
  - `GROUP_08_逐文件删除勾验行_v1.md` 的 `60` 条标题锚点
  - `D:\Stock\cut_file\S\03_券商研报` 下 `477` 份 `pdf`
- 结果已落盘到：
  - `GROUP_08_外部精确路径勾验_v1.tsv`

## 本轮硬结论

- `60` 条标题锚点里：
  - `1` 条 `EXACT_LIKE_MATCH`
  - `59` 条 `NO_MATCH`
- 当前唯一直接类匹配到的例子：
  - `S-009`
  - 标题锚点：`极值视角下的多因子选股策略`
  - 外部路径：`D:\Stock\cut_file\S\03_券商研报\04_多因子\海通选股因子系列研究6：极值视角下的多因子选股策略.pdf`

## 2026-06-22 修正

- 这张表仍然只负责：
  - `D:\Stock\cut_file\S\03_券商研报` 这条旧外部树的精确路径勾验
- 但 `research pdf` 的原始承载父目录现在已经另行确认到：
  - `D:\Stock\cut_file\《Python股票量化交易从入门到实践》完整版\2.其他量化资料(62份)（赠品）`
- 因而从本轮开始，应明确区分两种外部角色：
  - `S\03_券商研报`：旧外部候选树，用于历史系列号/主题锚点补查
  - `《Python股票量化交易从入门到实践》完整版\2.其他量化资料(62份)（赠品）`：当前 `61/62` 归属审计与原始 pdf 回溯的直接真值目录
- 特别是：
  - `S-014`
  - `S-027`
  当前已经在后者拿到精确原始 `pdf` 路径，不再属于“只能在 `S` 树里继续盲找原件”的状态。
- 因而从这轮开始，应把它们视为：
  - `BOOKDIR_EXACT_PATH_CONFIRMED`
  - 后续主要工作转为前后路径台账与最终删除勾选，而不是继续扫 `S` 树找源。

## 2026-06-22 后续推进：bookdir 全量精确路径确认

- 通过对 `《Python股票量化交易从入门到实践》完整版\2.其他量化资料(62份)（赠品）` 进行“标题锚点 = 文件名 stem 规范化一致”的自动确认：
  - 已把 `GROUP_08` 的 `60` 条标题锚点全部推进到“外部精确路径 + staging 副本 + 台账落盘”
  - `GROUP_08_前后路径台账_v1.tsv` 当前 `WAIT_MANUAL_PATH=0`
- 当前外部侧路径确认状态分布（以台账为准）：
  - `51` 条 `BOOKDIR_EXACT_PATH_CONFIRMED`
  - `5` 条 `SERIES_MANUAL_CONFIRMED`
  - `1` 条 `EXACT_LIKE_MATCH`
  - `1` 条 `TOPIC_MANUAL_CONFIRMED`
  - `1` 条 `SHARED_SERIES_SOURCE_CONFIRMED`
  - `1` 条 `SHARED_BOOKDIR_SOURCE_CONFIRMED`
- 其中唯一的 bookdir 共享源歧义：
  - `A-009` 在 `行业轮动模型/风格轮动模型` 下存在两个物理副本
  - 当前已裁定为 `SHARED_BOOKDIR_SOURCE_CONFIRMED`，只保留一份 staging 副本
  - 歧义候选记录：`GROUP_08_bookdir_ambiguous_candidates_v1.tsv`

## 这说明什么

- 现在不能按“标题锚点 = 外部文件名”直接搬。
- 原因不是路径调不动，而是：
  - 当前 `research pdf` 清单里的标题锚点，和外部原件文件名大多不是同一命名口径
  - 外部原件常带：
    - 日期
    - 券商名
    - 系列号
    - 完整副标题
  - 而当前 `GROUP_08` 清单很多是吸收后的短标题或主题标题
- 所以如果现在直接批量移动外部文件：
  - 漏搬概率高
  - 错搬概率高
  - 后面按文件夹回查时会很痛苦

## 第二批人工系列确认

- 在自动匹配只命中 `1` 条之后，本轮又基于：
  - `S_BUCKET_INDEX__2026-06-17.tsv`
  - `海通选股因子系列研究1-6` 的系列号闭环
  - `GROUP_08 research pdf` 的 `S-037 ~ S-041 + S-009`
  做了一轮人工系列确认。
- 本轮新增确认：
  - `S-037 -> 海通选股因子系列研究1`
  - `S-038 -> 海通选股因子系列研究2`
  - `S-039 -> 海通选股因子系列研究3`
  - `S-040 -> 海通选股因子系列研究4`
  - `S-041 -> 海通选股因子系列研究5`
- 这 `5` 条当前已写回：
  - `GROUP_08_外部精确路径勾验_v1.tsv`
  - `GROUP_08_前后路径台账_v1.tsv`
  - `00_external_import_staging\confirmed_series\04_多因子\`
- 因而当前状态已从：
  - `1` 条可复制
  变为：
  - `1` 条 `EXACT_LIKE_MATCH`
  - `5` 条 `SERIES_MANUAL_CONFIRMED`
  - 合计 `6` 条已进入 staging copy
  - 剩余 `54` 条继续等待精确路径补齐

## 第三批主题确认与共享源确认

- 在前两批之后，本轮只补两类高置信条目：
  - `S-003`：共享已确认海通系列源
  - `S-010`：主题确认后新增复制
- 本轮新增确认：
  - `S-003 -> 海通选股因子系列研究2：因子模型的尾部相关性研究`
    - 依据：`A股市场特征研究（一）——沪深300样本股尾部相关性观察` 与已确认的 `选股因子研究系列（二）——因子模型的尾部相关性研究` 共用同一尾部相关性主轴
    - 当前状态：`SHARED_SERIES_SOURCE_CONFIRMED`
    - 当前处理：复用 `confirmed_series` 已有副本，不重复复制
  - `S-010 -> 海通选股因子系列研究21：分析师一致预期相关因子`
    - 依据：仓库内 `S-010` 正文反复以“一致预期评级 / 一致预期PE / 一致预期EPS变动”作为核心对象
    - 当前状态：`TOPIC_MANUAL_CONFIRMED`
    - 当前处理：已新增复制到 `confirmed_topic`
- 因而当前状态又进一步变为：
  - `1` 条 `EXACT_LIKE_MATCH`
  - `5` 条 `SERIES_MANUAL_CONFIRMED`
  - `1` 条 `SHARED_SERIES_SOURCE_CONFIRMED`
  - `1` 条 `TOPIC_MANUAL_CONFIRMED`
  - 合计 `8` 条已完成路径确认
  - 其中真实 staging 副本累计 `7` 份
  - 剩余 `52` 条继续等待精确路径补齐

## 第四批放宽：主题锚点登记层

- 本轮在继续收窄第四批时发现：
  - `S-005 / S-012 / S-014 / S-015 / S-027` 里，暂时仍没有新的“可直接写入删除台账”的外部原件路径
  - 但其中有一小批已经具备“共享已确认真值主题 / 已确认上游研究引用”的辅助锚点价值
- 为了不污染后续删源依据，本轮不把这批对象写回：
  - `GROUP_08_外部精确路径勾验_v1.tsv`
  - `GROUP_08_前后路径台账_v1.tsv`
- 改为单独新增：
  - `GROUP_08_主题锚点勾验_v1.tsv`
  - `tools\group08_batch4_topic_anchor.py`
- 这层当前只做：
  - 主题锚点登记
  - 复用已存在的 staging 副本路径
  - 保留“后续继续找真实外部原件”的证据入口
- 这层当前明确不做：
  - 不新增 `DELETE_READY`
  - 不把锚点当作原件精确路径
  - 不因为有锚点就允许删除外部原件

### 本轮新增主题锚点

- `S-005 -> METHOD_THEME_ANCHOR`
  - 主题锚点外部源：`海通选股因子系列研究6：极值视角下的多因子选股策略`
  - 依据：`S-005` 明确是“极值选股方法论落地到换手率”的单篇实证，而旧摘要已把它表述为 `S-009` 这类“极值视角多因子框架”的前置方法论
  - 当前处理：只登记到 `GROUP_08_主题锚点勾验_v1.tsv`，复用 `confirmed_exact` 中已存在的 `研究6` 副本，不新增复制
- `S-012 -> UPSTREAM_CITATION_ANCHOR`
  - 主题锚点外部源：`海通选股因子系列研究1：弱者终有逆袭日,强势几无持续时：A股市场的动量反转效应研究`
  - 依据：`S-012` 正文显式引用该研究作为“动量反转效应以及机会分析”的上游研究起点
  - 当前处理：只登记到 `GROUP_08_主题锚点勾验_v1.tsv`，复用 `confirmed_series` 中已存在的 `研究1` 副本，不新增复制

### 第四批后的当前边界

- 删除级别的外部精确路径确认数仍然保持：
  - `8` 条
- 真实 staging 副本数仍然保持：
  - `7` 份
- 新增的只是：
  - `2` 条 `TOPIC_ANCHOR_ONLY`
- 因而当前剩余删除级别待补对象仍然是：
  - `52` 条 `WAIT_MANUAL_PATH`

## 第四批主题锚点收紧复核

- 已新增：
  - `GROUP_08_第四批主题锚点收紧核查_v1.md`
- 本轮只复核两条：
  - `S-005`
  - `S-012`
- 本轮核查后得到更硬边界：
  - `S-005` 虽然与 `海通选股因子系列研究6：极值视角下的多因子选股策略` 形成明确方法论前后关系，但当前仍不能证明两者是同一外部原件
  - `S-012` 虽然正文显式引用 `海通选股因子系列研究1：弱者终有逆袭日,强势几无持续时：A股市场的动量反转效应研究`，但这只是上游引用，不足以证明它自己的原件路径
  - 外部树补查后，也没有发现 `短线反弹` / `超跌反弹` / `换手率上的实证` 这类更贴题的隐藏原件名
- 因而本轮裁决仍是：
  - `S-005 -> 保持 TOPIC_ANCHOR_ONLY`
  - `S-012 -> 保持 TOPIC_ANCHOR_ONLY`
- 本轮不更新：
  - `GROUP_08_外部精确路径勾验_v1.tsv`
  - `GROUP_08_前后路径台账_v1.tsv`
- 删除级别统计继续保持：
  - `8` 条路径确认
  - `7` 份真实 staging 副本
  - `52` 条 `WAIT_MANUAL_PATH`

## 当前更稳的执行法

- 不走“标题驱动直接移动”
- 改走“路径驱动复制入库 + 台账核对 + 后删外部”

### 具体就是三步

1. 以前路径为准
   - 以 `S_BUCKET_INDEX__2026-06-17.tsv` 里的绝对路径为真值锚点
2. 先复制，不先删
   - 先把需要保留的外部原件复制到仓库内 staging
   - 复制后记录：
     - `source_path_before`
     - `repo_staging_path_after`
     - `paper_id`
     - `copy_status`
3. 逐批检查后再删外部
   - 等仓库内引用、分组、吸收清单都过一轮检查后
   - 再把外部对应子集标成可删

## 当前对 `D:\Stock\cut_file\` 的执行建议

- 你的目标“`D:\Stock\cut_file\` 最终一定要删”是可以承接的。
- 但当前不能直接做的是：
  - 先删 `D:\Stock\cut_file\S\03_券商研报`
- 当前可以立刻开始做的是：
  - 建立前后路径台账
  - 建立 repo 内 staging 落点
  - 按 `03_券商研报` 子集先复制
  - 先吃“系列号闭环明确”的小簇，再扩到更模糊标题
  - 再吃“主题簇明确”但仍需人工确认的一条一条补齐

## 当前阶段裁决

| 对象 | 当前裁决 | 原因 |
|---|---|---|
| `D:\Stock\cut_file\S\03_券商研报` 整组 | `NOT_MOVE_BLINDLY` | 标题锚点直接对路径的成功率过低 |
| `03_券商研报` 子集复制入库 | `READY_FOR_STAGING_COPY` | 可以先复制，再靠台账检查 |
| `海通选股因子系列研究1-6` 子簇 | `SERIES_COPY_CONFIRMED` | 系列号与标题锚点已形成闭环，可先复制 |
| `尾部相关性` 共享源条目 | `SHARED_SOURCE_CONFIRMED` | 可复用已复制海通系列副本，不重复复制 |
| `分析师一致预期` 主题条目 | `TOPIC_COPY_CONFIRMED` | 主题闭环明确，可新增复制一条 |
| `D:\Stock\cut_file\` 最终删除目标 | `KEEP_AS_GOAL` | 目标保留，但执行顺序必须是复制/核对在前 |

## 下一步只剩什么

- 建 `03_券商研报` 的前后路径台账
- 建 repo 内 staging 落点
- 按路径而不是按标题，继续第三批精确路径补齐与 staging copy 放大
