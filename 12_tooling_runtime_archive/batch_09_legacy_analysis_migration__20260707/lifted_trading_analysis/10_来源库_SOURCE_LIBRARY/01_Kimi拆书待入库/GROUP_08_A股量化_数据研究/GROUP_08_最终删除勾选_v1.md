# GROUP_08 最终删除勾选 v1

更新时间：2026-06-21

## 为什么前面整理了但文件没大变

- 因为前几轮主要做的是：
  - 真值锚点固定
  - 映射关系固定
  - 删源边界固定
  - 删除验收骨架固定
- 这些步骤的目的不是立刻搬文件，而是避免：
  - 把还被别的资产线引用的外部源误删
  - 把还没完成替代产物验收的仓库内源误删
  - 以后回头发现“证据没了但结论还在”
- 所以到这一步之前，文件变化看起来会偏少，这是刻意保守，不是没做事。

## 当前阶段结论

- 现在已经从“原则整理”推进到“最终删除勾选前一跳”。
- 其中本轮已单独推进：
  - `S-014`
  - `S-027`
  到删除级别重核通过状态。
- 当前这批真正缺的，不再是：
  - 继续写标题
  - 继续补映射
  - 继续讲口径
- 当前只缺：
  - 最终删除勾选
  - 外部精确路径勾验

## 最终勾选口径

- `DELETE_READY`
  - 现在就可进入删除/移动执行
- `MOVE_READY_NOT_DELETE`
  - 可以整理、归并、移动，但还不应删除
- `WAIT_PATH_CHECK`
  - 已知可进入候选，但必须先补精确路径
- `HOLD`
  - 当前不动

## A. 仓库内侧最终删除勾选

| 对象 | 当前状态 | 最终勾选 | 说明 |
|---|---|---|---|
| `txt` 线 | `99` 条源 `txt -> md_path` 已对齐 | `DELETE_READY` | 可进入仓库内侧删源执行候选 |
| `research pdf` | 已有逐条映射 + 逐文件勾验骨架；`S-014 / S-027` 已完成删除级别重核 | `WAIT_PATH_CHECK` | 整组仍先逐条补最终删除勾选，不等于整组已可删 |
| `pdf 入门书` | 仅索引/章节卡 | `HOLD` | 当前不删不移 |
| `epub` | 仅粗切/解释层 | `HOLD` | 当前不删不移 |
| `GROUP_08_A股量化_数据研究__SOURCE_RAW\新的参考书` 整目录 | 混合了可删与不可删子线 | `HOLD` | 当前不可整目录动刀 |

## B. 外部侧最终删除勾选

| 外部对象 | 当前状态 | 最终勾选 | 说明 |
|---|---|---|---|
| `D:\Stock\cut_file\S` 整目录 | 仍服务多条资产线 | `HOLD` | 整目录不可删也不可大搬 |
| `D:\Stock\cut_file\S\01_集合竞价教程` | 被 `02_原子化拆解文件` 直接引用 | `HOLD` | 当前不能整理移动 |
| `D:\Stock\cut_file\S\02_游资悟道交割单` | 历史上已有局部删除案例 | `MOVE_READY_NOT_DELETE` | 仅限继续做低价值局部整理，不是整目录可删 |
| `D:\Stock\cut_file\S\03_券商研报` 整组 | `GROUP_08` 的主要外部真值子树 | `WAIT_PATH_CHECK` | 先补 `GROUP_08` 对应精确文件路径 |
| `D:\Stock\cut_file\S\03_券商研报` 中与 `GROUP_08 research pdf` 一一对应的子集 | 已确认是未来局部整理对象 | `MOVE_READY_NOT_DELETE` | 先可做归并/分桶/标注，不直接删 |
| `D:\Stock\cut_file\《Python股票量化交易从入门到实践》完整版\2.其他量化资料(62份)（赠品）` 中 `S-014 / S-027` 对应原件 | 已确认精确原始 `pdf` 路径 | `MOVE_READY_NOT_DELETE` | 前后路径台账已补，不直接删原件 |

## B2. S-014 / S-027 前后路径台账落地

- 已落盘到：
  - `GROUP_08_前后路径台账_v1.tsv`
- 已同时生成仓库内 staging 副本：
  - `00_external_import_staging\confirmed_bookdir\量化选股 40份\财务指标选股研究系列\上市公司动量反转以及市值因子的选股识别度.pdf`
  - `00_external_import_staging\confirmed_bookdir\量化选股 40份\选股因子研究系列\现金流量市值比因子的极值效应.pdf`

## C. 外部文件究竟哪些现在可以整理移动

- 现在就能动的，不是整个 `S`，而是：
  - `D:\Stock\cut_file\S\02_游资悟道交割单` 中已经被判低价值、低复用的局部子目录
  - `D:\Stock\cut_file\S\03_券商研报` 中与 `GROUP_08` 一一对应、且后续会补精确路径的子集
- 现在不能动的：
  - `D:\Stock\cut_file\S` 整目录
  - `D:\Stock\cut_file\S\01_集合竞价教程`
  - `D:\Stock\cut_file\S\03_券商研报` 中未完成精确路径勾验的其余部分

## D. 现在有没有在做

- 有在做，而且已经做到：
  - `GROUP_08` 逐条映射
  - 双侧删除验收
  - 逐文件删除勾验行
  - 现在这份最终删除勾选
- 但“实际外部移动/删除执行”还没开始批量做。
- 原因不是拖，而是：
  - 外部 `S` 还存在跨资产线引用
  - 不先补最终勾选，直接搬会把别的线一起砍断

## E. 下一步只剩什么

- 最顺下一跳：
  - 给 `research pdf` 补最终删除勾选（条目级清单）
  - 给 `S-014 / S-027` 补最终删除勾选（条目级清单）
  - 给 `D:\Stock\cut_file\S\03_券商研报` 对应子集继续补精确路径勾验
- 当前单独保持主链外：
  - `事件驱动策略之十——如何刻画股票热度以及寻找“潜在热门股”`
  - 先不纳入现有 `paper_id` 主链
- 做完之后，才进入真正的：
  - 外部局部整理移动
  - 仓库内局部删源执行

## E1. research pdf 条目级最终删除勾选清单

- 本轮已建立条目级清单：
  - `GROUP_08_research_pdf_最终删除勾选_逐条清单_v1.tsv`
- 当前清单仅用于：
  - 把 `S-014 / S-027` 的最终删除勾选落到可执行条目
  - 复用同一模式，优先覆盖已具备外部精确路径 + staging 副本的下一批 `paper_id`
- 当前清单不用于：
  - 触发真实删除
  - 把 `MOVE_READY_NOT_DELETE` 自动升级为 `DELETE_READY`
