# GROUP_08 第七批树外目录与旧来源层反查 v1

## 目的

- 在第六批确认“当前 `D:\Stock\cut_file\S` 真值树内没有 `S-027 / S-014` 新原件”之后，继续做两条树外反查：
  - `S-027`：查 `S` 树之外的海通旧研报目录或备份区
  - `S-014`：查旧来源目录里整组“行业内识别度”系列的原始文件名

## 本轮方法

### 对 `S-027`

- 在 `D:\Stock` 全盘范围继续查：
  - `*现金流量市值比因子的极值效应*`
  - `*现金流量市值比*`
  - `*Cfmv*`
  - `*海通*研究*.pdf`
  - `*海通*研究*.txt`
- 同时回查仓库内所有提到：
  - `海通证券研究所`
  - `海通旧研报`
  - `备份区`
  - `券商研报`
  的文档与索引，判断是否还存在未并入 `S_BUCKET_INDEX` 的树外目录。

### 对 `S-014`

- 在 `D:\Stock` 全盘范围继续查：
  - `*上市公司动量反转以及市值因子的选股识别度*`
  - `*动量反转*市值因子*选股识别度*`
  - `*行业内*识别度*`
- 同时补查：
  - `GROUP_08_A股量化_数据研究` 目录自身
  - `05_txt源码_md归档` 中遗留的 `origin_path`
  - `A股_量化选股_研究PDF_总摘要_v1_part1/part2`
- 目标是确认：
  - 旧来源目录是否还在磁盘上
  - 如果不在，是否还能从残留路径锚点反推出整组原始文件名

## 结果

### 1. `S-027` 在 `S` 树之外仍未找到同名或近名原件

- `D:\Stock` 全盘范围内没有命中：
  - `现金流量市值比因子的极值效应`
  - `现金流量市值比`
  - `Cfmv`
- 全盘范围内也没有补出新的：
  - 海通旧版 `pdf`
  - 海通旧版 `txt`
  能和 `S-027` 建立一一映射。
- 这进一步说明：
  - `S-027` 当前不是“树外还有一个同名文件只是没并入索引”
  - 更像是其原件当前确实不在本机现存的常用来源树里

### 2. `S-014` 在旧来源目录层没有补出真实目录，只剩残留路径锚点

- `D:\Stock` 全盘范围内没有命中：
  - `上市公司动量反转以及市值因子的选股识别度.txt`
  - `*动量反转*市值因子*选股识别度*.txt`
  - `*行业内*识别度*.txt`
- 仓库内与 `S-014` 相邻的标题组仍只停留在摘要和 `v2` 切片层：
  - `A股上市公司毛利率的均值回归及选股实证`
  - `上市公司估值指标的稳定性与选股识别度`
  - `上市公司动量反转以及市值因子的选股识别度`
- 没有在磁盘上补出这组文件对应的原始 `txt` 文件实体

### 3. 一个新的硬结论：文档中的 `SOURCE_RAW` 真值锚点当前并不在磁盘上

- 仓库多处文档仍把下列路径写成真值锚点：
  - `GROUP_08_A股量化_数据研究__SOURCE_RAW\新的参考书`
- 但本轮直接核对磁盘后：
  - `d:\Stock\trading_analysis\10_来源库_SOURCE_LIBRARY\01_Kimi拆书待入库\GROUP_08_A股量化_数据研究__SOURCE_RAW`
    当前不存在
- `05_txt源码_md归档` 中确实保留了大量旧 `origin_path`：
  - 说明此前这层目录存在过，且曾被用于来源归档
- 但当前真实状态是：
  - 只能通过残留 `origin_path` 文字锚点反推
  - 不能直接回到旧来源目录核原始文件名

### 4. `S-014` 这条当前只能被“旧锚点反推”，无法被“旧文件实体核对”

- 因为 `SOURCE_RAW` 目录实体不在磁盘上：
  - 本轮无法继续直接核整组“行业内识别度”原始文件名
- 目前还能保留的证据只包括：
  - `A股_量化选股_研究PDF_总摘要_v1_*`
  - `CUTPACK__...__v2.md`
  - `origin_path` 的残留文字
- 所以本轮只能把 `S-014` 的裁决收紧为：
  - 旧来源目录反查在“文件实体层”已中断
  - 后续若继续，只能去找历史备份、旧归档或已删前快照

## 当前裁决

- `S-027`
  - 树外目录与备份区补查后，仍无新原件
  - 继续保持：`NO_MATCH / WAIT_MANUAL_PATH`
- `S-014`
  - 旧来源目录的真实实体当前不存在
  - 只能依赖旧摘要与残留路径锚点
  - 继续保持：`NO_MATCH / WAIT_MANUAL_PATH`

## 对主线的影响

- 本轮没有新增删除级别精确路径
- 本轮没有更新：
  - `GROUP_08_外部精确路径勾验_v1.tsv`
  - `GROUP_08_前后路径台账_v1.tsv`
- 本轮没有新增真实 staging copy
- 删除级别统计继续保持：
  - `8` 条已完成路径确认
  - `7` 份真实 staging 副本
  - `52` 条 `WAIT_MANUAL_PATH`

## 新增边界

- 后续若继续追 `S-027`，优先级最高的不是再扫当前来源树，而是：
  - 找历史备份区
  - 找旧离线归档
  - 找已删前快照
- 后续若继续追 `S-014`，优先级最高的不是再扫当前 `GROUP_08` 目录，而是：
  - 先恢复或找到 `GROUP_08_A股量化_数据研究__SOURCE_RAW\新的参考书` 的历史副本
  - 否则只能停留在标题与摘要反推层，不能升级为删除级别路径
