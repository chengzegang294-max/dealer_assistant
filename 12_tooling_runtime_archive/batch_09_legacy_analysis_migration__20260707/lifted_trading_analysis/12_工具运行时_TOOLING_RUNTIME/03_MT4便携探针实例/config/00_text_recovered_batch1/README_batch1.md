# batch1 text recovered ini

- Only readable text ini copies are exported here. Original files stay unchanged.
- servers.ini is decoded as latin-1 and re-saved as utf-8 for inspection.
- Other exported files are normalized utf-8 readable copies.
- Private or binary-like configs are intentionally skipped.
