# AGENTS.md — dealer_assistant 项目级代理指令（每轮对话自动加载）

创建日期：2026-08-10
生效范围：d:\Stock\dealer_assistant（新仓 / 正式主线仓）

---

## 一句话目标（偏离就停）

把「**东方财富 PC 客户端为核心数据锚点的 A 股情绪、龙虎榜异动、打板策略辅助助手**」做成一条可以稳定复跑、稳定验收、持续回测的工程闭环。

**先有数据标准合同 → 再有脚本批次 → 最后才有指标/策略/UI。绝不能反。**

---

## 双仓分工红线（任何读写操作前先过）

### 6 句执行清单（每条都要过，少一条就停）

1. **当前这步是正式主线，还是缓冲/试验？**
   - 正式主线（正式文档、已收口runtime、已确认长期保留的工具代码、独立成commit的内容）→ 落本仓 `d:\Stock\dealer_assistant`
   - 试验/待裁决/临时中转 → **立刻切到** `D:\Stock\trading_assistant\90_SCRATCH_AND_TEST_ZONE\`，不许写本仓
2. **默认落新仓还是旧仓？** — 正式落新仓，试验落旧仓 90 测试区
3. **结果是否明确到可以独立提交？** — 不明确就落旧仓，别抬进新仓
4. **这份材料是否仍依赖临时路径才能成立？** — 依赖就别进新仓
5. **会不会把旧仓较旧内容反向覆盖到新仓？** — 禁止
6. **提交和推送目标仓是否明确？** — 默认只从本仓推送正式主线

### 一句话版本

> 默认在本仓工作。改文件、跑脚本、生成结果前先过上面 6 句。
> 拿不准 → 去旧仓 90_SCRATCH_AND_TEST_ZONE/ 放着，别污染新仓。

---

## 当前主线（偏离就拉回）

只推进这 4 条，其他都先放一放：

| 主线 | 现状 | 不该做的红线 |
|------|------|------------|
| 1. 直播间收口 | ✅ 评级精简阶段完成，A/B/C/D分桶落地 | 不再纠结"能不能抓" |
| 2. 连板天梯收口 | ✅ 最小抓取链跑通，字段合同冻结 | 不扩写复杂功能 |
| 3. P1 + sector 日常 | 🔄 连续记录阶段，需要每个交易日收盘后用户发开工口令 | 不急于回补历史（权限墙问题） |
| 4. 三段式私人短线工作台 | 🔄 阶段2事实预填，自动抓数据+判断格留空 | 不做实时信号流、不做自动替人判断、不做全自动盯盘 |

**Stop Words（一出现立刻叫停，拉回到当前待办）**

```
顺便优化一下      → 停！当前待办收完再说
为了完整做个重构   → 停！当前值不值？
还有第 N 层细节    → 停！先把当前第 1 层收完
再写 20 页合同     → 停！合同写1页能推进就别扩
```

---

## 文件组织风格（写任何东西前先看）

```
正式裁决/入口/合同 →  00_entry/<大专题目录>/A5_<主题>_<一句话说明>__YYYYMMDD.md
运行时（脚本输入输出）→ 02_runtime/<家族>/batch_<序号>_<一句话说明>__YYYYMMDD/
                        必须配 README.md + artifact_index
工具脚本            → 20_tools_workspace/batch_<序号>_<主题>_tools/
                        必须配 README.md
跨文档引用          → 全部用 file:/// 绝对路径格式 + #Lxx-Lxx 行锚
```

**Python 脚本纪律：**
- 不硬编码绝对路径，用相对路径或运行时传参
- 注释只写「为什么这么做」，不写「做了什么」（后者代码本身已经说明）
- 每次改 1~2 个脚本，不大拆大改

---

## 回填纪律（每次大改动后必做）

**只要属于以下任一条，必须在收尾前回填** [A5_新仓交接总记录__20260809.md](file:///d:/Stock/dealer_assistant/00_entry/A5_%E6%96%B0%E4%BB%93%E4%BA%A4%E6%8E%A5%E6%80%BB%E8%AE%B0%E5%BD%95__20260809.md)：

1. 新增了正式入口页 / 裁决页 / 合同页（A5_*）
2. 新增或升级了运行时脚手架（02_runtime/<新家族>）
3. 新增或升级了工具脚本（20_tools_workspace/batch_*）
4. 从旧仓吸收了一批正式内容进新仓
5. 改变了当前主线推进方向或开工口令

回填只写 3 件事：
- ✅ 今天新增/改动了什么
- ⚠️ 有什么新的边界或阻塞
- 🚀 下一次开工的最小启动点

---

## 旧仓合法回切场景（只有这 5 件事才切旧仓）

1. 清老尾巴
2. 查看或整理历史上下文
3. 暂存区 / scratch 区收口
4. 试验、讨论、待裁决材料继续推进
5. 从旧仓筛选下一批「结果明确」的吸收物

除此之外，默认不要把工作重心拉回旧仓。
