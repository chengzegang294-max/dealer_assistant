# Root Notes

## 这份文件是干什么的

- 这是一张放在仓库根目录的备注说明卡。
- 目的不是替代各层 README，而是让人一进 `trading_assistant` 就先知道这个仓库现在是什么、该先看哪里、哪些事情不要做。

## 这个仓库现在的定位

- `仓库根目录` 是新的主工作根目录。
- `旧仓库` 继续保留为旧仓库和历史来源仓，不再作为后续工作的默认落点。
- 新内容进入这里时，必须是按边界、按批次、按职责纳入，不走“整包复制旧目录”的老路。

## 命名口径

- 根目录下的新协调文件，优先使用英文文件名。
- 目录名如果已经承载迁移意义、批次语义或历史上下文，可以继续保留现有风格。
- 文档正文优先以中文说明为主，只要更利于长期维护和日常推进就可以。

## 第一次进仓库先看哪里

- `README.md`
  - 先看整体结构、当前阶段和已落地批次。
- `00_entry\MIGRATION_PRINCIPLES.md`
  - 先看什么能进、什么不能进。
- `00_entry\ARTIFACT_NOTE_CONTRACT.md`
  - 先看运行产物、脚本和备注说明必须怎么绑定，避免把新仓库做成“结果堆放区”。
- `00_entry\FULL_REPO_MIGRATION_MAP.md`
  - 先看旧仓库各层现在应该落到新仓库哪里。
- `04_active_main_docs\batch_01_selected\`
  - 这里是当前长期主文档层，后续主线推进要持续回写这里。

## 当前主线

- `DY-R1 / KD_MTF_P0`
  - 已经完成最小对象包迁入，活跃对象层、运行时层、文档层都已独立化。
- `MT indicator engineering`
  - 目前主推的是 `Volty / XBreaking`。
  - 首批 runtime 批次包在 `02_runtime\mt_indicator_probes\batch_01_volty_xbreaking`。

## 根目录下这类文件应该起什么作用

- 根目录说明文件不应该写成聊天记录。
- 它应该至少回答这些问题里的一个：
  - 这个仓库为什么存在
  - 什么内容可以进入这个仓库
  - 当前正在推进什么
  - 下一步从哪里继续

## 当前不要做的事

- 不要把这个仓库当成 `trading_analysis` 的完整镜像。
- 不要把乱码备份、重复材料、无职责临时文件直接搬进来。
- 不要把 probe、字段草案、语义观察，直接升级成默认交易门控。

## 一句话记忆

- 英文文件名是为了根目录更干净、更稳定；中文说明内容是为了让后续推进更直接、更少歧义。
