# Cursor / Trae 开工标准打开方式

更新时间：2026-08-06

## 结论

Cursor / Trae 本身完全能用。**你感觉它们"容易昏头"，根本不是模型能力问题，是打开方式没按下面 3 步走，导致 AI 每次开新对话都像第一次进项目。**

## 3 步标准打开方式（必须严格执行，任何一步跳过都会复现"钻细节"）

### 第 1 步：先喂统领地图 + 阶段任务卡（5 秒）

新建一个对话，第一件事不是说"帮我写 XXX"，而是：

1. 把 [PROJECT_STATUS_CARD__统领地图.md](file:///D:/Stock/trading_assistant/PROJECT_STATUS_CARD__统领地图.md) 的内容整段贴进去。
2. 再把 [PROJECT_STAGE_TASKS__阶段任务卡.md](file:///D:/Stock/trading_assistant/PROJECT_STAGE_TASKS__阶段任务卡.md) 的内容整段贴进去。
3. 最后加一句明确的开工口令：
   ```
   按统领地图，当前只做阶段任务卡中的待办 1，范围限 d:\Stock\trading_assistant\20_tools_workspace\batch_07_info_live_room_tools。
   做完返回：diff + 风险点 + 验收步骤。不许扩范围。
   ```

### 第 2 步：只给 1 条待办 + 1 个范围（不能同时写"做这 5 件事"）

错误问法（90% 的人都这么问，所以 AI 会钻牛角尖）：
- "帮我把信息直播间修好，顺便优化一下导出格式"
- "金融助手的信息源这块你看怎么弄好"
- "你看看这个项目现在应该做什么"

正确问法：
- "按阶段任务卡待办 1，只改 `batch_07_info_live_room_tools\ingest_info_live_export_v1.py`，把字段合同对齐到 `info_live_export` 的 JSON。返回 diff + 验收脚本。"
- "按阶段任务卡待办 2，只读 `A5_信息源主线当前进度与下一步__20260806.md`，把其中 3.1 的结论重新压缩成 5 句话，不扩写。"
- "按阶段任务卡待办 3，只跑 `孙悟空金牌` 的增量导出脚本一次，把失败原因写成 3 行，不开新方案讨论。"

### 第 3 步：AI 一偏就立刻叫停，先改文档不先改代码

一旦 AI 出现以下行为，你不要顺着它改代码，要立刻打断：

- 一上来就说"我先重构一下整体结构"
- 一口气列 10 条建议
- 写了一大堆合同页、裁决页，但代码/验收没推进
- 开始"为了后续好扩展……"这种话

立刻回一句：

> 停。回到统领地图第 6 条。当前待办是第几？把那条待办收完再谈别的。

然后：
- 如果是你自己刚才提的需求不够窄——**先去改 `PROJECT_STAGE_TASKS__阶段任务卡.md`**，把待办写窄，再让 AI 动手。
- 如果是 AI 自己跑偏——直接让它回"上一步正确输出的位置"继续。

## 角色分工（四个角色别搞混）

### 角色 1：Cursor / Trae = 执行手

- **输入**：统领地图 + 阶段任务卡 + 单条待办 + 单目录范围
- **输出**：代码 / diff / 脚本 / 一次运行产物
- **不该让它做的事**：
  - "你觉得下一步应该怎么走？"
  - "这个项目整体架构怎么样？"
  - "帮我写 30 页合同页"

### 角色 2：Open WebUI `coding-main` = 拆解手

- 只在"要开出下一组 3 条待办"时用它。
- 喂：统领地图 + 上一组待办的验收结果 + 最近进度页。
- 要求它输出：**下一阶段 3 条待办 + 每条的验收门禁**。不许扩到别的。

### 角色 3：Open WebUI `audit` = 守门手

- coding-main 给完 3 条待办后，立刻甩给 audit。
- 只问一个问题：
  > "这 3 条待办是不是符合统领地图 A 阶段目标？哪一条容易把我带进细节？给出替换建议。"

### 角色 4：Kimi 网页版 = 阶段复盘手

- 每做完一组 3 条待办（或每周一次），把下面 4 份材料上传到 Kimi：
  1. `PROJECT_STATUS_CARD__统领地图.md`
  2. `PROJECT_STAGE_TASKS__阶段任务卡.md`（旧版 + 验收结果）
  3. `A5_信息源主线当前进度与下一步__20260806.md`
  4. 最近一批 `info_live_room_sampling/batch_XX` 的真实产出
- 只问一个问题：
  > "下一组 3 条待办应该是什么？为什么？哪一条最容易把我带进坑里？"

## 一个真实例子（你可以直接套）

### 正确开场（把这段直接 Ctrl+C 到 Cursor / Trae 里）

```
先读下面 3 份文档：

---
[把 PROJECT_STATUS_CARD__统领地图.md 整段贴进来]
---
[把 PROJECT_STAGE_TASKS__阶段任务卡.md 整段贴进来]
---
[把 A5_信息源主线当前进度与下一步__20260806.md 里 1~80 行贴进来]
---

当前只做：阶段任务卡待办 1。
范围：d:\Stock\trading_assistant\20_tools_workspace\batch_07_info_live_room_tools

验收门禁：
- 只改这个目录下的 ingest_info_live_export_v1.py / build_info_live_room_review_table_v1.py
- 字段合同冻结成 room_anchor / room_host / occurred_at / content / anchor_crc
- 最终给我 1 条 run 命令 + 1 条验收命令

不许做：
- 不许动其他目录
- 不许写新的裁决页/合同页
- 不许把待办 2、待办 3 顺带做了
```

### 错误开场（90% 的情况都在这么用）

```
你看一下我这个项目，信息源这块很乱，帮我把信息直播间修一下。
最好顺手把导出格式也统一一下，UI 上能更直观就更好了。
```

这个错误开场的后果：
- AI 一定会开始"想太多"
- 一定会去动不该动的文件
- 你一定会在 3 轮对话后觉得它钻牛角尖

## 最后一句：为什么之前"花了好多时间和好多钱还是卡在原地"

因为你把 **执行手（Cursor/Trae）当统领手用**。
执行手就像司机，你得先给它目的地 + 走哪条路 + 不能走哪条路；你把司机当导航员兼地图测绘员兼交通规划师用，它当然会绕死。

统领这件事，**现在已经不缺新 AI，缺的是你每次对话开头那 3 份文档和 1 条单待办指令**。
