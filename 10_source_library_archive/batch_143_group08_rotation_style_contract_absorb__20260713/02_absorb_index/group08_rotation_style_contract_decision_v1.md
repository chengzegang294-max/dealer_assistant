# GROUP_08 行业轮动与风格轮动项目裁决 v1

## 主负责人裁决

- 当前允许进入正式合同层的行业轮动 / 风格轮动子集：
  - `A-C01` 残差动量多空行业配置模型
  - `A-C02` 涨跌比行业择时与轮动体系
  - `A-C03` 板块效应动量-反转交叉 Alpha
  - `A-C04` 公募基金持仓测算风格轮动模型

## 为什么先冻结这条线

- 它已经有：
  - 主题入口
  - 对象入口
  - 研究摘要
  - 四个可重开候选
- 也足够形成：
  - 最小输入
  - 最小输出
  - 参数边界
  - 不接线说明

## 当前不升级为默认逻辑

- 不直接升级为 A股 P0 默认配置引擎
- 不把行业多空组合直接写成当前默认仓位模板
- 不把风格轮动信号直接写成当前首页主信号
- 不把论文中的对冲实现默认转成当前 runtime 逻辑

## 当前可以怎么用

- 作为：
  - 合同层固定资产
  - 研究对象冻结入口
  - 后续验证与 runtime 接线候选
- 不作为：
  - 当日真实交易指令
  - 已验证完毕的生产级配置系统

## 暂不入合同层

- `future bucket` 中的融资融券杠杆策略
- 跨行业统一基本面预测框架
- 股指期货日内过度反应模型
- 未披露融合规则的多周期 KAMA 配置模型
