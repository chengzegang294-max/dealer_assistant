# 趋势系统交易：四轴状态模板（后续对象定义入口 v1）

## 目标

把 `GROUP_05_趋势_系统交易` 中的“四轴状态模板”收敛为可重开对象入口：先作为研究/诊断标签体系，不直接进入交易门控或仓位引擎。

## 来源锚点

- 候审整理稿入口：
  - `10_来源库_SOURCE_LIBRARY\01_Kimi拆书待入库\GROUP_05_趋势_系统交易\GROUP_05_trend_system_trading_STATE_TEMPLATE.md`

## 当前范围（v1）

- 只承接“状态模板”的结构与字段候选，不承接具体策略执行细节。
- 只做 `diag-only`：
  - 允许落盘诊断标签
  - 禁止默认变成硬门控
- 不做：
  - 真实下单执行
  - 资金曲线优化
  - 多资产组合权重

## 四轴定义（候选）

- 结构轴（Structure Axis）：市场结构与价格行为
- 偏置轴（Bias Axis）：交易者心理偏置与群体行为
- 摩擦轴（Friction Axis）：交易成本、滑点与执行难度
- 风险轴（Risk Axis）：资金管理与尾部风险

## 最小可落地字段（候选 v1）

说明：字段名先固定为“诊断标签”，后续若进入运行时层，再补合约表头与枚举值冻结。

- `trend_existence_state`
  - 候选：`trend_up` / `trend_down` / `no_trend` / `unknown`
- `breakout_validity_state`
  - 候选：`valid` / `false_breakout_risk` / `unknown`
- `volatility_regime_state`
  - 候选：`low` / `normal` / `high` / `shock`
- `crowd_extremity_state`
  - 候选：`overbought` / `oversold` / `neutral` / `unknown`
- `transaction_cost_load_state`
  - 候选：`acceptable` / `heavy` / `unknown`
- `liquidity_adequacy_state`
  - 候选：`adequate` / `thin` / `unknown`

## 复审口径（严格）

在进入“正式可用对象”前，至少满足：

- 每个字段有明确的可观测证据定义（可计算或可审计）
- 每个字段有可证伪判据（失败条件）
- 每个字段的来源锚点可追溯（文件路径 + 段落范围）

## 下一步（建议顺序）

1. 先从结构轴挑 `1-2` 个字段做成最小实现入口（类似 `多周期KD` 的 proof-of-mapping）。
2. 再从摩擦轴挑 `1` 个字段做成执行层约束标签（仍保持 diag-only）。
3. 完成一份最小 proof 输入/输出样本后，再讨论是否需要工具运行时目录空壳。
